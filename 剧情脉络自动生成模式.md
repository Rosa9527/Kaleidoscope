# 剧情脉络自动生成模式（时间 × 地点 沙盒版）

> 你的任务：把一章小说的完整原文，转换成一个**时空沙盒**——玩家从某个时间点开始，在一张地图上自由移动；本模式保证：**玩家在规定时间到达规定地点，就能触发小说中相应的剧情**。
> 依据：《使用指南》的剧情脉络章节（操作格式）与本文档「核心理念」一节（思路）。
> 交付：主产物 `<章号>-剧情脉络.yaml` ＋ 附产物 `<章号>-生成说明.md`（含 Trigger 走查记录）。

---

## 0. 核心理念

### 0.1 沙盒：时间 × 地点

RPG 有两个维度：**地点**与**时间**。剧情不是线性喂给玩家的，而是嵌在由（时间段 × 地点）构成的格子里：

- 小说是线性文本，RPG 是时空沙盒。转换的本质 = 把小说剧情**填进格子**。
- 预筛 AI 每轮只读事件目录与最近对话，判断"该不该触发"。玩家在对话里能核实的事实只有两件：**我人在哪、现在是什么时候**——位置与时间就是最可靠的触发依据。
- 全部工作浓缩为一句话：**玩家在规定时间到达规定地点，剧情就触发**。没到，不触发；到了，必触发。
- **沙盒不替玩家做选择**：地图上的每个地点都摆出来、剧情挂好。玩家没到的地方，剧情安静地等在那里——万一玩家就是想来呢？来了就有剧情。
- **玩家只会做"他知道的事"**：玩家的行动依据只有两个——**看得见的地图**（建档地点）与**已发生的剧情**（提示过的信息）。触发条件也只能从这两者取材。

### 0.2 四条铁律

1. **trigger 是唯一触发依据**——四要素（时间、地点、人物、触发条件）写全；触发条件以**"玩家"为主语**（不写"你"）；每个要素都能从"最近对话 + 正文首行日期"核实。
2. **到达即触发**——每个格子的**第一个事件**，触发条件 = `时间，地点，玩家位于此处`：玩家不需要任何额外动作，到了就有剧情。动作触发只用于格子内后续事件的衔接。
3. **够得着**——触发条件只从两类玩家已知信息取材：**① 建档地点**（地图可见）；**② 先前事件提示过的信息**。角色习惯动作、未提示的信息、未建档的地点一律不成立。
4. **日期是唯一锚点**——正文首行写日期与时段；trigger、正文、时间节点三者一致。

### 0.3 术语表

| 术语 | 含义 |
| --- | --- |
| 地图 | 小说出现的**全部**地点池；每个建档地点都出现在玩家可见的地图上 |
| 时段节点 | 时间段单位，默认一天两段（上午/下午），按剧情可加早晨、中午、傍晚、夜晚、深夜 |
| 格子 | （时段节点 × 地点节点）组合：该时段该地点的剧情槽位 |
| 在场触发 | 基准触发形态：`时间，地点，玩家位于此处` |
| 触发条件 | 玩家的动作/在场/状态；只取材于建档地点与先前提示；主语写"玩家" |
| 渐进触发 | 同一格子内多事件按剧情顺序依次触发：在场 → 动作/前置状态 |
| 切镜地点 | 玩家通常不在场的地点（学院本部、他人家）；照常建节点挂事件，玩家到了才触发 |
| 你得知 | 主线关键信息的兜底渠道：玩家可能错过场景时，经手机/群聊/传闻/当面听闻流入 |
| 假想执行 | 走查最后一关：说出一个具体的游戏内时刻，让 trigger 第一次成立 |
| if 线 | 选择点备选走向的推断事件 |
| 规则事件 | 全局永久生效设定的注入事件（R8） |

---

## 1. 输入与输出

### 1.1 输入

**必需**：一章小说的完整原文（直接粘贴，或给出文件路径）。

**可选**（用户提供则直接采用；缺省按流程推定，并记入生成说明）：

- 章节号（事件 id 前缀、跨章合并用）；
- 主角 / 玩家角色名（多视角小说必须确认）；
- 玩家角色的**行动范围**（身份、位置、日程；决定哪些格子会被自然走到、"你得知"兜底渠道放哪里）；
- 日期基准（如"上一章结尾是 2026年8月10日 晚上"）；
- 已有剧情脉络文件（地图与时间表跨章累积，合并依据）。

### 1.2 输出（写入会话工作目录）

1. **主产物** `<章号>-剧情脉络.yaml`：kaleidoscope-story 格式，可直接导入"剧情脉络"。
2. **附产物** `<章号>-生成说明.md`：全部推定与假设 + **Trigger 走查记录**（每个事件一行，见第 8 步）。推定与记录不进入 YAML，避免破坏导入格式。

### 1.3 工作约定

- 一次只处理一章；多章逐章运行，章号递增保证事件 id 全局唯一。
- 原文超长：先通读建立地图与剧情单元清单，再逐段细读补齐细节；分析在内部完成，只交付两个文件。
- 判断冲突时，一律回到 0.2 四条铁律裁决。

---

## 2. 处理流程（九步）

### 第 1 步：输入确认

通读原文，确认：主角身份（玩家扮演谁）、**行动范围**、时间范围、全部出场人物。

- 缺关键信息：能推定的推定（记入生成说明）；必须用户拍板的才提问一次（无日期锚点时的基准日期、多视角下的玩家角色）。
- 用户给的背景信息直接采用，不重新推定。

### 第 2 步：建地图（提取地点）

- 小说出现的**所有地点都建档**（含切镜地点：学院本部、他人家），沙盒不替玩家排除任何地方。
- 行动范围决定的是"哪些格子会被自然走到"，不是"哪些地点能建节点"；玩家到不了的地点，事件因无人触发而安静等待，没有副作用。
- 每个地点节点 `description` 写氛围或可做的事。**建档 = 进入玩家可见的地图：玩家知道有这里，去了不奇怪；未建档的地方玩家不知道，不会去——剧情需要玩家去的地方，必须先建档。**
- 地点池 ≥5 个时，生成一条"地点总览"规则事件（玩家询问或进入新地点时注入地点图景）。

### 第 3 步：建时间表（划分时段）

- 默认一天两段：`2010年8月7日 上午`、`2010年8月7日 下午`；原文有明确的早晨/中午/傍晚/夜晚/深夜剧情 → 相应加段。
- 日期推定，优先级从高到低：
  1. **原文显式日期**（"8月11日""2026年8月11日"）直接采用；缺年份沿用基准年。
  2. **相对词推定**："第二天/次日/隔天"→ +1 天；"当天/同日"→ 同日期；"一周后"→ +7 天；"当晚/深夜"→ 同日夜晚；其余时间词按字面。
  3. **无任何锚点**：询问基准日期一次（给出推荐值）；用户不提供则以 `2026年1月1日` 起算，生成说明显著标注"锚点日期为推定，可整体替换"。
- 每个时段节点 `description` 写该时段主线目标一句话。
- 跨多日的过渡段合并为时间段节点（`2026年7月10日~2026年7月17日`）。
- **切镜地点按当地时区建时段节点**：如本部深夜 = 玩家时区上午时，本部格子建在 `2010年7月16日深夜（美国时间）` 下，trigger 标注时区——玩家若真在场，核实的也是当地时间。

### 第 4 步：剧情梳理（原文 → 剧情单元）

按"时间或地点发生明确切换"把原文切成**剧情单元**（连续发生在同一时段、同一地点的剧情），每个单元标注：

```
单元N | 时段：__ | 地点：__ | 在场人物：__
剧情摘要：……
关键对白/信息点：……
选择点：□ 无  □ 有（……）
结束状态：……
```

- 一个自然段落内的连续动作不切分；时间跳变（"第二天"）、地点跳变（"他回到书房"）必须切分。
- **切镜段落单独标记**：挂进该地点自己的节点（R10）；**不得**挂到玩家所在格子。
- **判主线兜底**：切镜内容含玩家必须知道的关键信息（如任务任命与玩家直接相关）→ 在玩家可达地点备一条"你得知"渠道。

### 第 5 步：填事件（剧情单元 → 格子）

把每个剧情单元填进对应格子，转成一个或多个事件（格子规则见 R9，沙盒规则见 R10）：

- 每个事件写四件套：`id`（章号2位+序号3位，如 `"01001"`，数字样加引号、全局唯一）、`name`（动词化、具体）、`trigger`（按 3.1）、`content`（按 3.3）；`description` 可选（提示事件 / if 线在此标注）；`effects` 可选（事件触发时确定性修改变量系统的父变量：`[{ path, op: add|set, value }]`，与「剧情触发」的事件效果同款，见「变量系统指南.md」第八章）。
- **信息不丢**（R6）：关键对白、事实、状态变化必须落在某个 content 里；压缩的是过渡描写，不是信息。
- **回忆/闪回类内容**：trigger 挂在玩家**当前时间线**的自然动作上（该动作须有先前提示作依据），content 开头标注"（回忆：……）"；禁止把回忆内容写成当前正在发生的动作（✗ `玩家在婶婶指挥下切萝卜、腌萝卜时`——切萝卜是回忆/下午的家务，当天早晨根本没这回事）。

### 第 6 步：选择点与 if 线

识别关键选择：接受/拒绝/答应/选择/决定/犹豫、二选一、立场取舍、邀约与答复……

- **选择场景本身** = 一个事件：trigger 写选择发生前的自然动作或在场；content 呈现选择并给出选项。
- **每条备选走向** = 一个 if 事件：挂在同节点链下；trigger 写**选择后的可核实状态**（`玩家答应A的邀约后`）；content 写开端、状态变化与可接续的钩子，标注为推断。
- **禁止把结果写进 trigger**："玩家拒绝A时"这类发生在选择场景里的结果，预筛无法核实，必须留在原事件 content，由 if 事件的"选择后状态"接续。分工：原事件负责选择场景，if 事件负责选择后的发展，两者条件不重叠。
- 克制：只对影响后续走向的关键选择生成 if 线，单章 0~3 条；小说没呈现的分支只写"开端+状态"，不补全结局。

### 第 7 步：规则事件

- 只写原文确立的、**全局永久生效**的设定（如"只有你能看见隙间"），且能写成"场景出现 → 发生某事"（R8）。
- 人物介绍、世界观、力量体系不写——那是角色卡或预设的事。
- 挂 `rule-events` 根节点；单章通常 0~2 条，没有就不写。

### 第 8 步：Trigger 走查（必做，逐条走查，不许走过场）

对**每一个**事件（主线/支线/if/规则），把 trigger 逐字读出来，按 5.1 走查清单六项核对，**当场写下核对记录**（每个事件一行，写入生成说明"Trigger 走查记录"节）。全部合格才进第 9 步；任一不合格 → 按 3.5 修法修，修完**重新走查该事件**。

核对记录格式（一个事件一行）：

```
事件 059005 | trigger：2010年7月17日 早晨，叔叔婶婶家，玩家准备出门买香肠小葱时
主语✓ 四要素✓ 动作来源：059004 婶婶差遣"下去买香肠小葱"✓ 禁项✓ 格子关系：非首事件(首=059004 在场触发)渐进衔接✓ 假想执行：玩家交完报告被差遣、起身准备出门时闪回✓
```

**假想执行是最后一关**：设想玩家正在玩，说出一个具体的游戏内时刻让该 trigger 第一次成立。说得出 = 合格；说不出（"玩家不会做这个动作""玩家不知道这个地方"）= ✗，按 3.5 修。

### 第 9 步：结构、覆盖与门槛（必做）

按 5.2 结构 / 5.3 覆盖 / 5.4 门槛逐项核对；推荐运行 `node 剧情脉络校验.cjs <章号>-剧情脉络.yaml`，全部 PASS 再交付。**不达标不出文件。**

---

## 3. 写作规范（规则编号为唯一事实源，走查时逐条对照）

### 3.1 trigger（R1~R4）

**基准形：`时间，地点，玩家位于此处。`**——触发条件的根本是"某个时间段走到某个地点"。**主语一律写"玩家"，不写"你"**（"你"只属于正文 content 的视角；预筛对账的是玩家，不是角色）。

四种触发形态（按场景选最顺的一种）：

| 形态 | 写法 | 示例 |
| --- | --- | --- |
| 在场触发（基准） | `时间，地点，玩家位于此处时` | `2010年8月7日 上午，悠远咖啡，玩家位于店内时` |
| 进入触发 | `时间，玩家走进某地时` | `2010年8月7日 上午，玩家走进悠远咖啡时` |
| 动作触发 | `时间，地点，玩家做某动作时` | `2010年8月7日 上午，叔叔婶婶家，玩家打开笔记本电脑时` |
| 选择后状态（if） | `时间，地点，玩家答应A之后` | `2010年8月7日 上午，悠远咖啡，玩家答应陪林晓调查之后` |

规则事件不适用上表：写触发场景（`隙间出现或旁人提及隙间时`），确有限制才写时间地点。

**R1 四要素与主语**：时间、地点、人物、触发条件写全；主语一律"玩家"；每个要素都能从"最近对话 + 正文首行日期"核实。

**R2 触发形态与禁项**：触发条件 = 玩家的动作/在场/状态；禁结果词（看到/听到/得知/发现/收到）；**他人动作只在"玩家在场"前提下可作为触发观察点**（玩家亲眼看到、亲耳听到才算可核实）：

- ✗ `值班教授曼施坦因处理全球突发报告时`——他人动作且玩家不在场，不可核实；
- ✗ `你打开电脑登录日常报告系统时`——主语是角色；且无任何提示，玩家不会去做 → 正解：首事件写"玩家位于此处"，把"打开电脑"写进剧情由剧情引导；
- ✓ `2010年7月17日 早晨，玩家位于（叔叔婶婶家）的卧室中`——首事件的正确写法：玩家不需要任何动作，位于即触发；
- ✓ `玩家位于店内时，林晓神色慌张地进门找你`——玩家在场的他人动作，玩家看得到，可核实。

**R3 前置状态（"完成〈X〉后"）**：只在**真实因果依赖**时写——同一格子内顺序剧情的衔接（`玩家听完林晓的求助、答应陪她调查时`：话听完才知道下一步）、if 的选择后状态。与触发动作无逻辑关系的前事一律不写（看新闻与发短信无关）。

**R4 两类来源（够得着）**：触发条件只从两类玩家已知信息取材——玩家只会做"他知道的事"：

- **① 建档地点**：地点节点已建（出现在玩家可见的地图上）→ 玩家知道有这里，到达不奇怪。未建档的地点（如"卡塞尔学院的后山"不是节点）→ 不能作触发地点；剧情需要玩家去 → **先建档**。
- **② 先前事件提示过的信息**：提示过"电脑发出声响，应该有新邮件" → 查邮件是自然动作；先前的选择 → 后续状态可作触发（if 线）。未提示过的信息 → 不能要求玩家据此行动。

答不出依据 → 放宽为在场触发（M1）：剧情在玩家在场时发生，在剧情里自然引导动作（引导本身也是一种提示）。

### 3.2 格子与沙盒（R7、R9、R10）

**R7 条件唯一**：同一格子内事件条件不重叠（渐进触发互斥）；不同格子的事件不重复。

**R8 规则事件**：只写原文确立的、**全局永久生效**的设定（如"只有你能看见隙间"），且能写成"场景出现 → 发生某事"；挂 `rule-events` 根节点，trigger 只写触发场景（确有限制才写时间地点）；单章通常 0~2 条，与剧情事件不重复信息。

**R9 格子规则**：每个格子的**第一个事件** = 在场触发（`时间，地点，玩家位于此处`）——玩家不需要任何额外动作，到达即触发；同一格子多个事件用**渐进触发**衔接：在场 → 动作 → 真实前置状态。

**R10 沙盒完整性**：

- 地图收全小说所有地点（含切镜地点），都建节点、挂事件，触发条件 = 玩家位于此处——玩家到了就触发，没到就安静等待，**不误触发、不漏触发**；
- 禁止把切镜内容挂到玩家所在格子、用玩家动作做 trigger 却注入他处剧情（✗ 挂在 `叔叔婶婶家` 下、正文却是卡塞尔本部 = 全知旁白注入）；
- **"你得知"只作主线兜底**：玩家可能错过关键信息（如任务任命与玩家直接相关）时，在玩家可达地点设可信渠道（手机/群聊/传闻/当面听闻），与切镜场景事件条件互斥；平时不用，信息保全记入生成说明即可。

### 3.3 content（R5、R6）

**R5 日期锚点**：首行 = `日期，时段。`，与 trigger、所在时段节点一致——预筛核实游戏内"今天"的唯一依据。

**R6 信息不丢**：保留原文关键对白（原文或最小改写）；压缩过渡性描写，保留信息点与氛围；结尾交代事件结果/状态变化，供后续事件承接。

- 第二人称（你），可体验视角：环境、动作、对白、信息、结果。
- 篇幅一般 100~400 字；关键场景可更长但克制——预筛注入的是正文全文，太长会稀释对话。

### 3.4 事件类型写法

| 类型 | trigger 写法 | content 要求 |
| --- | --- | --- |
| 主线/支线 | 在场/进入/动作（R1~R4、R7、R9、R10） | 该格子该触发单元的完整信息 |
| 被动信息型 | 日常动作（`玩家刷手机时`）；玩家几乎必然做该动作时，可直接只写"时间+地点"（在场触发） | 信息随动作/场景注入；"你得知"（主线兜底）也归此类（R10） |
| if 线 | 选择后的可核实状态（`玩家答应A的邀约后`） | 标注"（推断走向）"；写开端+状态+钩子，不补全结局 |
| 规则事件 | 触发场景（R8） | 标注"（长期规则）"；全局适用 |

### 3.5 修法优先级（事件不合格时，从高到低）

1. **M1 放宽为在场触发**——把动作触发（`玩家打开电脑时`）放宽为 `时间，地点，玩家位于此处`（最宽：到达即触发）；剧情内容照常注入，在剧情里自然引导玩家做动作。
2. **M2 给提示**——目标事件缺"玩家已知依据"：先写一个事件把依据立起来（差事、邀约、消息、声响、目击……即"②先前提示"），让目标事件跟在其后；每章 ≤1 条。
3. **M3 你得知兜底**——主线关键信息玩家可能错过场景：在玩家可达地点设"你得知"渠道（手机/群聊/传闻/当面听闻）；非关键信息省略并记入生成说明。

---

## 4. 输出模板

```yaml
format: kaleidoscope-story
version: 1
character: 主角名            # 可选：主角名
nodes:
  # ===== 剧情类事件：时间段 → 地点 → 事件 =====
  - id: plot-events
    name: 剧情类事件
  # 时间段节点：一天分上午/下午（按剧情可加早晨、中午、傍晚、夜晚）
  - id: t-2026-08-07-am
    parentId: plot-events
    name: 2026年8月7日 上午
    description: 当日主线目标一句话
  - id: loc-coffee-0807-am
    parentId: t-2026-08-07-am
    name: 悠远咖啡
    description: 老街尽头的咖啡店，暖黄的灯光
  - id: char-lin-0807-am
    parentId: loc-coffee-0807-am
    name: 林晓
  # 同一地点另一个时段 → 各自一个节点（格子不同）：
  - id: t-2026-08-07-pm
    parentId: plot-events
    name: 2026年8月7日 下午
    description: 调查书店怪事
  - id: loc-coffee-0807-pm
    parentId: t-2026-08-07-pm
    name: 悠远咖啡
  # ===== 规则类事件 =====
  - id: rule-events
    name: 规则类事件

scripts:
  - id: "01001"             # 章号2位 + 序号3位；数字样 id 必须加引号
    nodeId: loc-coffee-0807-am
    name: 林晓的求助
    trigger: 2026年8月7日 上午，悠远咖啡，玩家位于店内时
    description: 主线：得知书店怪事（在场触发）
    content: |-
      2026年8月7日，上午。
      你正翻开桌上的菜单，咖啡店的门被猛地推开。林晓脸色发白……
  - id: "01002"             # 同一格子的第二个事件：渐进触发
    nodeId: loc-coffee-0807-am
    name: 答应陪林晓调查
    trigger: 2026年8月7日 上午，悠远咖啡，玩家听完林晓的求助、答应陪她调查时
    content: |-
      2026年8月7日，上午。
      你答应陪林晓调查书店的怪事，她攥紧了你给的旧笔记本……
```

**id 方案**：

| 对象 | id 写法 | 示例 |
| --- | --- | --- |
| 根节点 | 固定 | `plot-events` / `rule-events` |
| 时段节点（单时段） | `t-YYYY-MM-DD-<时段>`（am=上午 pm=下午 morning=早晨 noon=中午 eve=傍晚 night=夜晚） | `t-2026-08-07-am` |
| 时段节点（大跨度） | `t-YYYYMMDD-YYYYMMDD` | `t-20260710-20260717` |
| 地点节点 | `loc-<关键词>-<MMDD>-<时段>`（跨年用 YYMMDD） | `loc-coffee-0807-am` |
| 人物节点 | `char-<关键词>-<MMDD>-<时段>` | `char-lin-0807-am` |
| 事件 | `"<章号2位><序号3位>"` | `"01001"` |

设计意图：日期+时段进 id，保证**跨章合并导入**时（同 id 更新、其余追加）同一天同时段的节点自然合并、事件永不互相覆盖；时段进 id 让"上午的悠远咖啡"与"下午的悠远咖啡"是不同格子。

---

## 5. 走查与质检（输出前必做）

### 5.1 Trigger 走查清单（第 8 步执行，对每个事件核对并留痕）

1. **主语**（R1）：主语是"玩家"？（✗ "你"、角色名、他人）
2. **四要素**（R1、R5）：齐全；时间/地点与所在节点链一致；content 首行日期+时段一致？
3. **来源**（R4）：地点类 → 该地**已建档**？动作类 → **指出提示来源**（哪条事件、哪句话）？选择状态类 → 选择在先前事件呈现过？
4. **禁项**（R2、R3）：无结果词；无玩家不在场的他人动作；无无关前置状态；回忆不冒充当前动作（content 标注"（回忆：…）"且 trigger 挂在当前动作上）。
5. **格子关系**（R7、R9、R10）：本格子第一个事件是"玩家位于此处"？同格子事件渐进互斥？切镜地点建了节点并挂在场触发事件？无全知旁白注入？"你得知"只作主线兜底？
6. **假想执行**（R4 最后一关）：说出一个具体的游戏内时刻让该 trigger 第一次成立。说得出 = 合格；说不出 = ✗ → 按 3.5 修。

### 5.2 结构（YAML 语法）

- 文件头带 `format: kaleidoscope-story` 与 `version: 1`；恰好两个根节点：`剧情类事件`、`规则类事件`；
- 数字样 id 加引号；全部 id 唯一；缩进一致（列表项 `- ` 对齐，正文比 `content:` 多缩进两格）；
- 层级：时间下只挂地点；地点下只挂事件或人物；人物下只挂事件；事件必须挂在地点或人物下，不挂时间下；
- 同一地点不同时段各建节点；每个事件 trigger 与所在节点链（日期/时段/地点）对得上；
- 时间段事件 trigger 写"某日至某日期间"，不写"旅途中""放假时"这类无法核实的词；
- 时间表连续：本章覆盖的时间段没有断档（跳过的时段在生成说明里说明原因）。

### 5.3 覆盖

- 原文每个剧情单元 ≥1 个事件（有格子落点）；关键对白与信息无遗漏（R6）；本章开头与结尾状态在事件链上连贯；
- 切镜地点都建了节点并挂上在场触发事件；未建节点或未设"你得知"的关键信息（下章要用的状态/伏笔）已记入生成说明；
- 规则事件与剧情事件无信息重复（R8）。

### 5.4 门槛

- 全部推定与假设写入生成说明；
- 推荐运行校验脚本复核结构：`node 剧情脉络校验.cjs <章号>-剧情脉络.yaml`，全部 PASS 再交付；
- **不达标不出文件**：宁可回到对应步骤重做（触发问题 → 第 5 步填事件或第 8 步走查；结构/覆盖问题 → 第 9 步），不交付残缺脉络。

---

## 6. 常见问题

### 触发

| 情况 | 处理 |
| --- | --- |
| 玩家到达某地却没有剧情 | 该格子补一个在场触发事件：`时间，地点，玩家位于此处`（M1） |
| 格子第一事件写成具体动作（如"打开电脑交报告"） | 玩家不会为小说角色的习惯动作买单：放宽为 `时间，地点，玩家位于此处`，把动作写进剧情、由剧情引导 |
| trigger 主语写成"你……" | 统一为"玩家……"："你"只属于 content 的正文视角 |
| 回忆/闪回内容被写成当前动作 | 挂错时间线：trigger 改用玩家当前自然动作（如"准备出门时"），content 标注"（回忆：…）" |
| 玩家没有理由去某个地点 / 做某动作 | 玩家只做他知道的事：先把该地**建档**，或用提示事件立起依据（差事/邀约/消息/声响……）（M2，每章 ≤1 条） |

### 格子与切镜

| 情况 | 处理 |
| --- | --- |
| 同一时间同一地点有多段剧情 | 渐进触发：在场 → 动作 → 真实前置状态，条件互斥（R7、R9） |
| 剧情发生在玩家到不了的地方（切镜） | 照常建节点挂事件，触发 = 玩家位于此处（沙盒完整性）；主线关键信息另设"你得知"兜底；其余记入生成说明（R10） |
| 想把切镜剧情塞进玩家所在格子 | 不行，那是全知旁白注入：把剧情挂到它自己的地点节点下，玩家到了自然触发 |

### 时间与合并

| 情况 | 处理 |
| --- | --- |
| 时间段怎么划 | 默认上午/下午；原文有明确晨午晚剧情就加时段 |
| 原文没有日期 | 询问基准日期一次；拒绝则 `2026年1月1日` 起算，生成说明显著标注"锚点可整体替换" |
| 一章跨多日 | 每天按时段建节点，日程骨架照排；过渡段用时间段节点 |
| 与已有脉络合并 | 章号递增即可按序合并导入；同一天同时段节点 id 一致会自然合并 |
| 事件正文太长 | 压缩过渡描写；正文只承载该格子该触发单元的信息 |

---

## 7. 运行方式（会话内）

1. 读取用户提供的章节原文（粘贴或文件路径）。
2. 按第 2 节九步在内部完成分析（先建地图与时间表，再逐段细读填事件）。
3. 用文件写入工具输出主产物 `<章号>-剧情脉络.yaml` 与附产物 `<章号>-生成说明.md` 到工作目录。
4. 按第 5 节走查与质检；推荐运行 `node 剧情脉络校验.cjs <章号>-剧情脉络.yaml`，全部 PASS 再交付。
5. 汇报：地图（地点数）、时间表覆盖、事件数、每个格子的事件与在场触发情况、**Trigger 走查结果（全部事件逐条核对、假想执行通过）**、if 线、规则事件、需要用户复核的推定。
