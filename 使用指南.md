# 万华镜（Kaleidoscope）核心功能使用介绍

> 本文件面向使用者，只讲**怎么用**。详细语法与设计见文末「深入阅读」。
> 插件版本：v1.4.0

---

## 一、打开插件

安装后，屏幕边缘会出现一个**彩虹风车悬浮球**：

| 操作 | 效果 |
| --- | --- |
| 拖拽 | 移动悬浮球位置（松开后停住，不触发其他动作） |
| 点击 | 打开万华镜主面板 |
| 长按 | 隐藏悬浮球（再打开面板可在设置中恢复显示） |

另外，宿主的**扩展菜单**里也有「万华镜」入口（自动注册 + 手动注入双保险），两种方式都能打开面板。

主面板打开后是「首页」：顶部 Logo + 一排功能卡片（剧情脉络 / 变量 / 注入实录 / 游戏模式 / 系统日志 / API 连接……），点击卡片即可进入对应功能。

---

## 二、第一步：配置 API（必做）

点首页「**API 连接**」卡片，填写：

- **Base URL**：模型服务地址（如 `https://api.example.com/v1`）
- **API Key**：访问密钥
- **模型**：从「模型列表」里选择，或手动填写模型名
- 并发限制 / 思考强度：按需调整，默认即可

跨域请求会自动走宿主代理，代理不可用时自动回退直连，一般无需手动干预。保存后即可正常使用依赖 AI 的功能（剧情预筛、变量自动维护）。

---

## 三、剧情脉络（核心功能）

把预先设计好的剧情按**树状结构**组织：节点是容器（如「第一章」「张三线」），节点下挂子节点或事件；事件就是一段会在运行时被注入上下文的正文本。

入口：首页点「**剧情脉络**」卡片（手机端为面板内视图）。

**基本操作：**

- **新建**：点「＋ 新建」→「新建节点」或「新建事件」；在节点行的「＋」可新建该节点下的子节点 / 事件。
- **编辑**：点条目右侧 ✎ 图标；事件编辑时需填写名称、触发条件（决定何时触发）与正文；还可选配**事件效果**——事件触发时确定性修改变量系统的父变量（加减值 / 覆盖，详见「变量系统指南.md」第八章）。
- **删除**：点条目右侧 🗑 图标（删除节点会连带其子节点与事件，删除前有确认弹层）。
- **开关**：节点行有开关滑块——关掉后，该节点及其所有子节点、事件都不再参与预筛注入。
- **导入 / 导出**：事件行可单独导出为 YAML 文件；节点行可导入事件文件（导入方式可选合并或替换）；面板顶部有整树导出。
- **角色卡绑定**：工作台头部徽标显示绑定状态——已绑定角色卡时，剧情数据随角色卡导入 / 导出自动携带；未选角色 / 群聊时保存在全局设置。

> 触发条件与事件正文的具体写法见「剧情脉络自动生成模式.md」。

---

## 四、剧情预筛（Story Gate，自动运行）

当你在聊天里发送消息时，插件会**自动**在后台做一轮「剧情预筛」：

1. 把剧情目录 + 最近几条消息发给 AI；
2. AI 返回本轮应触发的事件；
3. 触发事件的正文被注入到本次生成请求里，AI 生成回复时会带上剧情内容。

整个过程**无需手动操作**——你正常聊天即可。预筛失败时会直接放行（不会卡住发送），并发上与其他扩展的发送前任务并行执行。

想确认 AI 这轮到底预筛出了什么？打开首页「**注入实录**」卡片：展示最近一轮的预筛原文、触发的事件（含触发条件 / 正文）、以及最终注入的提示词全文，方便排查「为什么没触发」。

---

## 五、提示词预设

剧情预筛和变量维护各有一段提示词在工作，万华镜把它们**打包成一套「预设」**来管理：

> **一个预设 = 一套完整的「剧情预筛 + 变量维护」提示词配置。**

入口：首页点「**预设模版**」卡片。

**界面布局**（从上到下）：

- **系统开关**：剧情预筛 / 变量自动维护 / 剧情触发三个总开关（与提示词内容无关，切换预设不影响开关状态）。
- **预设选择行**：下拉框切换当前生效的预设 + 「📝 另存」「🗑 删除」「⬆ 导出」「⬇ 导入」。
- **提示词编辑区**：「剧情预筛」「变量维护」两个标签页，各自编辑当前预设里的提示词。

**基本用法：**

- **切换预设**：下拉框选择即可，切换后两组提示词整体替换为该预设的内容并立即生效（切换预设不复制文本，每个预设各自持有完整内容）。
- **修改当前预设**：直接在编辑区改，点「💾 保存」写入当前选中的预设。
- **另存为新预设**：改完点「📝 另存」，输入名字即可把当前两组提示词（含未保存的修改）保存为一个新预设并自动激活——适合在默认基础上微调出「RP增强版」「高精度版」等。
- **删除预设**：选中自定义预设后点「🗑」；**内置「默认预设」不可删除也不可覆盖**，永远作为兜底存在。
- **导入 / 导出**：「⬆ 导出」把当前选中的完整预设（名称 + 两组提示词）存为 YAML 文件分享给别人；「⬇ 导入」选择预设文件后写入当前角色卡并激活，同名自动编号不会覆盖。

**存储位置**：自定义预设与剧情脉络一样**保存在角色卡数据里**——切换角色卡时读取的是那张卡自己的预设；群聊 / 未选角色时保存在全局设置。角色卡没有预设时，自动使用插件内置的「默认预设」（兜底配置，永不丢失）。

> 激活「默认预设」时：直接修改提示词点保存 = 全局生效（老用法不变）；激活自定义预设时：修改只影响该预设。

---

## 六、变量系统

给角色 / 聊天维护一套数值型变量（如「好感度 = 30」），由 AI 每轮生成后自动更新，并可按需注入提示词让主模型看到。

入口：首页点「**变量**」卡片，工作台左侧导航有五个页面：

**1. 变量注册** —— 先注册变量，注册时填写**变化规则**（AI 自动维护的唯一依据），如「好感度：随着互动增进、冒犯下降」。
> 内置已注册好「友谊 / 友谊等级 / 情欲 / 情欲等级」四个变量，任意角色卡直接可用（带「内置」徽标，可编辑覆盖、可删除恢复默认）。

**2. 变量树** —— 组织变量的实际取值：

- 变量分两层：**默认值（角色卡）**= 作者预设的初始状态，只能手动改；**游戏值（聊天）**= 当前聊天的实际数值，由 AI 自动维护。
- 新建条目：「＋ 新建」→ 新建变量（从已注册变量下拉选择，填初始值）/ 新建节点（纯容器，用于嵌套）。
- 父变量由 AI 按变化规则维护；**子变量**按派生方式自动计算——区间（如 好感度 40 → 态度「颇具好感」）或公式（如 综合评分 = 0.5*服从值+0.5*美貌值），派生源可为父变量或另一子变量（链式）。
- 所有条目可按住行首「⋮⋮」把手拖动排序。

**3. 剧情触发** —— 按变量当前值是否满足预设条件，**确定性**触发剧情事件（不依赖 AI 判断），与「剧情脉络」互补。

**4. 游戏地图** —— 为当前角色卡制作一张游戏地图：

- 「**上传图片**」选一张地图底图（学校平面图 / 城镇布局 / 世界地图……），在裁剪窗口里拖动、缩放裁剪框，**确定裁剪**后即为地图背景。
- **双击**地图任意位置添加地点，**拖动**地点调整位置，点击选中后可直接改名（如「学校」）或删除。
- 全部编辑先记在内存里，点「**保存**」才写入当前角色卡（随角色卡导入 / 导出自动携带）；有未保存修改时保存键会变橙色提醒。
- 群聊 / 未选角色时保存会落到全局设置，只作展示兜底，不随角色卡携带。

**5. 注入预览** —— 只读查看实际注入提示词的 `<Values>` 内容。

**自动维护开关**：工作台里可开关「自动维护」；想手动跑一轮就点「✨ 立即维护」。游戏值跑偏了，点「↺ 重置为默认值」一键恢复角色卡预设。

**注入提示词**：变量树的条目（或整个节点）可勾选，勾选后每轮发送前把当前变量状态注入提示词（World Info after 之后），让主模型直接看到数值。

---

## 七、游戏模式

首页点「**游戏模式**」卡片：进入后上方有**两个切换图标**——

- **游戏地图**（默认）：展示当前角色绑定的地图背景与全部地点，地点名称直接标在地图上，点击可高亮；没有地图时点「去编辑地图」可跳转到变量系统制作。
- **游戏数据**：只读展示当前变量状态（游戏值总览），适合玩家查看自己的人物属性。它展示的就是变量系统注入的那些变量，不需要单独配置。

---

## 八、系统日志

首页点「**系统日志**」卡片：面板内日志视图，支持按级别过滤、暂停 / 跟随滚动、清空、复制、导出。插件出问题排查时先看这里。

---

## 九、主题切换

面板页脚有**主题按钮**，可切换三套皮肤，选择后即时生效并持久化：

- **手绘涂鸦**（默认）：牛皮纸 + 墨棕描边 + 蜡笔多色点缀，全直角方正
- **古风典雅**：宣纸底 + 墨色描边 + 朱砂印章 + 鎏金点缀，楷体字
- **macOS 风格**：玻璃 / 圆角 / 蓝紫强调，亮暗自适应

---

## 深入阅读

- `变量系统指南.md` —— 变量注册规则、两层存储、AI 维护细节
- `剧情脉络自动生成模式.md` —— 剧情脉络的自动生成模式与写法
- `README.md` —— 项目概览、开发与构建
