# 开发日志-归档

> 已归档的历史开发日志（v0.1.x 及更早）。
> 当前开发日志见「开发日志.md」。

---

## 2026-08-10（上午）

### v0.1.0 新增（2026-08-10 上午）— 项目骨架：构建管线 / 悬浮球 / 面板 / API 连接

**背景**
- 从零搭建万华镜（Kaleidoscope）插件骨架，参考 SoulLink 的既有模式：单文件产物 + 模块化源码、双通道菜单、悬浮球 + 面板外壳、API 连接。目标先把「地基」打稳，叙事引擎功能逐步叠加。

**改动**
- 工程结构：`js/` 源码按依赖顺序经 `build.js` 拼接成单文件 `index.js`（TauriTavern manifest 只接受单个 JS 文件）；`build.bat` 一键构建；`manifest.json`（loading_order 50）。
- `js/constants.js`：全部常量——DOM ID / 默认设置 / 超时 / 尺寸 / 全局运行时 key 命名。
- `js/utils.js`：设置读写（默认值合并 + WeakMap 兜底）、`getApiBase` 归一化、`fetchModelList`（跨域宿主代理 + 直连回退）、`chatCompletion`（超时 / 重试 / 并发限制）、`parseAgentJson`。
- `js/host.js`：`getContextSafe`、`onHostEvent`（event_types 解析 + 去重重挂）。
- `js/ui-shell.js`：六棱万花筒悬浮球（拖拽 / 点击开面板 / 长按隐藏 / 位置持久化）、宣纸卷轴面板（拖拽 / ESC / 视图切换）、首页 + API 视图脚手架。
- `js/views-api.js`：连接并拉取模型、输入即存、模型下拉 + 手动输入双通道、并发限制开关（1–10）、思考强度下拉。
- `js/main.js`：bootstrap（幂等 + 兜底重试 60×500ms）、菜单双通道（宿主 API + 手动注入）+ 菜单自愈（MutationObserver）。
- 主题：古风典雅（宣纸 / 墨色 / 朱砂 / 鎏金），样式全部以 `--k-*` CSS 变量组织。
- 版本号升至 0.1.0（js/constants.js `MODULE_VERSION` / manifest.json）。

**后续**
- v0.1.0 起「剧情脉络」与「系统日志」开始规划；v0.2.0 落地（见开发日志.md）。