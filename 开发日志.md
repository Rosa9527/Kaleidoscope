# 开发日志

> 按日期记录万华镜（Kaleidoscope）的开发进度、决策与下一步计划。
> 与「重要开发经验.md」（踩坑与解决方案）和「重要设计.md」（架构与设计决策）配合阅读。
> v0.1.x 及更早的历史日志已归档至「开发日志-归档.md」。

## 目录

- [2026-08-16](#2026-08-16)
  - v1.3.0 修复 — 保存误报「未生效」：宿主重排 JSON 键序，校验改用忽略键序的深比较
  - v1.3.0 新增 — 地图系统：变量工作台第 5 个 tab「游戏地图」编辑器（上传裁剪背景 + 双击建点 / 拖动 / 命名）+ 游戏模式双入口（游戏地图 / 游戏数据）
  - v1.2.2 修复 — 默认值删除不生效：深合并宿主下用哨兵标记删除键
  - v1.2.2 新增 — 剧情脉络事件同样支持事件效果（AI 预筛选中触发时修改父变量）
  - v1.2.1 调整 — 默认数值界面新增「保存」按钮 + 写卡磁盘校验
  - v1.2.1 修复 — 默认值删除不持久化：恢复角色卡绑定 + 删改立即落盘
  - v1.2.0 新增 — 子变量公式派生（多变量四则运算）+ 链式派生（子变量可为另一子变量的派生源）
  - v1.2.1 新增 — 剧情触发事件效果（触发时确定性修改父变量：加减值 / 覆盖，支持文本与 null）
- [2026-08-14](#2026-08-14)
  - v1.1.0 新增 — 悬浮球半透明磨砂玻璃背景（macOS / 古风典雅双主题）+ 标语精简「镜中万象」
  - v1.0.9 新增 — 主题系统：新增「古风典雅」主题
  - v1.0.8 调整 — 前端整体重设计：macOS 风格界面（实色不透明面板 + 左上返回/右上关闭 + 亮/暗双主题）
- [2026-08-13](#2026-08-13)
  - v1.0.1 调整 — 手机端游戏档案封面保持单行（题签 + 刷新 + 返回主页图标不换行）
  - v1.0.0 发布 — 首个正式版（游戏模式宽度与首页一致 + 手机端导航隐藏图标）
  - v0.13.3 调整 — 游戏模式界面重构（玩家档案：朱砂印章节 + 点线目次）
  - v0.13.2 调整 — 注入提示词预览移入变量系统（「注入预览」页）
  - v0.13.1 调整 — 游戏模式界面重构（镜鉴战况：英雄横幅 + HUD 战况条 + 数据面板）
  - v0.13.0 新增 — 游戏模式：玩家数据展示面板（游戏值总览 + 注入内容预览）
  - v0.12.0 新增 — 变量系统：父变量 / 子变量（子变量按父变量值区间自动派生）
  - v0.11.0 新增 — 剧情触发：事件类型（一次性默认，触发后自动关闭 / 常驻可重复触发）
  - v0.10.0 新增 — 剧情触发：按变量当前值确定性触发剧情事件（不依赖 AI 判断）
  - v0.9.0 新增 — 变量系统：默认数值层勾选节点/变量，注入提示词（World Info after 之后）
  - v0.8.1 调整 — 变量系统：默认值仅手动修改，游戏值可一键重置为默认值
- [2026-08-12](#2026-08-12)
  - v0.8.0 新增 — 变量系统：键注册表 + 默认值 / 游戏值双层 + AI 自动维护
  - v0.7.10 调整 — 剧情脉络手机端改为面板内视图（与其它功能一致）
  - v0.7.9 调整 — 剧情脉络移动端改为 540px 视窗（与预设模版一致）
  - v0.7.8 修复 — 移动端面板默认位置与 SoulLink 一致
  - v0.7.7 修复 — 移动端面板标题栏上方大块空白
  - v0.7.6 修复 — 移动端面板浮窗可拖动定位（与 SoulLink 一致）
  - v0.7.5 修复 — 剧情脉络工作台小屏显示问题
- [2026-08-11](#2026-08-11)
  - v0.7.4 修复 — 删楼后重发不触发剧情预筛
  - v0.7.3 调整 — 剧情预筛设置迁移至预设模版
- [2026-08-10](#2026-08-10)
  - v0.7.2 新增 — 剧情脉络导入方式（合并/覆盖）与节点启停开关
  - v0.7.1 调整 — 移动端显示优化（SillyTavern 手机端）
  - v0.7.0 新增 — 跨扩展发送屏障（与 SoulLink 并发协调）
  - v0.6.0 新增 — 注入实录详情视图
  - v0.5.0 新增 — 剧情脉络 · 角色卡整包导出
  - v0.4.0 新增 — 剧情脉络 · 角色卡绑定
  - v0.3.0 新增 — 剧情预筛（Story Gate）
  - v0.2.0 新增 — 剧情脉络可视化工作台
  - v0.2.0 新增 — 系统日志
  - v0.2.0 新增 — 自动化测试
  - v0.2.0 调整 — 文档体系与结构速查
- [下一步计划](#下一步计划)

## 2026-08-10

### v0.7.2 新增（2026-08-10）— 剧情脉络导入方式（合并/覆盖）与节点启停开关

**背景**
- 整包导入此前只有一种语义（同 id 更新、其余追加），换角色 / 整体换血时旧数据
  会残留；需要显式的「合并 / 覆盖」二选一。
- 剧情脉络是「整棵目录树」一起参与预筛的，作者需要一个不删除数据、随时停用某条
  剧情线（含其子节点与事件）的开关。

**改动**
- `js/story-data.js`：
  - 节点新增 `enabled` 字段（新建默认启用）；新增 `toggleStoryNodeEnabled` /
    `isStoryNodeActive` / `getStoryActiveScripts`——关闭节点 = 停用整棵子树，
    其事件不再参与预筛；未分类事件不受影响。
  - `mergeStoryBundleInto(ctx, bundle, { replace })`：默认合并（同 id 更新、
    其余追加），`replace` 为覆盖（清空后整体替换）。
  - 整包导出每个节点都写 `enabled: true / false`，往返保留启停状态；
    合并导入时包中显式 `enabled` 生效、缺失时默认启用（导入文件对启停状态有最终决定权）。
- `js/story-gate.js`：事件目录与预筛管线只取「激活」事件（`buildStoryEventCatalog`
  排除停用节点及其子树；`runStoryGatePipeline` 的候选名单同步收窄）。
- `js/views-story.js` / `js/constants.js` / `style.css`：
  - 整包导入解析成功后弹出**导入方式选择浮层**：🔀 合并导入（默认）/ 📦 覆盖导入，
    点遮罩取消；单事件 frontmatter 导入不经过此选择。
  - 工作台节点行新增**启用滑块**：点击关闭（整行淡化、滑块变灰，子节点与事件停用），
    再点激活；行内 title 说明停用影响。
- 文档：`剧情脉络书写指南 1.0.0.md` 补 `enabled` 字段与导入方式说明；
  `插件结构.md` 更新整包格式字段。
- 版本升至 v0.7.2（manifest + constants）。

**测试**
- `tests/data.test.js`：启停开关 / 停用子树排除 / enabled 导出往返 / 合并保留停用 /
  覆盖导入清空替换。
- `tests/gate.test.js`：停用节点目录排除子树与事件、未分类保留；停用整棵子树全排除。
- `tests/ui.test.js`：节点行滑块交互（关闭 → 激活）；整包导入弹出方式选择并支持
  「合并」与「覆盖」两条路径。

### v0.7.1 调整（2026-08-10）— 移动端显示优化（SillyTavern 手机端）

**背景**
- 手机端（本质是手机浏览器，窄视口 + 触屏 + 动态地址栏）此前只有剧情脉络工作台做了
  全屏适配，面板仍是「372px 浮动卡片 + 24px 边距 + 100vh 固定高度」，窄屏下观感与
  可达性都不好：底部内容可能被浏览器栏裁掉、小图标难点、iOS 聚焦输入框会自动放大页面。

**改动**
- `style.css`：
  - 面板 ≤640px 全屏（`100dvh` + `100vh` 回退，盖过宽视图类与历史拖拽位置）；
    工作台高度补 `100vh` 回退；面板 / 工作台补安全区 padding（`env(safe-area-inset-*)`）。
  - 表单控件（`.kaleido-input`）移动端字号提到 16px，避免 iOS 聚焦自动放大破坏布局。
  - 触控目标放大：行内图标按钮 24→32px、面板返回/关闭 26→34px、折叠箭头 22→30px、
    首页日志按钮 30→36px、通用图标按钮 38px、按钮统一 `min-height: 40px`、树行 42px。
  - 树缩进从内联 paddingLeft 改为 `--depth` CSS 变量，移动端收敛为 `8/22px + depth×14px`，
    深层嵌套不再把行名挤没。
  - 触屏（`@media (hover: none)`）复位粘滞 hover 视觉；`touch-action: manipulation` +
    `-webkit-tap-highlight-color: transparent` 优化点按；`overscroll-behavior: contain`
    隔离滚动；API 头部 / 日志工具行窄屏换行，日志列表、预设编辑区改用视口比例高度。
- `js/ui-shell.js`：新增 `isMobileViewport()`；移动端面板不做浮窗定位（不写内联
  left/top，位置交给 CSS 全屏）也不启动标题栏拖拽。
- `js/views-story.js`：树行缩进改 `--depth` 自定义属性；行内图标按钮补 `aria-label`
  （手机端无 title 悬浮提示）。
- 版本升至 v0.7.1（`constants.js` / `manifest.json`），同步刷新 `tests/_mobile_preview*.html`。

**验证**
- 全量测试通过：data 37 / ui 38 / proxy 9 / gate 13 / barrier 10（107 项）。
- 手机浏览器（≤640px）应看到：面板与工作台全屏铺满、无浏览器栏裁切、表单聚焦不缩放、
  图标按钮易点按、触屏点击无残留 hover 高亮。

### v0.7.0 新增（2026-08-10）— 跨扩展发送屏障（与 SoulLink 并发协调）

**背景**
- 万华镜与 SoulLink 都通过 messageSent 阻塞监听器做发送前预筛注入。宿主
  （TauriTavern）的 eventSource.emit 会**按注册顺序逐个 await 监听器**，两个
  扩展各跑各的时，发送前耗时 = 剧情预筛 Gate + 角色推演 Gate（串行，最坏近 90s）。
- 正确性其实一直有保证（宿主等全部监听器 resolve 才发主请求），问题在串行耗时翻倍。

**设计（与 SoulLink 共用的发送屏障协议 v1）**
- 新增 `js/send-barrier.js`（两边逐字节一致）：`globalThis.__preSendInjectionBarrier__`，
  提供 `register(name, task)` 与 `waitAll(ctx, payload, timeoutMs)`。
- 两个扩展的预筛管线改为注册进屏障的发送前任务（所有守卫：开关 / 能力 / 载荷校验 /
  主生成配对 / 运行锁都留在任务内），messageSent 监听器只做「取屏障 → waitAll」，
  屏障不可用时回退为「自己直接阻塞」的原有行为。
- 轮次签名取末条消息 ID（缺失回退文本）：宿主串行 emit 下后一个监听器必然晚于本轮
  完成才被调用，同签名直接复用本轮结果，**绝不重跑**（否则每次发送 Gate 跑两遍、
  重复注入）；新发送产生新消息 → 新签名 → 开新轮。
- 并发执行 + `Promise.allSettled` 失败隔离 + 整轮硬截止兜底：发送前耗时从
  「各 Gate 之和」降为「各 Gate 最大值」，任何一方失败/超时都不阻塞发送。

**改动**
- `js/send-barrier.js`（新增）：屏障协议（register / waitAll / 签名 / 硬截止）。
- `js/story-gate.js`：`runStoryGateBarrierTask`（原监听器全部守卫迁入）+ 薄监听器
  `onStoryGateMessageSent` + 模块加载时注册 `kaleidoscope-story-gate` 任务。
- `js/constants.js`：`MODULE_VERSION` → 0.7.0。
- `tests/barrier.test.js`（新增）：幂等 / 签名 / 并发启动 / 同签名去重 / 级联去重 /
  失败隔离 / 载荷透传 / 与剧情预筛打通 / 回退路径 / 硬截止。
- `tests/gate.test.js`：messageSent 用例改为「每次发送产生新消息（新 id → 新签名）」
  与宿主行为一致。

**验证**
- 全量测试通过：data 37 / ui 38 / proxy 9 / gate 13 / barrier 10。
- 运行期确认：两边日志视图的 fetch 时间戳应显示两个 Gate 请求重叠发出，
  messageSent emit 总时长 ≈ max(两个 Gate) 而非两段相加。
### v0.6.0 新增（2026-08-10）— 注入实录详情视图

**背景**
- 剧情预筛此前只有首页卡片摘要（已注入 N 事件 / 本轮无事件 / 超时放行），
  看不到「触发了什么、注入了什么」；参考 SoulLink「角色扮演」视图补全详情。

**设计（参考 SoulLink「角色扮演」视图）**
- 最近一轮快照除入选 ID 外，新增 `selectedEvents`（入选事件详情：
  id / name / trigger / description / content）与 `injectionText`（注入提示词原文）；
  Gate 原文改为完整保留（此前截断 400 字符）。
- 面板新增「注入实录」视图：摘要（结果 + 耗时 / 候选 / 入选 + 触发名单）→
  预筛原文（Gate 返回）→ 本轮触发的事件（名称 / ID / 触发条件 / 说明 / 正文）→
  注入提示词原文（一键复制）四段展示；无记录时显示空态。
- 首页「注入实录」卡片点击进入详情视图（此前仅 toast 摘要）。

**改动**
- `js/constants.js`：`INJECT_VIEW_ID` 与视图 DOM ID 常量，标题 / 宽视图注册。
- `js/story-gate.js`：快照记录 `selectedEvents` / `injectionText`，Gate 原文完整保留。
- `js/views-inject.js`（新增）：`renderInjectView` / `buildInjectSummary` /
  `buildInjectEventCard` / `copyInjectInjectionText`。
- `js/ui-shell.js`：面板新增注入实录视图 HTML；首页卡片点击进入视图；复制按钮接线。
- `style.css`：注入实录视图样式（摘要 / 事件卡片 / 原文块 / 宽视图模式）。
- `build.js` / `tests/harness.js`：新增 `js/views-inject.js`。
- `tests/gate.test.js`：断言快照含入选事件详情与注入原文。
- `tests/ui.test.js`：注入实录视图渲染与空态用例。

### v0.5.0 新增（2026-08-10）— 剧情脉络 · 角色卡整包导出

**背景**
- 剧情脉络绑定角色卡后，分享时只能整卡携带；需要把「剧情脉络」单独导出成文件，
  文件名直接体现归属角色卡，导入时自动识别。

**设计**
- 导出文件名：绑定角色卡 → `剧情脉络: 角色卡名.yaml`（如 `剧情脉络: 辉夜大小姐.yaml`）；
  群聊 / 未选角色回退 `万华镜-剧情脉络-时间戳.yaml`。
- 整包格式自描述：新增 `format: kaleidoscope-story` 标记行 + `character: 角色卡名`
  （仅绑定角色卡时写入）；解析凭 format 标记或 nodes / scripts 段识别，兼容旧版导出文件。
- 导入识别：确认框显示来源角色卡（「来自『辉夜大小姐』」），识别不依赖文件名。

**改动**
- `js/constants.js`：`STORY_BUNDLE_FORMAT` / `STORY_CARD_BUNDLE_FILENAME_PREFIX` 常量，版本升至 v0.5.0。
- `js/story-data.js`：`serializeStoryBundle` 输出 format / character 头部；新增
  `buildStoryBundleFilename`；`parseStoryBundleFile` 凭标记识别并返回 character。
- `js/views-story.js`：整包导出改用角色卡名文件名；导入确认提示来源角色卡。
- `tests/data.test.js`：新增格式标记 / 角色卡名往返 / 文件名规则用例。
- `tests/ui.test.js`：新增导出文件名、导入识别提示用例。

### v0.4.0 新增（2026-08-10）— 剧情脉络 · 角色卡绑定

**背景**
- 剧情脉络是「角色专属」内容，此前却存在全局设置里：换角色、换电脑、分享角色卡都会丢。
  参考 SillyTavern 局部正则脚本的做法，把剧情数据存进角色卡本身——导入角色卡自动带入，
  导出角色卡自动携带，无需任何额外操作。

**设计（参考 SillyTavern 局部正则脚本）**
- 数据存 `character.data.extensions.kaleidoscope_story`（`{ version, nodes, scripts }`），
  角色卡序列化天然携带该字段，导入 / 导出 / 复制 / 替换角色卡全部自动生效。
- 读写路径：`getStoryNodes` / `getStoryScripts` 优先读当前角色卡；群聊 / 未选角色 /
  卡上无数据时回退全局设置（兼容旧数据）。`saveStoryData` 有角色且宿主提供
  `ctx.writeExtensionField` 时写角色卡，否则写全局设置。
- 首次保存即迁移：角色卡尚无数据时，把当前数据（可能来自旧版全局设置）整体迁入
  角色卡并清空全局兜底，toastr 提示一次。
- 防抖持久化：变更先同步落在内存角色卡对象，再经 500ms 防抖调用
  `writeExtensionField` 写回宿主；定时器挂 `globalThis`，热重载不丢。
- 按 avatar 定位写回：防抖期间切换角色不写错卡；角色已删除 / 写入失败 / 宿主不支持时
  数据回退全局设置，避免丢失。

**改动**
- `js/story-data.js`：新增角色卡绑定层（`getStoryCharacter` / `getStoryCardData` /
  `setStoryCardData` / `scheduleStoryCardSave` / `persistStoryCardData` /
  `fallbackStoryDataToSettings`），`getStoryNodes` / `getStoryScripts` / `saveStoryData`
  改为「角色卡优先、全局兜底」。
- `js/views-story.js`：工作台头部新增绑定徽标 `refreshStoryBindingStatus`
  （已绑定 / 待绑定 / 未绑定角色），随 `renderStoryTree` 刷新。
- `js/views-home.js`：首页剧情脉络卡片状态悬停提示绑定信息，空数据时显示「已绑定 · 尚未添加」。
- `js/constants.js`：`STORY_CARD_*` 常量（扩展键 / 版本 / 防抖时长 / 定时器 key）、`STORY_BINDING_ID`。
- `style.css`：绑定徽标三态样式。
- `tests/harness.js`：`makeContext` 支持 characters / characterId / writeExtensionField，
  新增 `makeCharacter`。
- `tests/data.test.js`：新增 8 个角色卡绑定用例（读取优先 / 迁移清空 / 防抖持久化 /
  切换角色不写错卡 / 角色删除回退 / 写入失败回退 / 宿主不支持降级）。
- `tests/ui.test.js`：新增绑定徽标三态用例。
- 版本升至 v0.4.0（manifest + constants）。

**修正（同日）— 剧情脉络只显示当前角色卡绑定的内容**
- 此前有角色但卡上无数据时会回退显示全局设置，导致「切换到没有绑定文件的角色卡
  却看到别的数据」。现改为：有角色时只读角色卡（无数据即空），只有群聊 / 未选
  角色 / 宿主不支持写角色卡时才回退全局设置。
- 各变更函数（create/update/delete 节点与事件、整包导入）开头先
  `ensureStoryCardData`，保证变更落在角色卡数组上；首次变更仍自动把旧版全局数据
  迁入当前角色卡并清空全局。
- 测试：新增「有角色无卡显示为空」「切换到无绑定角色卡显示为空」用例，
  `data.test.js` 33 个用例全绿。

**后续**
- 群聊场景的剧情数据策略（当前回退全局设置）。
- 角色卡数据过大时的体积提示（剧情正文可能撑大卡片）。

### v0.2.0 新增（2026-08-10）— 剧情脉络可视化工作台

**背景**
- 万华镜的定位是「动态叙事引擎」：作者把剧情、世界观、事件组织成多层级结构，运行时由 AI 按对话状态选择模块注入上下文。第一步先把「内容组织」做成可视化工作台，让作者在酒馆里直接维护节点与事件，再谈运行时注入。

**改动**
- 新增 `js/story-data.js`：数据模型（`storyNodes` / `storyScripts`，存 `ctx.extensionSettings.Kaleidoscope`）、树约束（防环、无效父级回落根节点）、事件 ID 自动递增（001 起、重复自动顺延）、删除语义（子节点上移、事件转未分类）、轻量 YAML 子集解析器、整包 / 单事件导入导出与合并（同 id 更新、悬空引用回落）。
- 新增 `js/views-story.js`：全屏遮罩 + 大窗口树状工作台（移动端自动全屏）；节点可层层嵌套（「第一卷」→「第一章」→ …），事件挂在任意节点下、未分类事件单独成组；「＋」菜单统一新建节点 / 事件；右侧浮层编辑器（节点 / 事件双表单，事件正文必填）；单事件 frontmatter 导入后进编辑器确认；保存以当前可见表单为准 + 会话号防抢占。
- 首页新增「剧情脉络」卡片与状态（N 节点 · M 事件），点击打开工作台。
- `js/constants.js` 新增 `STORY_*` 常量与 `DEFAULT_SETTINGS.storyNodes / storyScripts`。

**后续**
- 事件触发条件（trigger）目前只是文本字段，运行时条件求值待叙事引擎阶段实现。

### v0.2.0 新增（2026-08-10）— 系统日志

**背景**
- 需要一套「看不见但查得到」的运行记录：console、页面错误、Promise 拒绝、宿主事件与 fetch 网络请求统一进内存缓冲，面板内可视化排查；参考 SoulLink 的日志系统实现。

**改动**
- 新增 `js/views-log.js`：`initLogCapture`（console 包装 + window error + unhandledrejection）、`initHostEventLogging`（14 个宿主事件）、`initNetworkCapture`（fetch 旁路观察，响应体 clone 读取）。
- 统一入口 `pushLogEntry`：级别 / 来源 / 消息 / 详情、统一脱敏、噪音过滤、连续重复折叠；缓冲上限默认 2000（可调 100–20000），渲染上限 1000。
- 日志视图 UI：级别 chips / 搜索（120ms 防抖）/ 来源 / 条数 / 暂停（快照语义）/ 跟随 / 清空 / 复制 / 导出 JSON / 噪音开关；日志视图加宽（`is-log-mode`）。
- 热重载安全：缓冲、暂停、捕获管道全部挂在 `globalThis.__kaleido_*__`，扩展热重载不丢日志、不重复包装 console / fetch。
- 对外暴露 `globalThis.KaleidoscopeLog`（debug / info / warn / error / log / clear / getEntries / setMaxEntries）；首页「系统日志」卡片显示错误 / 警告数。
- 新增「📦 完整请求体」导出：对话请求的完整请求体 / 响应体（未截断、已脱敏）保留最近 5 次，一键导出 JSON 排查原始内容（对齐 SoulLink 的完整请求体捕获）。

### v0.2.0 新增（2026-08-10）— 自动化测试

- `tests/harness.js`：共享加载器——按构建顺序把 `js/` 源码读进 vm / jsdom 沙箱（`main.js` 含启动副作用，单独排除），简易断言运行器 + 宿主 mock。
- `tests/data.test.js`：数据层——层级创建 / 防环 / 无效父级回落 / 默认名 / 删除上移与转未分类 / ID 顺延 / YAML 往返 / 合并导入（同 id 更新、悬空引用转未分类、导入成环回落）。
- `tests/ui.test.js`：工作台 UI——打开 / 新建节点与事件 / 编辑 / 导入导出 / 删除确认，jsdom 环境（mock `URL.createObjectURL` / `confirm` / `toastr`）。
- 运行方式：`node tests/data.test.js`、`node tests/ui.test.js`（jsdom 装在 `tests/node_modules`）。

### v0.2.0 调整（2026-08-10）— 文档体系与结构速查

- 补齐与 SoulLink 对齐的文档体系：`开发日志.md`（本文件）、`开发日志-归档.md`、`重要开发经验.md`、`重要设计.md`。
- `插件结构.md` 更新为 v0.2.0 结构速查；README 更新目录结构、宿主差异说明与 Roadmap。

### v0.3.0 新增（2026-08-10）— 剧情预筛（Story Gate）

**背景**
- 万华镜的定位是「动态叙事引擎」：作者把剧情组织成节点与事件，运行时由 AI 按对话状态选择事件注入上下文。v0.2.0 完成了内容组织（剧情脉络工作台），本版落地运行时选择与注入的第一环——剧情预筛。

**设计（参考 SoulLink 角色预筛 Gate）**
- 触发时机：用户点击发送（宿主 `messageSent` 事件，监听器返回 Promise 阻塞主模型请求），预筛完成并注入后才恢复发送。
- Gate 输入刻意保持最小：剧情预筛提示词 + 事件目录 + 最近 4 条消息，不携带事件正文、世界书等任何其他上下文。
- 事件目录 `buildStoryEventCatalog`：节点（层级 + 说明）+ 事件（只含名字 / ID / 触发条件 / 描述，不含正文），未分类事件单独成组；JSON 紧凑序列化省 token。
- 请求体四段式：system 提示词 → `<Story_Events>` 目录块 → `<Recent_Messages>` 剧情块 → 输出契约（`{ "events": [] }`）。
- 解析：`parseStoryGateEventIds` 与现有事件求交集，未知 ID 一律丢弃，最多保留 5 个。
- 注入：`<Story_Event>` 块（强制指令 + 事件 id / 名称 / 触发条件 / 说明 + 正文），经 `setExtensionPrompt(IN_CHAT, depth 0, SYSTEM)` 注入到最后一条用户消息正下方；`generationEnded` / `generationStopped` 后清空。
- 稳定性：运行锁 + 签名去重（同一末条只处理一次）、载荷校验（其他插件 emit 的 messageSent 不误触发）、失败即降级放行、`STORY_GATE_TIMEOUT_MS`（45s）硬截止、宿主不支持 `setExtensionPrompt` 时静默跳过。

**改动**
- 新增 `js/story-gate.js`：目录构建 / Gate 消息 / 解析 / 注入文本 / 管线 / messageSent 阻塞监听 / 生成结束清理。
- `js/constants.js`：`STORY_GATE_*` 常量、`DEFAULT_SETTINGS.storyGateEnabled / storyGatePrompt`、`DEFAULT_STORY_GATE_PROMPT`（剧情预筛默认提示词）。
- `js/main.js`：`installHostEventSubscriptions` 挂载 messageSent 阻塞监听与生成结束清理。
- `js/ui-shell.js`：API 连接页新增「剧情预筛」设置区（启用开关 + 提示词编辑 + 恢复默认）；首页「注入实录」卡片启用。
- `js/views-api.js`：`renderGateControl` / `toggleStoryGate` / `resetStoryGatePrompt`。
- `js/views-home.js`：`refreshHomeInjectStatus`（最近一轮结果：已注入 N 事件 / 本轮无事件 / 超时放行 / 未启用）。
- `tests/gate.test.js`：10 个用例（目录 / 消息 / 解析 / 注入 / 管线 / 监听器）。
- 版本升至 v0.3.0（manifest + constants）。

**后续**
- 事件触发条件（trigger）结构化与运行时求值。
- 注入实录详情视图（当前仅首页卡片摘要）。
- 多轮事件去重 / 冷却（同一事件避免连续触发）。

## 2026-08-11

### v0.7.3 调整（2026-08-11）— 剧情预筛设置迁移至预设模版

**背景**
- API 连接页的「剧情预筛」设置区（启用开关 + 提示词编辑）与「预设模版」的
  提示词编辑功能重复，设置入口分散；统一收口到「预设模版」，API 连接页只保留
  连接 / 模型 / 并发 / 思考强度等连接相关配置。

**改动**
- js/ui-shell.js：API 连接页移除「剧情预筛」设置区；预设模版视图顶部新增
  剧情预筛区（状态徽标 + 启用开关），提示词编辑沿用既有标签页。
- js/views-api.js：移除 
enderGateControl / 	oggleStoryGate /
  
esetStoryGatePrompt 及对应事件绑定。
- js/views-preset.js：迁入 
enderPresetGateControl / 	oggleStoryGate，
  初始化时渲染开关状态。
- js/constants.js：GATE_* 常量替换为 PRESET_GATE_TOGGLE_ID /
  PRESET_GATE_STATUS_ID；版本升至 v0.7.3。
- style.css：移除 .kaleido-api__divider / .kaleido-gate__prompt，
  新增 .kaleido-preset__gate* 样式。
- 文档同步：README / 重要设计 / 插件结构。

### v0.7.4 修复（2026-08-11）— 删楼后重发不触发剧情预筛

**背景**
- 删掉最新两层楼（user + assistant）后再次发送，首条消息不触发剧情预筛、直接生成，
  再发第二条才恢复正常。宿主（TauriTavern）删除消息会复用被删消息的 ID（楼层序号式），
  删楼后重发的新消息拿到与旧轮相同的 ID，发送屏障把它误判为「同一发送已处理」
  直接返回旧轮结果，剧情预筛任务根本不会执行。

**改动**
- js/send-barrier.js：轮次复用判据从「签名（末条消息 ID）」升级为「签名 + 末条消息
  内容」——同 ID 不同内容必是新发送，开启新轮；新增 `clearSendBarrierRound()`
  清空旧轮。
- js/main.js：订阅 `messageDeleted`，删消息后清空发送屏障旧轮（同 ID 同内容重发
  也兜得住），看门狗重挂事件源时自动带上。
- 版本升至 v0.7.4（manifest + constants）。

**测试**
- tests/barrier.test.js：同 ID 不同内容开启新轮（删楼重发）；`clearSendBarrierRound`
  清轮后同签名重新执行。

## 2026-08-12

### v0.8.0 新增（2026-08-12）— 变量系统：键注册表 + 默认值 / 游戏值双层 + AI 自动维护

**背景**
- 玩家需要一套「自定义键值」系统：可以新建人名（如张三）并在其下挂键（如好感度=30），
  也可以直接建键（如金钱=1000）；键要在界面中统一注册并填写变化规则，新建键时从
  下拉选择已注册的键。
- 变量分两层：**默认值**与角色卡绑定（同剧情脉络模式，随角色卡导入/导出），
  **游戏值**与聊天文件绑定（存 `chatMetadata.kaleidoscope_values`，随 jsonl 首行
  chat_metadata 保存/加载）。
- 每轮生成结束后自动调用一次 API，由 AI 依据「系统提示词 + 各键变化规则 + 最新两条消息」
  维护游戏值。

**改动**
- `js/values-data.js`（新增）：数据模型与双层存储。
  - 键注册表：`upsertValuesKey` / `deleteValuesKey` / `getValuesKeys`（以键名为身份）。
  - 默认值：角色卡 `data.extensions.kaleidoscope_values`（`{ version, keys, defaults }`），
    读写路径与剧情脉络一致（`writeExtensionField` 防抖持久化，群聊/未选角色回退全局
    设置 `valuesData`，首次变更自动迁入角色卡）。
  - 游戏值：`chatMetadata.kaleidoscope_values`（`{ version, values, updatedAt, lastSignature }`），
    `saveValuesChatState` 防抖调用 `ctx.saveChat()`；未初始化时回退默认值克隆。
  - 变量树工具：路径读写删除 / 叶子计数 / 容器判定。
  - AI 补丁合并 `mergeValuesPatch`：null 删除、容器递归、已注册键才允许新增、未注册
    新键忽略并记录；`parseValuesPatch` 支持 JSON 与 YAML 两种返回形态。
  - YAML 整包导入导出：`format: kaleidoscope-values` 自描述标记 + keys + defaults；
    变量标量保持原生类型（数字不引号化，形如数字的字符串加引号）。
- `js/values-maintain.js`（新增）：AI 自动维护管线。
  - 触发时机与 SoulLink 自动档案维护同款稳定设计：generationStarted → generationEnded
    配对、末条签名去重、运行锁、中断检查、失败即跳过本轮。
  - 请求体四段式：system 维护提示词 → `<Key_Rules>`（键 + 变化规则）→
    `<Current_Values>`（当前游戏值 YAML）→ `<Recent_Messages>`（最新 2 条消息）。
  - 有变化才写聊天文件；最近一轮结果存 `globalThis.__kaleido_values_last_round__` 供 UI 展示。
  - `runValuesMaintainNow()`：工作台「立即维护」手动触发（跳过配对约束）。
- `js/views-values.js`（新增）：变量工作台 UI（电脑端大窗口 + 手机端面板内视图）。
  - 两个标签页：**变量**（节点树 + 默认值/游戏值层切换 + 自动维护状态行 + 立即维护）
    与**键注册**（键列表 + 名称/变化规则编辑）。
  - 「＋ 新建」菜单分「新建节点 / 新建键」：节点只填名称与上级节点（不对应值，可嵌套）；
    键从已注册键下拉选择并填值（编辑时键名只读）；支持新建 / 编辑 / 移动 / 删除（节点删除带确认）。
  - 整包 YAML 导入 / 导出（`变量: 角色卡名.yaml`）。
- `js/constants.js`：变量系统全部 DOM ID / 存储 key / 默认设置（`valuesData`、
  `valuesAutoUpdateEnabled`、`valuesMaintainPrompt`）/ 默认维护提示词 / 预设模版登记。
- `js/ui-shell.js`：首页新增「变量」卡片（按视口分流工作台 / 面板视图），
  预设模版新增「变量自动维护」开关。
- `js/views-home.js`：首页变量卡片状态（键数 · 值数 + 绑定提示）。
- `js/views-preset.js`：变量自动维护开关 + 「变量维护」提示词标签页（可编辑 / 恢复默认）。
- `js/main.js`：挂载 generationStarted / generationEnded / generationStopped / chatChanged
  事件（变量维护配对跟踪）。
- `style.css`：变量工作台样式（标签页 / 层切换 / 树行 / 编辑器 / 维护状态行，古风主题一致）。
- 版本升至 v0.8.0（manifest + constants）。

**测试**
- 新增 `tests/values.test.js`（12 项）：键注册去重、角色卡写入、全局设置回退、
  chatMetadata 写入与 saveChat 触发、游戏值回退默认值、补丁合并（更新/删除/新增注册键/
  忽略未注册键/无变化）、AI 返回 JSON/YAML 解析、整包 YAML 往返、错误格式抛错、
  维护消息四段式、树工具。
- 新增 `tests/values-ui.test.js`（8 项，jsdom）：工作台创建、键注册表单、新建菜单、
  节点 + 子键、编辑键只读键名、游戏值层写入 chatMetadata、删除确认、空树新建节点。
- 既有测试全部通过（barrier 12 / build / data 42 / gate 15 / mobile-layout / proxy 14 /
  ui 43 / ui-mobile 7）。
### v0.7.10 调整（2026-08-12）— 剧情脉络手机端改为面板内视图（与其它功能一致）

**背景**
- 五个功能中只有剧情脉络在手机端视窗显示异常：v0.7.9 的「遮罩 + 540px 居中窗口」
  在 SillyTavern 手机端仍显示失败。用户要求与其它四个功能（API 连接 / 系统日志 /
  预设模版 / 注入实录）完全一致——由主页卡片切换进入的面板内视图。

**改动**
- `js/views-story.js`：
  - 新增 `isNarrowViewport()`（`matchMedia(max-width: 640px)`），`initStorySection(panel)`
    按视口分流：手机端创建面板内视图（`kaleido-story-view`），电脑端创建独立大窗口
    工作台（`kaleido-story-dialog`，全屏遮罩 + 920×680，保持不变）。
  - 提取 `buildStoryContentHTML(editorClass)` / `bindStoryContentEvents()`：树区、编辑器、
    导入方式、新建菜单、文件输入与事件绑定由对话框和面板视图共用，仅编辑器浮层类名不同。
  - 面板视图内工具栏：绑定徽标 + 导入 / 导出按钮；无关闭按钮与遮罩（由面板返回键 / ESC 回首页）。
- `js/constants.js`：新增 `STORY_VIEW_ID`，登记视图标题「剧情脉络」与宽视图模式 `is-story-mode`。
- `js/ui-shell.js`：首页剧情卡片按视口分流（手机端 `showPanelView(STORY_VIEW_ID)`，
  电脑端 `openStoryWorkbench()`）；`showPanelView` 挂剧情脉络渲染。
- `style.css`：新增 `.kaleido-story-view` 布局、`.kaleido-story__toolbar`、视图内编辑器
  `.kaleido-story__editor`、`.kaleido-panel__dialog.is-story-mode`（540px，与预设模版一致）。
- 同步刷新 `tests/_mobile_preview*.html` 与 `tests/_layout_repro.html` 内嵌 CSS。
- 版本升至 v0.7.10（manifest + constants）。

**测试**
- 新增 `tests/ui-mobile.test.js`（模拟 ≤640px 视口）：面板内视图创建、首页卡片切换、
  空状态、新建节点、ESC / 返回键回首页、绑定徽标，7 项全过。
- `tests/mobile-layout.test.js`：追加断言视图布局、`is-story-mode` 540px、入口分流。
- 电脑端对话框路径（`tests/ui.test.js` 43 项）不受影响，全部通过。
### v0.7.9 调整（2026-08-12）— 剧情脉络移动端改为 540px 视窗（与预设模版一致）

**背景**
- 剧情脉络工作台在手机端（≤640px）是全屏铺满（`100vw × 100dvh`），
  与其它功能（预设模版等 540px 面板浮窗）观感不一致；用户要求手机端
  与「预设模版」窗口大小一致，电脑端保持大窗口工作台。

**改动**
- `style.css`（移动端 ≤640px 媒体查询）：
  - `.kaleido-story-dialog__inner` 不再全屏：改为 `width: 540px` +
    `max-width: calc(100vw - 24px)` + `max-height: calc(100dvh - 24px)`，
    保留圆角与边框，与预设模版面板一致；高度随内容自适应。
  - 树区补 `min-height: 40vh`（与预设模版文本区一致），内容多时窗口
    最高到视口 - 24px，树区内部滚动。
  - 删除工作台标题栏 / body / 编辑器的安全区 inset（窗口不再贴边，
    无需刘海与底部横条补偿）。
- 同步刷新 `tests/_mobile_preview*.html` 与 `tests/_layout_repro.html` 内嵌 CSS。
- 版本升至 v0.7.9（manifest + constants）。

**测试**
- `tests/mobile-layout.test.js`：改为断言移动端工作台为 540px 视窗
  （不再 `width:100vw` / `height:100dvh` 全屏），且不再叠加安全区 inset。
### v0.7.8 修复（2026-08-12）— 移动端面板默认位置改为与 SoulLink 一致

**背景**
- 手机端（SillyTavern 手机浏览器）打开面板时，首页浮窗按 CSS 居中布局
  （`top: 50% + translate(-50%,-50%)`）。当浮窗内容高于视口（或 WebView
  不支持 `dvh` 导致高度上限失效）时，对话框顶部会超出可见视图上方，
  「镜中万象」标语以上的部分（含「万华镜」标题栏）全部跑到屏幕外，
  标题栏无法长按拖动。

**原因**
- v0.7.6 起移动端默认位置交给 CSS 尺寸模式（首页/API 居中、日志/预设/注入
  铺边），`ensurePanelPosition` 在移动端且未拖动时直接返回，不写内联
  left/top；居中布局没有「顶部 clamp」，内容过高时标题栏就溢出视口。

**改动**
- `js/ui-shell.js`：
  - 删除 `isMobileViewport()` 与 `ensurePanelPosition` 的移动端提前返回：
    面板打开 / 视图切换 / resize 时始终写入内联 left/top，并 clamp 在视口内
    （与同目录 SoulLink 完全一致），标题栏永远可见可拖。
  - `setPanelPosition` 不再添加 `is-user-positioned` 类（CSS 规则已删除）。
  - `showPanelView` 不再应用 `PANEL_MOBILE_MODES`。
- `js/constants.js`：删除 `PANEL_MOBILE_MODES`（移动端尺寸模式整体移除）。
- `style.css`（移动端 ≤640px 媒体查询）：删除面板按视图分级铺边/居中的
  布局规则与 `is-user-positioned` 规则；面板统一为内联定位浮窗，仅保留
  安全区底部内边距与 overscroll 隔离。剧情脉络工作台全屏规则不变。
- 同步刷新 `tests/_mobile_preview*.html` 内嵌 CSS 与演示脚本。
- 版本升至 v0.7.8（manifest + constants）。

**测试**
- `tests/mobile-layout.test.js`：改为断言不再存在移动端尺寸模式、
  `ensurePanelPosition` 始终写内联 left/top、标题栏无安全区叠加、
  工作台仍全屏。
- `tests/ui.test.js`：新增「面板打开时始终以内联 left/top 定位」回归。

### v0.7.7 修复（2026-08-12）— 移动端面板标题栏上方大块空白

**背景**
- 手机端打开面板（首页等）时，「万华镜」标题与面板顶边之间出现大块空白，
  标题不再紧贴边框；同目录 SoulLink 无此问题。

**原因**
- 移动端媒体查询给 `.kaleido-panel__header` 叠加了
  `padding-top: calc(9px + env(safe-area-inset-top))`。该规则源自 v0.7.1 面板
  全屏铺满时期（顶边贴住刘海/状态栏，需要安全区内边距）；现在面板已恢复为
  浮窗，对话框 top 本身已按 `env(safe-area-inset-top)` 偏移，标题栏再叠加
  一次安全区就形成了标题上方的空白带。

**改动**
- `style.css`（移动端 ≤640px 媒体查询）：面板标题栏固定基础 `padding-top: 9px`，
  不再叠加 `env(safe-area-inset-top)`；剧情脉络工作台标题栏的全屏安全区
  内边距保持不变（工作台仍为全屏）。
- `tests/mobile-layout.test.js`：回归断言面板标题栏不含安全区 inset、
  工作台标题栏仍保留。
- 同步刷新 `tests/_mobile_preview*.html` 内嵌 CSS。
- 版本升至 v0.7.7（manifest + constants）。

**验证**
- 全量测试通过。

### v0.7.5 修复（2026-08-12）— 剧情脉络工作台小屏显示问题

**背景**
- 手机端（SillyTavern 手机浏览器，视口 ≤390px 的较小屏幕）打开剧情脉络工作台时，
  树状列表出现显示问题：深层嵌套节点 / 事件行的名称被挤成 0 宽（完全不可见），
  行右侧的「删除」等操作按钮被裁出屏幕，树区出现横向滚动。

**原因**
- 行内固定宽元素过多（折叠箭头 + 图标 + 数量徽标 + 启停滑块 + 4 个 32px 操作按钮 +
  逐层缩进），320px 视口下单行放不下；名称（flex 可收缩）被压缩到 0，操作组溢出。

**改动**
- style.css（移动端 ≤640px 媒体查询）：
  - 树行允许 flex-wrap 换行：动作按钮组放不下时自动换到第二行（右对齐），
    放得下（大屏手机 / 平板）时保持单行，无需固定断点。
  - 节点 / 事件名称保底 min-width: 60px：换行前名称保持可读，不再被挤成 0 宽。
  - 树区由 flex 列改为块级堆叠：修复「固定高度 flex 父容器把行高压成单行、
    第二行溢出行框」的问题（行内换行后行高按内容增长）。
- 版本升至 v0.7.5（manifest + constants）。
- 同步刷新 tests/_mobile_preview*.html（内嵌 CSS 更新 + 工作台样例改为当前行结构：
  启停滑块、4 个节点操作按钮、深层嵌套、未分类分组；故事预览默认打开工作台）。

**验证**
- 无头 Chrome 按 320 / 360 / 375 / 390 / 414 / 480 / 640px 视口实测：
  修复前 320px 全部节点行名称宽 0、深层行删除按钮超出视口、树区横向滚动；
  修复后各行名称完整可读（≥60px）、操作按钮全部在视口内、树区无横向滚动。
- 全量测试通过：data 42 / ui 41 / proxy 14 / gate 15 / barrier 12（124 项）。

### v0.7.6 修复（2026-08-12）— 移动端面板浮窗可拖动定位（与 SoulLink 一致）

**背景**
- 移动端（≤640px 视口）面板已恢复为「按视图分级浮窗」，但标题栏拖拽仍被
  `isMobileViewport()` 拦截，且移动端 CSS 用 `!important` 固定位置——与同目录的
  SoulLink 相比，浮窗无法按住拖动，体验不一致。

**改动**
- `js/ui-shell.js`：
  - `initDraggablePanel` 不再拦截移动端：标题栏按下即可拖拽；偏移改用
    `getBoundingClientRect()` 视觉位置计算，避免移动端居中布局（带 transform）首帧跳动。
  - `setPanelPosition` 不再跳过移动端：写入内联 left/top 并给对话框加
    `is-user-positioned` 类，同时清掉 right/bottom/transform，让内联位置接管。
  - `ensurePanelPosition` 移动端默认交给 CSS 尺寸模式（居中/铺边不变），
    仅用户已拖动后恢复内联位置。
- `style.css`（移动端 ≤640px 媒体查询）：位置属性去掉 `!important`，让内联样式
  可覆盖；新增 `.kaleido-panel__dialog.is-user-positioned` 解除 CSS 居中/铺边约束，
  并限制 `max-width / max-height`（vw/dvh - 24px）防止拖出视口。
  - 标题栏加 `touch-action: none`：否则触屏拖动会被浏览器接管为滚动/平移，
    触发 `pointercancel` 打断拖拽（悬浮球一直有该规则，标题栏此前缺失）。
- `js/ui-shell.js`：拖拽监听补 `pointercancel` 兜底，防止手势被系统接管后
  `dragState` 卡死。
- 版本升至 v0.7.6（manifest + constants）。

**测试**
- `tests/mobile-layout.test.js`：静态断言移动端拖拽不再被拦截、样式表含
  `is-user-positioned` 规则、位置属性无 `!important` 残留。

## 2026-08-13

### v0.13.2 调整（2026-08-13）— 注入提示词预览移入变量系统

**背景**
- 游戏模式是给玩家看的展示界面，注入提示词预览属于「配置注入」的调试信息，
  放在游戏模式里既占空间又偏离「赏心悦目」的目标；把它移回变量系统，
  作为「注入预览」页（只读），游戏模式只保留玩家关心的数据展示。

**改动**
- `js/views-values.js`：
  - 工作台导航新增「注入预览」标签页（`VALUES_TAB_INJECT_ID` / `VALUES_INJECT_PANE_ID` /
    `VALUES_INJECT_TEXT_ID`），面板内为图标题头 + 墨色卷宗预览框；
    渲染逻辑 `renderValuesInjectPreview()` 与注入管线同源（`buildValuesInjectText`），
    注入开关 / 勾选变化（`refreshValuesInjectUI`）与切到该页时自动刷新。
  - 三个既有标签页（变量树 / 变量注册 / 剧情触发）的切换处理补上对注入预览
    标签与面板的取消激活。
- `js/views-game.js`：移除注入提示词预览面板与 `renderGameInjectPreview`，
  游戏模式只保留镜鉴横幅 + 战况指示条 + 游戏值总览；注入开关与勾选数仍显示在
  HUD 芯片上。
- `js/constants.js`：新增注入预览相关常量，移除 `GAME_INJECT_TEXT_ID`。
- `style.css`：注入预览样式（`kaleido-values__inject-preview-*` / `__inject-text`）
  移入变量系统样式区，移除游戏模式里的预览框样式。
- 版本升至 v0.13.2（`constants.js` / `manifest.json`）。

**测试**
- `tests/game-ui.test.js`：移除注入预览断言（预览区创建 / `<Values>` 内容 /
  未开启提示），保留 HUD 注入状态断言。
- `tests/values-ui.test.js`：新增「注入预览」标签页测试（标签与面板创建、
  切换激活、未开启提示、勾选后显示 `<Values>` 内容、切回变量树取消激活）。

### v0.13.3 调整（2026-08-13）— 游戏模式界面重构：玩家档案

**背景**
- v0.13.1 的「镜鉴战况」仍是工作台式结构：折叠块 + 徽标（注入 / 派生 / 项数）
  + HUD 状态条，信息偏工程、视觉偏重；游戏模式是玩家游玩时看的界面，
  数值应当清晰、美观、简洁。重构为档案体例：只呈现数值本身，不做任何
  工作台式标注。

**改动**
- `js/views-game.js`：
  - 视图结构重构：顶部「档案封面」（朱砂印章徽记 + 题签标题 + 最近更新时间 +
    刷新按钮）→「档案正文」（条目行 + 章节头），移除镜鉴横幅 / HUD 战况条 /
    数据面板三层结构。
  - 条目行改为「名称 · 点线目次 · 数值」的档案目次式；容器改为「章节」——
    朱砂印（章节名）+ 鎏金分隔线，全部平铺展示、不再折叠，移除展开状态
    （`gameExpanded`）与折叠按钮。
  - 移除「注入 / 派生 / 项数」徽标与相关 title 提示；HUD 芯片（游戏值 /
    注入 / 自动维护）整体移除，封面副题只保留最近更新时间。
  - `renderGameStatus` 精简为 `renderGameMeta`；`countGameInjectedLeaves`
    保留（首页卡片状态仍用）。
- `js/constants.js`：移除 `GAME_STATUS_ID` / `GAME_INJECT_STATUS_ID` /
  `GAME_MAINTAIN_STATUS_ID`；版本升至 v0.13.3。
- `style.css`：重写游戏模式整块样式（封面题签 / 印章 / 点线目次 / 章节印 /
  鎏金分隔线 / 空档案），移动端与 `hover: none` 适配同步更新。
- 版本升至 v0.13.3（`constants.js` / `manifest.json`）。

**测试**
- `tests/game-ui.test.js`：按档案结构重写——条目 / 章节断言、无工程标注断言
  （注入 / 派生 / 项数 / 折叠按钮）、章节内容平铺展示、最近更新时间、
  空档案提示；首页卡片状态与刷新按钮断言保留。

### v1.0.0 发布（2026-08-13）— 首个正式版

**背景**
- 自 v0.13.3 起的两项界面调整随本次发布正式定版，版本号升至 1.0.0：
  ① 游戏模式宽度与首页一致；② 手机端变量系统导航隐藏图标。

**改动**
- `js/constants.js`：从 `PANEL_WIDE_MODES` 移除 `GAME_VIEW_ID` 登记，
  游戏模式不再获得 `is-game-mode` 宽视图类，面板保持默认 372px 宽度
  （与首页一致）。
- `style.css`：移除 `.kaleido-panel__dialog.is-game-mode { width: 540px; }`
  规则；在 `@media (max-width: 640px)` 块内新增
  `.kaleido-values__nav-item .kaleido-values__nav-icon { display: none; }`，
  手机端隐藏变量树 / 变量注册 / 剧情触发 / 注入预览四个导航项的图标，
  桌面端工作台不受影响（图标保留）。
- 版本升至 v1.0.0（`constants.js` / `manifest.json` / `README.md`）。

**测试**
- `tests/mobile-layout.test.js`：新增静态断言——手机端媒体查询内应包含
  导航项图标隐藏规则；全部测试套件通过。

### v1.0.1 调整（2026-08-13）— 手机端游戏档案封面保持单行

**背景**
- 手机端（≤640px）游戏模式档案封面此前 `flex-wrap: wrap`，屏幕较小时
  刷新按钮 / 返回主页图标会被挤到第二行；改为封面整体保持单行。

**改动**
- `style.css`：手机端 `.kaleido-game__cover` 改为 `flex-wrap: nowrap` 并
  收紧间距（`gap: 8px`）；题签 `.kaleido-game__cover-title` 不换行；
  最近更新时间 `.kaleido-game__cover-updated` 放不下时省略号截断
  （不再把按钮挤到下一行）；刷新按钮收紧内边距。
- 版本升至 v1.0.1（`constants.js` / `manifest.json`）。

**测试**
- `tests/mobile-layout.test.js`：新增静态断言——手机端媒体查询内
  `.kaleido-game__cover` 应保持 `flex-wrap: nowrap`。

### v0.13.1 调整（2026-08-13）— 游戏模式界面重构：镜鉴战况

**背景**
- 游戏模式是玩家游玩时看的展示界面，旧版只是「状态条 + 列表树 + 预览框」的
  平铺样式，不够赏心悦目；重构为游戏数据展示风格，保持全部功能与 DOM 契约不变
  （所有 `kaleido-game-*` id、行 class / dataset 均保留，测试零改动）。

**改动**
- `js/views-game.js`：
  - 视图结构重构：顶部「镜鉴横幅」（墨色鎏金开场 + 六棱手柄徽章 + 流光扫过 +
    鎏金取景框角饰 + 刷新按钮）→「战况指示条」（HUD 芯片：游戏值 / 注入 /
    自动维护 / 最近更新，状态点带呼吸光晕）→ 两张「数据面板」（游戏值总览 /
    注入提示词预览，宣纸卡 + 四角鎏金 + 图标题头）。
  - HUD 芯片改为「标签 + 值」双段结构（`__hud-label` / `__hud-value`），状态
    刷新只更新值段；整芯片 textContent 仍为「标签：值」，兼容既有断言。
  - 行渲染增强：节点行加文件夹图标（展开/收起切换 `fa-folder-open`/`fa-folder`）；
    叶子数字值加 `is-numeric` 类，渲染为墨底鎏金胶囊（◆ 前缀），与文字值区分。
- `style.css`：
  - 重写游戏模式整块样式：横幅渐变 / 徽章脉冲 / 流光动画 / HUD 呼吸点 /
    面板四角鎏金 / 行悬停浮起 / 药丸属性槽 / 墨色卷宗预览框；树滚动条鎏金化。
  - 补上 `is-game-mode` 宽视图（540px，此前登记未生效）；移动端 HUD 芯片
    两列排布、横幅可换行、面板提示隐藏。
  - `@media (hover: none)` 增加游戏模式行 / 按钮 hover 复位。
- 版本升至 v0.13.1（`constants.js` / `manifest.json`）。

**测试**
- 全量 jsdom 测试通过（含 `tests/game-ui.test.js` 7 项，断言未改动）。

### v0.13.0 新增（2026-08-13）— 游戏模式：玩家数据展示面板

**背景**
- 变量系统把勾选的变量以 `<Values>` 块注入提示词，主模型能看到当前游戏数据，
  但玩家自己看不到——想知道「现在好感多少、金币多少、状态如何」只能去变量
  工作台翻编辑界面；需要一个只读的展示面板，让玩家随时看到当前游戏中的
  各种数据怎么样了。

**改动**
- `js/views-game.js`（新增）：
  - 游戏模式视图：只读展示「变量系统」注入提示词的那些变量，数据源与变量注入
    完全一致（`getValuesGameTree` + `buildValuesInjectText`），展示的就是主模型
    实际看到的内容；不提供任何编辑入口。
  - 状态条：游戏值状态（已初始化 / 未初始化显示默认值 + 数据量）、注入开关与
    实际勾选注入的变量数、自动维护状态（含最近一轮失败 / 更新项数）、最近更新
    时间（聊天游戏值写入时间，回退最近维护轮次时间）+「↻ 刷新」按钮。
  - 游戏值总览：只读树（节点可展开 / 收起），已注册变量名标朱砂色，子变量带
    「派生」徽标，勾选注入的叶子带「注入」徽标。
  - 注入提示词预览：显示实际注入的 `<Values>` YAML 块原文（未开启 / 未勾选时
    给出引导提示）。
  - 自动刷新：视图打开时渲染；每轮生成结束后若视图正打开自动刷新；变量维护
    完成（`runValuesMaintain` 收尾）后再刷一次，保证展示最新值。
- `js/constants.js` / `js/ui-shell.js` / `js/views-home.js` / `style.css`：
  - 首页新增「游戏模式」卡片（手柄图标），状态显示数据量 + 注入开关与勾选数；
    点击进入游戏模式视图（手机端 / 电脑端均为面板内视图）。
  - 视图注册进 `PANEL_VIEW_TITLES`（标题「游戏模式」）与 `PANEL_WIDE_MODES`
    （宽视图模式）。
- `js/main.js`：`generationEnded` 挂游戏模式刷新钩子（视图未打开时为空操作）。
- 版本升至 v0.13.0（`constants.js` / `manifest.json`）。

**测试**
- `tests/game-ui.test.js`（新增）：视图与首页卡片创建 / 游戏值总览渲染（值、
  注入徽标、派生徽标）/ 注入预览 `<Values>` 块内容与未开启提示 / 节点展开收起 /
  游戏值已初始化显示聊天数据与更新时间 / 首页卡片状态 / 刷新按钮。

### v0.12.0 新增（2026-08-13）— 变量系统：父变量 / 子变量

**背景**
- 变量注册此前只有一种类型：全部由 AI 按「变化规则」维护。但很多变量是**派生
  状态**（如「态度」由「好感度」决定），让 AI 每轮重复维护既浪费调用又容易跑偏；
  需要一种确定性派生：子变量按父变量值的区间自动计算，不参与 AI 维护。

**改动**
- `js/values-data.js`：
  - 键注册表新增 `type`（`parent` 默认 / `child`）+ 子变量字段 `parent`（父变量名）
    与 `rules`（区间规则 `[{ min?, max?, value }]`，min / max 可省略，按顺序匹配
    首个满足的区间）；旧键缺省 type 就地归一化为父变量。
  - 新增 `isValuesChildKey` / `isValuesParentKey` / `getValuesChildKeys` /
    `getValuesChildKeysByParent` / `normalizeValuesChildRules`；
    `deriveValuesChildAt` / `deriveValuesChildren` 按同路径父变量值派生子变量
    （父变量缺失 / 非数值 / 无区间命中时保持原值，多轮收敛支持链式导入数据）。
  - 派生在**读取时自动计算**：`getValuesGameTree`（克隆后派生）与
    `getValuesDefaults`（就地派生）都返回派生后的树；`serializeValuesBundle`
    导出前派生一次，默认值树里的子变量按父变量当前值输出。
  - YAML 导入 / 导出：子变量写 `type: child` + `parent` + `rules`，父变量写
    `type: parent` + `rule`；导入兼容旧包（无 type 按父变量），合并导入同步更新
    类型 / 父变量 / 区间。
  - 条目顺序表：变量包新增 `order`（`{ 父路径: [条目名] }`，顶层路径为 `''`），
    记录玩家拖动排序；`valuesOrderedNames` 按顺序表渲染、未记录条目按名称排序
    追加；`reorderValuesKeys` / `reorderValuesTriggers` / `reorderValuesTreeAt`
    写回顺序；YAML 导出 `order` 段、导入合并 / 覆盖同步。
- `js/values-maintain.js`：
  - `<Key_Rules>` 只列父变量，并注明子变量为派生变量禁止修改；
  - 补丁合并后按最新父变量重算子变量（AI 对子变量的改动一律被派生结果覆盖），
    子变量路径不计入变化列表（父变量变化已体现在父变量路径上）。
- `js/views-values.js` / `js/constants.js` / `style.css`：
  - 键注册编辑器新增「变量类型」选择：父变量显示变化规则；子变量显示父变量下拉
    （只列已注册父变量，排除自身）+ 派生区间编辑器（下限 ~ 上限 + 文本，可增删行）。
  - 派生区间校验：区间不能重叠（含边界，如 `0~1000` 之后只能从 `1001` 开始），
    重叠 / 下限大于上限的行实时标红并阻止保存；新添加的行自动接续上一行上限 + 1。
  - 键列表行显示「父 / 子」类型徽标，子变量行显示派生摘要（`由「好感度」派生：…`）。
  - 树里子变量叶子带「派生」徽标、不可手动编辑（编辑按钮禁用）；新建子变量时
    变量值可留空（留空存 null，父变量存在时自动计算）。
  - 删除父变量前检查依赖：有子变量引用时阻止并提示先删除 / 改绑。
  - 条目拖动排序：变量树 / 变量注册 / 剧情触发三处列表的行首新增「⋮⋮」把手，
    按住上下拖动调整同级顺序（树按父路径分组，只允许同级互排），松手后写回
    数据层并重渲染；拖动中行半透明、容器禁用文本选择。
  - 顺带修复：键列表行内编辑 / 删除按钮取名的 dataset 读取（此前取按钮自身
    data-name 恒为空，列表行编辑 / 删除实际不可用）。
- 版本升至 v0.12.0（`constants.js` / `manifest.json`）。

**测试**
- `tests/values.test.js`：子变量注册与归一化 / 区间命中（40 → 颇具好感、
  90 → 生死相依、边界 30）/ 默认值层就地派生 / 父变量缺失与非数值保持原值 /
  AI 维护后子变量自动重算且 AI 改动被覆盖 / 维护消息排除子变量 / YAML 往返。
- `tests/values-ui.test.js`：子变量注册表单（类型切换、父变量下拉、区间行增删）/
  树行派生徽标与禁用编辑 / 新建子变量值可留空提示 / 删除父变量依赖保护。
- 拖动排序：`tests/values.test.js` 键 / 触发 / 树顺序表与 YAML order 往返；
  `tests/values-ui.test.js` 三处列表拖动把手排序（指针事件 + 模拟行高）。

### v0.10.0 新增（2026-08-13）— 剧情触发：按变量当前值确定性触发剧情事件

**背景**
- 「剧情脉络」由预筛 AI 依据对话判断触发，存在不确定性；需要一套**确定性**的
  触发机制：不依赖 API，直接按「某节点下变量的当前值」是否满足预设条件判定。
- 与「剧情脉络」互补：剧情脉络负责 AI 判断，剧情触发负责变量条件判定，两者独立
  运行，最终都作为剧情事件注入后续流程。

**改动**
- `js/values-trigger.js`（新增）：剧情触发数据模型 / 条件求值 / 注入管线。
  - 数据：随变量包 `triggers` 字段保存（角色卡 / 全局设置），每条触发含
    `id / name / enabled / logic（all=且 / any=或）/ conditions（path + op + value）/
    description / content`；ID 从 001 自动递增，支持创建 / 更新 / 删除 / 启停。
  - 求值：`evaluateValuesCondition` 支持 `== != > >= < <= contains exists not exists`，
    数字 / 布尔 / 字符串 / 混合类型比较，未定义变量除 exists / not exists 外一律不满足；
    `evaluateValuesTrigger` 按 logic 组合条件，`evaluateValuesTriggers` 返回满足条件
    且启用的触发（读当前游戏值，未初始化回退默认值）。
  - 注入：发送前任务注册进跨扩展发送屏障（与剧情预筛并发，同步判定无 API 调用），
    满足条件的事件以 `<Story_Trigger>` 块注入（IN_CHAT, SYSTEM，与 `<Story_Event>`
    同款强制指令）；generationEnded / generationStopped 后清空；最近一轮快照存
    `globalThis.__kaleido_values_trigger_last_round__`。
- `js/values-data.js`：变量包 YAML 导入 / 导出新增 `triggers` 段（合并导入同 id 更新、
  覆盖导入整体替换）；角色卡迁移 / 回退全局设置时携带 triggers。
- `js/views-values.js` / `js/constants.js` / `style.css`：
  - 变量工作台新增「**剧情触发**」导航页：总开关 + 状态条 + 触发列表（名称 /
    条件摘要 / 启停滑块 / 编辑 / 删除）。
  - 触发编辑器：事件名称、条件逻辑（且 / 或）、动态条件行（变量路径下拉 +
    运算符 + 比较值，exists / not exists 自动隐藏值输入）、事件说明、事件正文。
  - 条件路径下拉自动列出变量树全部叶子路径（结构以默认值为准）。
- `js/views-preset.js` / `js/ui-shell.js`：预设模版页新增「剧情触发」开关区
  （settings `valuesTriggerEnabled`，默认开）。
- `js/views-home.js`：首页「变量」卡片状态追加触发数量与关闭提示。
- `js/views-inject.js` / `js/ui-shell.js`：注入实录视图新增「剧情触发」段
  （摘要 + 触发事件 + 注入原文）。
- `js/main.js`：挂载剧情触发的 generationEnded / generationStopped 清理事件。
- 版本升至 v0.10.0（manifest + constants）。

**测试**
- `tests/values-trigger.test.js`（新增，14 项）：触发 CRUD / 归一化 / 条件求值
  （数值 / 布尔 / 字符串 / 混合 / 未定义）/ 且或逻辑 / 无条件恒不触发 / 停用排除 /
  注入文本 / 条件摘要 / 发送前任务（IN_CHAT 注入 / 不满足与关闭与非用户末条跳过）/
  生成结束清理 / YAML 导出解析往返 / 合并与覆盖导入。

### v0.11.0 新增（2026-08-13）— 剧情触发：事件类型（一次性 / 常驻）

**背景**
- 剧情触发此前只有一种行为：条件满足就注入，且保持启用、可反复触发。
- 实际剧情里大量事件是「只发生一次」的（告白、真相揭露、战争爆发），需要触发后
  自动关闭，避免同一事件反复注入；同时保留可重复触发的常驻事件。

**改动**
- `js/values-trigger.js`：触发新增 `once` 字段（`true` = 一次性，默认；`false` = 常驻）。
  - 归一化：`once: raw?.once !== false`，旧数据 / 未指定一律按一次性处理。
  - 新增 `autoDisableFiredValuesTriggers`：注入成功后把已触发的一次性事件置为停用
    并持久化，返回被关闭的 id 列表；常驻事件不受影响。
  - 发送前任务：注入成功后自动关闭一次性事件，轮次记录新增 `autoDisabledIds`，
    toast 提示「N 个一次性事件已自动关闭」。
- `js/values-data.js`：变量包 YAML 导出 / 解析新增 `once` 字段，往返保留事件类型。
- `js/views-values.js` / `js/constants.js` / `style.css`：
  - 触发编辑器新增「**事件类型**」下拉：一次性事件（触发后自动关闭，默认）/
    常驻事件（可重复触发）。
  - 触发列表行新增类型徽标：一次性（朱砂）/ 常驻（黛蓝）。
- `js/views-inject.js`：注入实录摘要追加「N 个一次性事件已自动关闭」。
- 文档：`变量系统指南.md` 补事件类型说明与 YAML `once` 字段；`插件结构.md` 更新
  触发数据字段。
- 版本升至 v0.11.0（manifest + constants）。

**测试**
- `tests/values-trigger.test.js`（16 项）：归一化默认一次性 / once: false 常驻 /
  一次性触发后自动关闭且常驻保留 / 第二轮一次性不再注入常驻仍注入 / YAML 导出
  once 往返。
- `tests/values-ui.test.js`（21 项）：编辑器事件类型默认一次性、保存与回显、
  列表徽标（一次性 / 常驻）。

### v0.9.0 新增（2026-08-13）— 变量系统：默认数值层勾选节点/变量，注入提示词

**背景**
- 变量系统此前只做「存储 + AI 维护」，主模型生成时看不到变量状态，剧情与变量
  容易脱节（AI 维护更新了数值，但生成时不知道当前值）。
- 需要一种轻量方式：作者在「默认数值」层勾选要暴露的节点 / 变量，发送时把
  当前变量状态注入提示词，让主模型直接参考。

**改动**
- js/values-data.js：变量包新增 inject 字段（{ enabled, paths }），随角色卡 /
  全局设置保存；新增 getValuesInjectConfig / saveValuesInjectConfig /
  setValuesInjectEnabled / setValuesInjectPath / 	oggleValuesInjectPath /
  isValuesInjectPathSelected——勾选节点 = 整棵子树注入（自动移除后代路径），
  取消勾选节点 = 节点与后代一并移除；角色卡迁移 / 回退全局设置时携带 inject。
- js/values-inject.js（新增）：uildValuesInjectText 按勾选路径裁剪当前游戏值
  （未初始化回退默认值）生成 <Values> YAML 块（祖先覆盖后代去重、缺失路径跳过）；
  发送前任务注册进跨扩展发送屏障（与剧情预筛并发），setExtensionPrompt(IN_PROMPT,
  depth 0) 注入到 **World Info (after) 之后**；generationEnded /
  generationStopped 后清空注入，下一轮重新注入最新值。
- js/host.js：新增通用 getExtensionPromptApi（含 IN_PROMPT 枚举兜底），
  剧情预筛的 getStoryGateExtensionPromptApi 改为委托复用。
- js/views-values.js / js/constants.js / style.css：
  - 「默认数值」层顶部新增**注入提示词**开关条（滑块开关 + 勾选统计状态，
    滑块样式与剧情脉络节点启停一致）；
  - 树行左侧新增注入滑块（仅默认数值层显示，与剧情脉络节点启停同款），
    节点上下级联动：打开下级自动提升全部祖先，关闭上级级联关闭全部后代，
    打开上级不自动打开下级（下级选择性打开）；只注入「自身打开」的变量，
    容器节点本身不注入内容；半选（滑块居中）仅出现在数据不一致场景。
  - 树行支持**双击进入编辑**：双击变量行打开变量编辑器，双击节点行打开
    节点编辑器（按钮 / 滑块等交互元素上的双击不触发）。
- js/main.js：挂载变量注入的 generationEnded / generationStopped 清理事件。
- 版本升至 v0.9.0（manifest + constants）。

**测试**
- 	ests/values.test.js：注入配置默认值 / 开关 / 勾选路径 / 节点覆盖后代 /
  祖先覆盖判断 / 随角色卡保存。
- 	ests/values-inject.test.js（新增）：注入文本构建（关闭 / 未勾选 / 节点子树 /
  去重 / 游戏值回退 / 缺失路径跳过）、发送前任务（IN_PROMPT 位置 / 关闭不调用 /
  非用户末条跳过）、生成结束清理。
- 	ests/values-ui.test.js：注入条层可见性 / 开关写入配置 / 行内勾选交互 /
  节点半选状态。

### v0.8.1 调整（2026-08-13）— 变量系统：默认值仅手动修改，游戏值可一键重置为默认值

**背景**
- 变量系统两层（默认值 / 游戏值）的职责此前只在文档里说明，界面没有体现：
  「✨ 立即维护」在默认值层也常显，容易让人误以为 AI 会维护默认值。
- 游戏值被 AI 维护跑偏后没有一键恢复手段，只能手动逐项改回。

**改动**
- `js/views-values.js` / `js/constants.js` / `style.css`：
  - 层按钮文案与 title 明确职责：**游戏值 · AI 维护**（每轮生成后由 AI 自动维护，
    可一键重置为默认值）/ **默认值 · 手动**（仅手动修改，AI 自动维护不会改动）。
  - 自动维护行（✨ 立即维护 + 状态）与新增的 **↺ 重置为默认值** 按钮只在游戏值层
    显示；默认值层改为显示提示「默认值仅手动修改 · AI 自动维护只更新游戏值」。
  - 新增 `handleValuesResetGame()`：确认后把当前聊天的游戏值整体重置为角色卡默认值
    （写入聊天文件，并清空 lastSignature，避免重置后自动维护被旧签名去重跳过）。
- 文档：`变量系统指南.md` 补「重置游戏值」说明与两层职责提示。

**测试**
- `tests/values-ui.test.js`：层联动（默认值层隐藏维护/重置、显示手动提示；游戏值层
  反之）；游戏值重置（确认弹窗、值恢复为默认、lastSignature 清空、树刷新）。

## 2026-08-14

### v1.1.1 新增（2026-08-14）— 主题系统：新增「手绘涂鸦」主题（方方正正）

**背景**
- 已有「macOS 风格」「古风典雅」双主题；希望再来一套手绘涂鸦风（白纸 + 圆珠笔/
  蜡笔的涂鸦笔记本感），但形状要求**方方正正**——全直角、不圆角、不旋转。

**改动（照 gufeng 主题模式，仅换皮肤不改布局）**
- `js/constants.js`：`THEMES` 注册表追加 `{ id: 'doodle', name: '手绘涂鸦' }`
  （菜单自动出现新选项，无需改 UI 代码）；版本升至 v1.1.1。
- `style.css` 末尾追加 `html[data-theme='doodle']` 覆盖块（置于暗色媒体查询之后，
  固定亮色皮肤）：
  - **变量层**：泛黄白纸底（`#fdf8ec` 系）+ 石墨铅笔字 `#33302b` + 圆珠笔蓝强调
    `#2b6fd4`，语义色为圆珠笔红/绿/橙/紫；字体栈换手写体
    （`Comic Sans MS` / `Segoe Print` / `Marker Felt` / `Bradley Hand`，
    中文兜底楷体）；阴影改「硬纸板」式（偏移实色不模糊）。
  - **直角兜底规则**：面板 / 悬浮球 / 工作台对话框 / 确认弹层内部所有元素
    `border-radius: 0`（含按钮、输入框、状态徽标胶囊、开关滑块、滚动条滑块），
    新增组件也自动保持直角。
  - **组件层**：逐段覆盖（照 gufeng 清单）——悬浮球 = 方形便利贴（白底 +
    圆珠笔蓝描边 + 硬纸板阴影，无旋转）；面板 / 对话框 = 白纸 + 石墨粗框 +
    圆珠笔蓝内线的双线描边；按钮 = 贴纸（白底 + 铅笔描边 + 硬阴影，按下位移）；
    输入框 = 答题纸下划线式；内容区 / 日志控制台 = SVG 噪点纸张颗粒；
    游戏模式封面 = 便利贴，印章 = 红色方签（无旋转）；主题菜单 / 新建菜单等
    其余组件逐一换皮。
- `manifest.json` / README / 开发日志：版本与主题列表同步。

**测试**
- 追加主题断言：style.css 含 `html[data-theme='doodle']` 变量块（定义
  `--k-window`）与直角兜底规则。

### v1.1.0 新增（2026-08-14）— 悬浮球半透明磨砂玻璃背景（双主题）+ 标语精简「镜中万象」

**背景**
- 悬浮球此前为实色底（macOS 风格：磨砂白 `#f8f8fc`；古风典雅：实心朱砂红），
  不透出背后的聊天内容，与 macOS 风格「毛玻璃」质感不符；希望悬浮球改为
  半透明磨砂玻璃，透出并模糊背后的页面。

**改动**
- `style.css`：新增主题变量 `--k-sphere-glass`（亮色半透明白 `rgba(255,255,255,0.45)` /
  暗色半透明深灰 `rgba(58,58,62,0.5)`）；`.kaleido-sphere__frame` 背景由实色
  `#f8f8fc` 改为该变量并加 `backdrop-filter: blur(14px) saturate(1.6)`
  （含 `-webkit-` 前缀），呈现半透明磨砂玻璃效果，保留内嵌高光描玻璃边。
- `style.css`：古风典雅主题的朱砂印章悬浮球同步改为半透明朱砂红
  `rgba(176, 58, 46, 0.55)` + 同一 `backdrop-filter` 模糊，保留墨色描边 /
  印章硬阴影 / 微旋转与「镜」字米白扇叶。
- `js/ui-shell.js`：首页大标语与面板页脚标语由「镜中万象，皆是文章」精简为
  「镜中万象」（首页保留首字「镜」放大字形样式）。
- `tests/`：同步更新预览快照 HTML（`_mobile_preview` 等）。
- 版本升至 v1.1.0（`constants.js` / `manifest.json` / README）。

**测试**
- 全量测试通过：14 个测试套件（ui / values-ui / game-ui / ui-mobile / mobile-layout /
  data / gate / proxy / barrier / values / values-inject / values-trigger / confirm / build）。

### v1.0.8 调整（2026-08-14）— 前端整体重设计：macOS 风格界面

**背景**
- 旧界面是「古风典雅」主题（宣纸 + 墨色 + 朱砂 + 鎏金），与万华镜意象统一但视觉
  语言偏传统；希望换一套更现代、风格统一、亮/暗环境都好看的前端。
- 本次是**全新重设计**（非修改/优化）：style.css 全部重写，类名与 DOM 结构不变
  （JS 无需改动），只换皮肤。

**改动（style.css 整体重写）**
- **设计系统**：macOS Sonoma 风格——SF 系统字体栈、实色不透明面板
  （取消 backdrop-filter 模糊）、0.5px 发丝描边、大圆角卡片、
  蓝色强调色（#007aff / #0a84ff）；prefers-color-scheme 亮/暗双主题自适应。
- **主面板**：macOS 窗口样式，标题栏左上返回键、右上关闭键（工具栏图标键风格，标题居中，无徽记图标）；悬浮球改为玻璃质感万花筒（旋转镜眼铺满球面）。
- **主页标题**：首页标题显示英文铜板体字标「Kaleidoscope」（Copperplate 雕刻感大写 + 蓝紫渐变），其余视图保持中文标题。
- **工作台**：剧情脉络 / 变量工作台大窗口改为 macOS 应用窗口（标题居左 + 右上工具行 +
  实色面板），左侧导航为 Finder/系统设置式侧栏（选中项蓝色高亮），新建菜单为
  macOS 上下文菜单样式，编辑器为右侧实色浮层。
- **控件**：按钮（主按钮蓝渐变 / 幽灵 / 迷你）、输入框（蓝色聚焦光环）、
  macOS 开关（蓝色滑块，部分选中为橙色）、分段控件（日志级别 / 预设标签页）、
  状态胶囊（绿/橙/红圆点）、细窄滚动条。
- **游戏模式**：档案封面改为 macOS 应用图标风格（蓝紫渐变圆角图标），
  数值列保持「可收缩 + min-width:0」的回归修复。
- **日志**：控制台保持深色终端风（亮暗主题下都深色，与 Xcode 控制台一致）。
- 保留既有布局决策：面板始终内联 left/top 定位、剧情工作台移动端 540px 视窗、
  手机端变量导航横排、触控目标放大、iOS 16px 防聚焦缩放。

**测试**
- tests/ui.test.js 主题回归断言更新为新 token（--k-window / --k-text）。
- 全量测试通过：ui 45/45、values-ui 33/33、game-ui 7/7、ui-mobile 7/7、
  mobile-layout 静态检查、data 42/42、gate 15/15、proxy 14/14、barrier 12/12、
  values 37/37、values-inject 10/10、values-trigger 16/16。

### v1.0.9 新增（2026-08-14）— 主题系统：新增「古风典雅」主题

**背景**
- v1.0.8 整体重设计为 macOS 风格后，希望沿用同目录 SoulLink 的主题切换机制，
  把 v1.0.8 之前的「古风典雅」皮肤（宣纸 + 墨色 + 朱砂 + 鎏金）以「主题」形式请回来，
  与 macOS 风格并列可选。

**改动（照 SoulLink 的主题模式，仅换皮肤不改布局）**
- `js/constants.js`：新增 `THEMES` 主题注册表（id 对应 style.css 的
  `[data-theme='...']` 覆盖；`macos`「macOS 风格」默认 + `gufeng`「古风典雅」）、
  `DEFAULT_THEME='macos'`、`THEME_ID` / `THEME_MENU_ID`；`DEFAULT_SETTINGS` 增加
  `theme: DEFAULT_THEME`。
- `js/ui-shell.js`：新增 `getCurrentTheme()` / `applyTheme()` / `initThemeSection()`——
  给面板与悬浮球设 `data-theme`、更新按钮文本、菜单渲染与选中持久化
  （`settings.theme` + `saveSettings`），点外部 / Esc 关闭菜单；页脚新增主题按钮 + 弹出菜单。
- `js/main.js`：bootstrap 创建面板与悬浮球后调用 `applyTheme(getCurrentTheme())` 应用当前主题。
- `style.css`：末尾追加 `:where([data-theme='gufeng'])` 覆盖块（置于暗色媒体查询之后，
  暗色系统下也生效）——变量覆盖（墨字 #2b241c / 宣纸底 #faf5e9 / 墨色标题栏 /
  朱砂 #b03a2e / 鎏金 #c9a227 / 楷体字体栈）+ 组件覆盖（悬浮球→朱砂印章「镜」字、
  面板→宣纸+墨框+鎏金内线、标题栏→墨带鎏金字、窗控→印章键、首页标题→楷体鎏金、
  按钮/输入框→印章/下划线、日志控制台、确认弹层、工作台对话框、页脚与主题菜单）。
  页脚新增主题按钮/菜单的基础样式（macOS 风格亦适用）。

**测试**
- 全量测试通过：ui 46/46、values-ui 34/34、game-ui 7/7、ui-mobile 7/7、
  mobile-layout 静态检查、data 42/42、gate 15/15、proxy 14/14、barrier 12/12、
  values 37/37、values-inject 10/10、values-trigger 16/16。

**补充（同日）— 主题覆盖补全**
- 修根因：`applyTheme` 原只在面板 / 悬浮球设 `data-theme`，剧情脉络 / 变量系统的
  工作台对话框是懒创建、且与面板平级挂在 body 下，永远拿不到主题。改为以
  `<html>` 作为唯一主题作用域（`applyTheme` 设 `documentElement.dataset.theme`），
  所有宿主元素（含后续懒创建的工作台对话框、确认弹层、新建菜单等）都是 html 后代，
  主题经变量继承自动覆盖到每一个窗口；gufeng 覆盖块同步改为 `html[data-theme='gufeng']` 作用域。
- 补齐古风典雅覆盖：游戏模式（档案封面→宣纸签、印章→朱砂、刷新/齿轮→印章、标题字距）、
  日志系统（筛选片 / 操作按钮 / 回到最新→印章式、选中态朱砂）、剧情脉络工作台
  （工具条 / 行图标 / 危险按钮 / 新建菜单 / 导入浮层→宣纸签 + 墨框）、变量系统工作台
  （导航选中 / 层图标 / 状态条 / 注入条→宣纸签 + 朱砂淡染）。

**补充（同日）— 子变量提示元素恢复**
- 修 bug：变量新建编辑器模板在早前重构中丢失了「子变量提示」元素
  （`VALUES_EDITOR_CHILD_HINT_ID`，`syncValuesKeyEntryChildHint` 一直引用它），
  导致选择子变量时提示「子变量值由父变量自动计算，可留空」不显示、标签不切
  「可留空」；模板补回该 span（`kaleido-values__editor-hint`），
  values-ui 测试「新建变量：选择子变量时值可留空并显示提示」由失败转通过。

### v1.0.7 功能（2026-08-14）— 内置默认注册变量：友谊 / 情欲（含等级）

**背景**
- 玩家每次换新角色卡 / 新聊天都要先注册「友谊」「情欲」等常用变量才能新建变量，
  重复劳动；希望最常用的变量在任何角色卡 / 聊天里都默认注册好，
  新建变量时直接就能在下拉里选到，无需再手动注册。

**改动**
- `js/constants.js`：新增 `VALUES_BUILTIN_KEYS` 内置注册表——友谊（父变量，
  规则「友谊度区间为0~100，变化幅度取决于当前关系…」）+ 友谊等级（子变量，
  父=友谊，lv1 萍水相逢 ~ lv5 生死与共 五个派生区间）；
  情欲（父变量，规则「情欲值的变化幅度取决于亲密行为的强度…」）+ 情欲等级
  （子变量，父=情欲，lv1 暗生情愫 ~ lv5 鱼水之欢 五个派生区间）。
- `js/values-data.js`：`getValuesKeys` 改为返回「内置键副本（带 builtin
  标记）+ 卡内注册键」，卡内同名键自动遮蔽内置，删除卡键后内置自动恢复
  （编辑内置 = 生成卡级自定义规则，删除 = 恢复内置默认）；新增
  `isValuesBuiltinKey` / `valuesTreeContainsName`；`getValuesChildKeysByParent`
  排除内置子变量（删除卡内「友谊」不会被内置「友谊等级」误拦截）。
- `js/values-maintain.js`：内置变量的规则只在游戏值树里已出现该变量名时才
  列入维护提示词（防止 AI 在从未使用过内置变量的聊天里凭空创建）；
  空态跳过条件改为只认卡内注册键（没有值也没有卡键时不发起维护）。
- `js/views-values.js` / `style.css`：变量注册页内置行带「内置」徽标、
  不可拖动、无删除按钮；编辑内置行保存即生成卡级覆盖（行变为普通卡键行，
  可删除恢复默认）；删除内置名卡键时确认文案改为「删除自定义规则并恢复
  内置默认」。
- 内置键为虚拟合并：不写入角色卡 / 全局设置、不随 YAML 导出；
  导入含同名键的包照旧按卡保存（遮蔽内置）。
- 版本升至 v1.0.7（`constants.js` / `manifest.json`）。

**测试**
- `tests/values.test.js`：注册键用例改为覆盖内置常驻 / 卡键遮蔽 / 删除恢复；
  新增内置子变量参与派生、维护提示词按树中出现过滤、YAML 导出不含内置键、
  valuesTreeContainsName。
- `tests/values-ui.test.js`：键注册与拖动排序用例适配内置行；新增内置行
  徽标 / 无删除钮 / 编辑生成卡级覆盖 / 删除恢复内置默认用例。

## 2026-08-16

### v1.3.0 新增（2026-08-16）— 地图系统：游戏地图编辑器 + 游戏模式双入口

**背景**
- 变量系统解决了「数值」维度（好感 / 金币 / 状态……），地图补上「空间」维度：
  剧情发生在哪、玩家能去哪，一直只能靠文字脑补。做一张与角色卡绑定的地图，
  游戏模式里直接看到「学校 / 森林 / 城镇」在哪里，玩家代入感更强。

**新增 · 变量工作台「游戏地图」tab（编辑器）**
- 变量工作台导航新增第 5 个 tab「游戏地图」（`js/views-values.js` 只加导航项 +
  挂载点，内容全部由 `js/views-map.js` 渲染；桌面大窗口 / 手机面板视图共用）。
- 上传图片 → **裁剪弹层**（全屏遮罩挂 body，图片等比缩放居中铺「视图层」，
  裁剪框拖动移位 + 四角缩放，最小 60px，确认时按视图层比例换算回自然像素，
  canvas 裁剪后最长边 > 1600px 等比降采样，导出 PNG）→ 即为地图背景。
- **双击**地图任意处添加地点（百分比坐标，展示 / 编辑按比例对齐，换背景不丢
  坐标）；**拖动**调整位置；点击选中后改名（如「学校」）或删除。
- 编辑先落内存（`mapEditorState`），有未保存修改时保存键转橙色；「保存」才写
  角色卡（`extensions['kaleidoscope_map']`，随卡导入导出），保存后从磁盘重读
  校验；群聊 / 未选角色回退全局设置 `mapData`。「删除地图」整卡清空，保存时
  触发整键哨兵删除。

**新增 · 游戏模式双入口（游戏地图 / 游戏数据）**
- 游戏模式封面下方两个切换图标：**游戏地图**（默认，背景图 + 地点标记常显名称、
  点击高亮；无地图显示空态 + 「去编辑地图」跳变量工作台）与**游戏数据**（原有
  游戏值总览档案）。刷新 / 更新时间 / 返回主页原样保留。

**数据层 · 深合并防坑沿用变量系统方案**
- 新文件 `js/map-data.js`：写卡前磁盘重读 → `buildMapUnsetPatch`（清空背景 →
  background 哨兵；空地图 → 整键哨兵）→ `persistMapCardData`；改动立即落盘
  不做防抖；`verifyMapCardWrite` 把宿主静默失败变成可见反馈。
- 新文件 `js/map-image.js`：图片读取 / 裁剪 / 降采样（纯函数，jsdom 加载安全）。

**测试**
- `tests/map-data.test.js`（13 用例）：读取 / 归一化（坐标收敛、非法点丢弃）、
  立即落盘、刷新重进不丢数据、背景清空哨兵补丁、整键删除、设置兜底、写卡校验。
- `tests/game-ui.test.js` 追加 5 用例：双图标与默认激活、切换、地图空态、地图
  展示（背景 + 标记 + 更新时间）、「去编辑地图」跳转。全部测试通过。

### v1.3.0 修复（2026-08-16）— 保存误报「未生效」：宿主重排 JSON 键序，校验改用忽略键序的深比较

**背景**
- 用户实测：编辑器点「保存」后 toastr 报「保存未生效：角色卡写入被宿主忽略，
  请重试」，但系统日志显示 merge-attributes 返回 `200 {"ok":true}`、重读
  `/api/characters/get` 也能读出地图——**写入其实成功，校验误判**。
- 根因：校验（`verifyMapCardWrite`）用 `JSON.stringify(onDisk) ===
  JSON.stringify(expected)` 全等比较，而宿主（TauriTavern 的 Rust 序列化器）读回
  角色卡时会把对象键按自己的顺序重排——日志里发送的 payload 是
  `{"version":1,"background":...}`，重读回来的卡里是 `{"background":...}` 在前。
  内容完全一致，键序一变字符串就不同，于是误报。

**改动**
- `js/utils.js`：新增 `jsonDeepEqual`——JSON 语义深比较：对象**忽略键顺序**、
  数组顺序敏感、原始值严格相等（`null` 与 `undefined` 区分）。
- `js/map-data.js` / `js/values-data.js`：`verifyMapCardWrite` /
  `verifyValuesCardWrite` 改用 `jsonDeepEqual`（变量系统同款校验有同样的隐患，
  一并修复）。
- `js/utils.js`：新增 `describeJsonDiff`——校验不一致时输出第一个差异字段
  （增删 / 值变化 / 数组长度），随系统日志可见，后续再报「保存未生效」可直接
  定位真实原因（如宿主合并语义吞掉某字段）。
- `tests/map-data.test.js`：新增 3 用例——宿主重排键序的相同内容校验通过
  （按实测逆序重建磁盘卡）；`jsonDeepEqual` 本身的相等 / 不等矩阵；
  `describeJsonDiff` 的差异定位。

### v1.2.2 修复（2026-08-16）— 默认值删除不生效：深合并宿主下用哨兵标记删除键

**背景**
- 保存按钮上线后，用户实测删除 李四 + 保存：系统日志显示三次
  `POST /api/characters/merge-attributes` 全部返回 `200 {"ok":true}`，但角色卡
  PNG（龙族2·悼亡者之瞳）mtime 停在 21:03 未变、李四原封不动——写入从未落地，
  校验报「保存未生效」是**正确的**，错在写入方式。
- 根因：TauriTavern 的 merge-attributes 是**深合并语义**（Rust
  `merge_json_value_with_unset`）：只更新请求里出现的键，请求里没有的键原样保留。
  删除 李四 后发送的 bundle 里没有 李四，宿主合并结果与磁盘原内容完全相同，
  `write_character_card_json` 的短路分支（新 PNG 字节 == 原字节 → 跳过写盘直接
  返回缓存）静默吞掉写入——所以返回 ok:true 但磁盘从未改变，删除的内容刷新后
  又从磁盘回滚。

**改动**
- `js/constants.js`：新增 `VALUES_UNSET_SENTINEL = '__@@UNSET@@__'`（宿主删除键
  的哨兵约定，与 SillyTavern / TauriTavern 一致）。
- `js/values-data.js`：
  - 新增 `readValuesCardFromDisk`——写卡前经 `/api/characters/get`（优先独立
    fetch，不触碰内存 characters；回退宿主 `getOneCharacter`）从磁盘重读当前
    变量包，兼容宿主返回的两种形状（normalizeCharacter 嵌套 `data.extensions`
    与展平顶层 `extensions`）。
  - 新增 `buildValuesUnsetPatch`——递归对比磁盘旧包与新 bundle：旧有新无的键
    标记哨兵（宿主合并时删除），新值 / 变化值直接携带，数组与标量整体替换
    （宿主对非对象值即整体覆盖）。
  - `persistValuesCardData` 写卡前先重读磁盘旧值生成哨兵补丁再发送；写卡后把
    内存角色恢复为干净的新包（writeExtensionField 就地写入了带哨兵的补丁，
    避免哨兵残留 UI）。重读失败（无 fetch / getOneCharacter）时按全量覆盖发送
    （降级为旧行为）。
  - `verifyValuesCardWrite` 读取兼容展平响应形状（`data?.extensions ??
    extensions`）。
  - `saveValuesCardNow` / `saveValuesData` 返回落盘 Promise：保存按钮
    `await saveValuesData` 后再校验，避免校验与写入并行读到旧数据误报。
- `js/views-values.js`：`handleValuesSaveNow` 先等落盘完成再重读校验。

**测试**
- `tests/values-persist.test.js` 升级为 14 用例：写卡 mock 改为模拟宿主真实深合并
  语义（对象逐键合并 + 哨兵删除 + 数组整体替换），`/api/characters/get` 用
  fetch mock 模拟（不替换内存 characters，与真实宿主一致）。新增：深合并宿主下
  删除单个角色 / 删除全部 / 修改值只覆盖变更键，以及展平 get 响应下校验仍通过。
- 全量 15 个测试文件通过（含 UI / 数据 / 触发 / 构建一致性）。

### v1.2.1 调整（2026-08-16）— 默认数值界面新增「保存」按钮 + 写卡磁盘校验

**背景**
- 绑定恢复、删改立即落盘后，用户仍反馈「无法成功修改绑定的默认值」。
  宿主（TauriTavern）的 `writeExtensionField` 失败只 `console.error` 不抛错，
  扩展无法感知写入是否真正落盘——需要把静默失败变成可见反馈。

**改动**
- `js/views-values.js` / `js/constants.js`：默认数值层标题旁新增「💾 保存」按钮
  （仅默认层显示，游戏值随聊天文件即时落盘无需手动保存）。点击后把当前默认值
  立即保存到绑定位置（角色卡 / 全局设置），并按目标提示结果。
- `js/values-data.js`：新增 `verifyValuesCardWrite`——保存后经宿主接口从磁盘重读
  角色卡（TauriTavern `getOneCharacter` → `/api/characters/get`，标准 SillyTavern
  回退 `getCharacters`），比对扩展字段与期望包是否一致：
  - 一致 → 提示「已保存到角色卡，并已从磁盘校验通过」；
  - 不一致 → 提示「保存未生效（写入被宿主拦截或返回旧数据），请重启酒馆重试」；
  - 宿主不支持重读 → 仅提示已保存（无法校验）。

**测试**
- `tests/values-persist.test.js`：保存校验三用例——写卡成功重读一致返回 true；
  宿主静默吞掉写入重读不一致返回 false；宿主不支持重读返回 null。
- `tests/values-ui.test.js`：保存按钮存在性断言 + 仅默认层显示 + 无角色点击
  保存到全局设置并提示。

### v1.2.1 修复（2026-08-16）— 默认值删除不持久化：恢复角色卡绑定 + 删改立即落盘

**背景**
- 在「变量工作台 → 默认数值」删除节点 / 变量后，刷新网页或退出酒馆重进，
  被删内容（如 龙族2·悼亡者之瞳 的 张三 / 李四）会从磁盘旧数据回滚。
- 根因：写角色卡走 500ms 防抖 `setTimeout`，防抖窗口内刷新 / 退出即丢失写入；
  宿主（TauriTavern）的设置保存同样防抖，兜底路径也在刷新时丢写。

**改动**
- 恢复产品设定：默认数值与**角色卡**绑定（`data.extensions.kaleidoscope_values`，
  随角色卡导入 / 导出携带），游戏数值与**聊天文件**绑定——回滚此前
  「TauriTavern 跳过角色卡、读写改走全局设置 + 聊天文件镜像」的临时方案
  （`isTauriTavernHost` 旁路、`VALUES_CHAT_BUNDLE_KEY` / `mirrorValuesBundleToChat`
  一并移除）。
- 根因修复：写角色卡改为**立即落盘**（`saveValuesCardNow` / `saveStoryCardNow`，
  不做防抖），每次删改直接发起写卡，连续变更按发起顺序落盘、最后一次生效；
  删除防抖常量与定时器 key。兜底路径（群聊 / 未选角色 / 写卡失败）仍回退全局
  设置并立即保存（`saveSettingsImmediate`）。
- `js/values-data.js` / `js/story-data.js` / `js/constants.js` / `js/host.js`。

**测试**
- `tests/values-persist.test.js` 重写为 7 用例：删除立即写卡（不依赖定时器）、
  写卡落盘后刷新 / 重进不回滚（核心回归）、快速连续删除最终态、卡上有数据时
  优先读卡（不回退全局设置）、群聊回退设置并立即保存、写卡失败回退不丢数据、
  剧情脉络同款（新增 / 删除节点立即写卡）。
- `tests/values.test.js` 两个「写入角色卡一次」用例按新语义更新（每次变更一次
  写卡，断言最后一次写入为最终状态）。

### v1.2.0 功能（2026-08-16）— 子变量公式派生 + 链式派生

**背景**
- 子变量此前只有「单派生源 + 区间」一种派生：值 = 同路径一个变量的数值命中的
  区间文本，派生源还被限制为父变量。综合类数值（如 奴隶的「综合评分」由 服从值、
  美貌值 加权算出）没法表达；也无法「综合评分（公式子变量）→ A级（区间子变量）」
  这样以子变量为派生源再派生一层。

**改动**
- `js/values-data.js`：
  - 新增公式派生：子变量存 `formula` 字符串（如 `0.5*服从值+0.5*美貌值`），
    `formula` 非空即公式模式，与区间模式（parent + rules）并存、向后兼容
    （旧数据无迁移）。自写递归下降解析器（`tokenizeValuesFormula` /
    `parseValuesFormulaNode` / `validateValuesFormulaSyntax` / `evalValuesFormula`），
    白名单 token（数字 / `+ - * / ( )` / 变量名，支持中文），不使用 eval；
    优先级 `* /` > `+ -`，支持括号与负号；变量缺失 / 非数值 / 除零 / 结果非有限
    → 求值失败保持原值（与区间派生语义一致）。
  - `deriveValuesChildAt` 增加公式分支：同路径取各引用变量求值，写数值；
    区间分支的派生源从「父变量」放宽为「任意注册变量（父 / 子均可）」，
    链式派生复用 `deriveValuesChildren` 多轮收敛（派生源也是子变量时自动多轮算出）。
  - 新增 `findValuesChildCycle`：按「区间派生源 / 公式引用」建引用图 DFS 找环，
    保存时阻止成环（区间互引、公式环、自引用都会检出并提示环链）。
  - 新增 `getValuesChildKeysByRef`（删除依赖检查同时覆盖公式引用与区间派生源）；
    `getValuesKeys` 归一化补 `formula` 字段；`upsertValuesKey` 与 YAML
    导出（`formula:` 字段）/ 导入同步支持公式模式。
- `js/views-values.js` / `js/constants.js`：
  - 子变量编辑器新增「派生方式」下拉：区间（派生源 + 派生区间）/ 公式（公式
    textarea + 语法提示）；打开编辑器按 `key.formula` 恢复派生方式。
  - 派生源下拉放开为全部已注册变量（父 / 子均可，排除自身）；保存时公式模式
    校验语法 + 环检测，区间模式保留重叠校验 + 环检测。
  - 列表 / 树行派生摘要支持公式（`由公式派生：0.5*服从值+0.5*美貌值`）；
    删除依赖检查改用 `getValuesChildKeysByRef`；文案同步（父变量 → 派生源）。
- `js/values-maintain.js`：维护提示词里的子变量说明按派生方式描述（公式子变量
  注明 `← 公式 <公式>`）；子变量照旧剔除出 AI 值表与补丁。
- 文档：`变量系统指南.md` 第五章重写（派生方式、公式语法、链式示例：
  综合评分 = 0.5*服从值+0.5*美貌值 → 70~150 A级）、YAML 示例补 `formula` 字段；
  `使用指南.md` 一句同步。
- 版本升至 v1.2.0（`constants.js` / `manifest.json`）。

**测试**
- `tests/values.test.js`：公式语法（优先级 / 括号 / 中文变量 / 负号 / 非法输入）、
  公式求值（缺失 / 非数值 / 除零）、公式模式派生（多变量加权、同路径查找）、
  链式派生（公式 → 区间：综合评分 70 → A级）、循环检测（区间互引 / 公式环 /
  自引用 / 无环不误报）、YAML 往返含 formula、删除依赖含公式引用。
- `tests/values-ui.test.js`：派生方式切换与公式保存、非法公式阻止保存并提示、
  循环引用阻止保存并提示环链、派生源可选子变量、列表摘要显示公式；
  既有用例同步新文案（派生源下拉 / 子变量提示文字）。

### v1.2.1 功能（2026-08-16）— 剧情触发事件效果（触发时确定性修改父变量）

**背景**
- 剧情触发此前只能注入事件正文，事件对变量的影响完全交给模型临场发挥；
  想精确控制「触发后好感 +30」「曹操的病 没治好 → 已治好」这类确定性修改没有入口。

**改动**
- `js/values-trigger.js`：
  - `normalizeValuesTrigger` 新增 `effects` 字段（`[{ path, op, value }]`，缺省 `[]`，
    空路径过滤、非法 op 回退 set），旧数据自动兼容；
  - 新增 `applyValuesTriggerEffects`：`add` = 加减值（当前值与效果值都可转数字才
    执行，正加负减）、`set` = 覆盖（数字 / 文本 / 布尔 / null 均可）；效果对象
    只允许父变量（注册为子变量的叶子、树中的节点 / 容器一律跳过）；有改动时
    重算子变量并落盘（与 AI 维护管线同款收尾），返回 `{ changed, skipped }`；
  - 发送前任务在注入前应用效果（确定性修改，不依赖注入能力），结果记入
    `record.effectsApplied`。
- `js/values-data.js`：YAML 导出 / 导入同步 `effects` 段（path / op / value）。
- `js/views-values.js` / `js/constants.js` / `style.css`：
  - 触发编辑器新增「事件效果（可选 · 触发时修改父变量）」区块：动态效果行
    （父变量路径下拉 + 加减 / 覆盖下拉 + 值输入 + 删除），样式与条件行一致；
    路径下拉只列父变量（子变量被过滤）；
  - 保存校验：路径非空但值留空阻止保存（想清除请显式填 null）；加减值要求数字；
  - 列表行显示效果摘要（如「效果：张三/好感 +30；曹操/病 → 已治好」）。
- 版本升至 v1.2.1（`constants.js` / `manifest.json`）。
- 文档：`变量系统指南.md` 第八章补「事件效果」小节与 YAML 示例；README 补
  values-trigger / values-persist 测试命令。

**测试**
- `tests/values-trigger.test.js`：效果归一化、加减 / 覆盖（数字与文本）、子变量与
  节点跳过、文本加减跳过、父变量变更后子变量重新派生、发送前任务落盘、YAML 往返。
- `tests/values-ui.test.js`：效果行填写保存、路径只列父变量、编辑回填、
  值留空阻止保存、显式 null 可保存、列表效果摘要。

### v1.2.2 功能（2026-08-16）— 剧情脉络事件同样支持事件效果

**背景**
- v1.2.1 的「事件效果」只覆盖了剧情触发（变量条件确定性触发）；剧情脉络
  （AI 预筛触发）的事件仍只能注入正文，无法在触发时确定性修改变量。

**改动**
- `js/story-data.js`：
  - 新增 `normalizeStoryScriptEffects`（与剧情触发的 effects 同构：空路径过滤、
    op 缺省 / 非法回退 add）；`createStoryScript` / `updateStoryScript` /
    `mergeStoryBundleInto` 全面支持 `effects` 字段，旧数据自动兼容；
  - 整包导出（scripts 段）与单事件 frontmatter 导出 / 解析同步携带 effects，
    导出再导入不丢效果。
- `js/story-gate.js`：`runStoryGatePipeline` 在 AI 选中事件后、注入前调用
  `applyValuesTriggerEffects`（复用剧情触发的执行函数，行为一致：加减值 /
  覆盖、只改父变量、子变量与节点跳过、有改动时重派生并落盘）；效果不依赖
  注入能力（宿主不支持注入时仍生效）；结果记入 `record.effectsApplied`；
  0 入选时不执行。
- `js/views-story.js` / `js/constants.js` / `style.css`：事件编辑器在「触发条件」
  后新增「事件效果（可选 · 触发时修改父变量）」区块（复用变量系统触发编辑器的
  效果行组件与样式：父变量路径下拉 + 加减 / 覆盖 + 值输入 + 删除）；保存校验
  与剧情触发一致（值留空 / 加减非数字拦截）；列表行显示效果摘要（绿色小字）。
- 版本升至 v1.2.2（`constants.js` / `manifest.json`）。
- 文档：《使用指南.md》剧情脉络编辑说明、《剧情脉络自动生成模式.md》事件字段
  说明补 effects；《变量系统指南.md》「事件效果」小节补剧情脉络同样支持。

**测试**
- `tests/data.test.js`：单事件 frontmatter 与整包 YAML 的 effects 往返（含空路径
  过滤、op 缺省 add）、合并导入解析 effects、旧数据合并兼容。
- `tests/gate.test.js`：AI 选中事件后效果落盘（加减 + 覆盖）、`record.effectsApplied`
  记录、0 入选不执行效果。
- `tests/ui.test.js`：新建事件填效果保存、列表效果摘要、编辑回填、值留空拦截。

## 下一步计划

- 多轮事件去重 / 冷却（同一事件避免连续触发）
- 日志/文档内容变多后，按 SoulLink 惯例滚动归档
