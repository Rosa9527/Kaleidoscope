# 开发日志

> 按日期记录万华镜（Kaleidoscope）的开发进度、决策与下一步计划。
> 与「重要开发经验.md」（踩坑与解决方案）和「重要设计.md」（架构与设计决策）配合阅读。
> v0.1.x 及更早的历史日志已归档至「开发日志-归档.md」。

## 目录

- [2026-08-10](#2026-08-10)
  - v0.7.0 新增 — 跨扩展发送屏障（与 SoulLink 并发协调）
  - v0.6.0 新增 — 注入实录详情视图
  - v0.5.0 新增 — 剧情脉络 · 角色卡整包导出
  - v0.4.0 新增 — 剧情脉络 · 角色卡绑定
  - v0.3.0 新增 — 剧情预筛（Story Gate）
  - v0.2.0 新增 — 剧情脉络可视化工作台
  - v0.2.0 新增 — 系统日志
  - v0.2.0 新增 — 自动化测试
  - v0.2.0 调整 — 文档体系与结构速查
- [下一步计划](#下一步计划)

## 2026-08-10

### v0.7.0 新增（2026-08-10）— 跨扩展发送屏障（与 SoulLink 并发协调）

**背景**
- 万华镜与 SoulLink 都通过 messageSent 阻塞监听器做发送前预筛注入。宿主
  （TauriTavern）的 eventSource.emit 会**按注册顺序逐个 await 监听器**，两个
  扩展各跑各的时，发送前耗时 = 剧情预筛 Gate + 角色推演 Gate（串行，最坏近 90s）。
- 正确性其实一直有保证（宿主等全部监听器 resolve 才发主请求），问题在串行耗时翻倍。

**设计（与 SoulLink 共用的发送屏障协议 v1）**
- 新增 `js/send-barrier.js`（两边逐字节一致）：`globalThis.__preSendInjectionBarrier__`，
  提供 `register(name, task)` 与 `waitAll(ctx, payload, timeoutMs)`。
- 两个扩展的预筛管线改为注册进屏障的发送前任务（所有守卫：开关 / 能力 / 载荷校验 /
  主生成配对 / 运行锁都留在任务内），messageSent 监听器只做「取屏障 → waitAll」，
  屏障不可用时回退为「自己直接阻塞」的原有行为。
- 轮次签名取末条消息 ID（缺失回退文本）：宿主串行 emit 下后一个监听器必然晚于本轮
  完成才被调用，同签名直接复用本轮结果，**绝不重跑**（否则每次发送 Gate 跑两遍、
  重复注入）；新发送产生新消息 → 新签名 → 开新轮。
- 并发执行 + `Promise.allSettled` 失败隔离 + 整轮硬截止兜底：发送前耗时从
  「各 Gate 之和」降为「各 Gate 最大值」，任何一方失败/超时都不阻塞发送。

**改动**
- `js/send-barrier.js`（新增）：屏障协议（register / waitAll / 签名 / 硬截止）。
- `js/story-gate.js`：`runStoryGateBarrierTask`（原监听器全部守卫迁入）+ 薄监听器
  `onStoryGateMessageSent` + 模块加载时注册 `kaleidoscope-story-gate` 任务。
- `js/constants.js`：`MODULE_VERSION` → 0.7.0。
- `tests/barrier.test.js`（新增）：幂等 / 签名 / 并发启动 / 同签名去重 / 级联去重 /
  失败隔离 / 载荷透传 / 与剧情预筛打通 / 回退路径 / 硬截止。
- `tests/gate.test.js`：messageSent 用例改为「每次发送产生新消息（新 id → 新签名）」
  与宿主行为一致。

**验证**
- 全量测试通过：data 37 / ui 38 / proxy 9 / gate 13 / barrier 10。
- 运行期确认：两边日志视图的 fetch 时间戳应显示两个 Gate 请求重叠发出，
  messageSent emit 总时长 ≈ max(两个 Gate) 而非两段相加。
### v0.6.0 新增（2026-08-10）— 注入实录详情视图

**背景**
- 剧情预筛此前只有首页卡片摘要（已注入 N 事件 / 本轮无事件 / 超时放行），
  看不到「触发了什么、注入了什么」；参考 SoulLink「角色扮演」视图补全详情。

**设计（参考 SoulLink「角色扮演」视图）**
- 最近一轮快照除入选 ID 外，新增 `selectedEvents`（入选事件详情：
  id / name / trigger / description / content）与 `injectionText`（注入提示词原文）；
  Gate 原文改为完整保留（此前截断 400 字符）。
- 面板新增「注入实录」视图：摘要（结果 + 耗时 / 候选 / 入选 + 触发名单）→
  预筛原文（Gate 返回）→ 本轮触发的事件（名称 / ID / 触发条件 / 说明 / 正文）→
  注入提示词原文（一键复制）四段展示；无记录时显示空态。
- 首页「注入实录」卡片点击进入详情视图（此前仅 toast 摘要）。

**改动**
- `js/constants.js`：`INJECT_VIEW_ID` 与视图 DOM ID 常量，标题 / 宽视图注册。
- `js/story-gate.js`：快照记录 `selectedEvents` / `injectionText`，Gate 原文完整保留。
- `js/views-inject.js`（新增）：`renderInjectView` / `buildInjectSummary` /
  `buildInjectEventCard` / `copyInjectInjectionText`。
- `js/ui-shell.js`：面板新增注入实录视图 HTML；首页卡片点击进入视图；复制按钮接线。
- `style.css`：注入实录视图样式（摘要 / 事件卡片 / 原文块 / 宽视图模式）。
- `build.js` / `tests/harness.js`：新增 `js/views-inject.js`。
- `tests/gate.test.js`：断言快照含入选事件详情与注入原文。
- `tests/ui.test.js`：注入实录视图渲染与空态用例。

### v0.5.0 新增（2026-08-10）— 剧情脉络 · 角色卡整包导出

**背景**
- 剧情脉络绑定角色卡后，分享时只能整卡携带；需要把「剧情脉络」单独导出成文件，
  文件名直接体现归属角色卡，导入时自动识别。

**设计**
- 导出文件名：绑定角色卡 → `剧情脉络: 角色卡名.yaml`（如 `剧情脉络: 辉夜大小姐.yaml`）；
  群聊 / 未选角色回退 `万华镜-剧情脉络-时间戳.yaml`。
- 整包格式自描述：新增 `format: kaleidoscope-story` 标记行 + `character: 角色卡名`
  （仅绑定角色卡时写入）；解析凭 format 标记或 nodes / scripts 段识别，兼容旧版导出文件。
- 导入识别：确认框显示来源角色卡（「来自『辉夜大小姐』」），识别不依赖文件名。

**改动**
- `js/constants.js`：`STORY_BUNDLE_FORMAT` / `STORY_CARD_BUNDLE_FILENAME_PREFIX` 常量，版本升至 v0.5.0。
- `js/story-data.js`：`serializeStoryBundle` 输出 format / character 头部；新增
  `buildStoryBundleFilename`；`parseStoryBundleFile` 凭标记识别并返回 character。
- `js/views-story.js`：整包导出改用角色卡名文件名；导入确认提示来源角色卡。
- `tests/data.test.js`：新增格式标记 / 角色卡名往返 / 文件名规则用例。
- `tests/ui.test.js`：新增导出文件名、导入识别提示用例。

### v0.4.0 新增（2026-08-10）— 剧情脉络 · 角色卡绑定

**背景**
- 剧情脉络是「角色专属」内容，此前却存在全局设置里：换角色、换电脑、分享角色卡都会丢。
  参考 SillyTavern 局部正则脚本的做法，把剧情数据存进角色卡本身——导入角色卡自动带入，
  导出角色卡自动携带，无需任何额外操作。

**设计（参考 SillyTavern 局部正则脚本）**
- 数据存 `character.data.extensions.kaleidoscope_story`（`{ version, nodes, scripts }`），
  角色卡序列化天然携带该字段，导入 / 导出 / 复制 / 替换角色卡全部自动生效。
- 读写路径：`getStoryNodes` / `getStoryScripts` 优先读当前角色卡；群聊 / 未选角色 /
  卡上无数据时回退全局设置（兼容旧数据）。`saveStoryData` 有角色且宿主提供
  `ctx.writeExtensionField` 时写角色卡，否则写全局设置。
- 首次保存即迁移：角色卡尚无数据时，把当前数据（可能来自旧版全局设置）整体迁入
  角色卡并清空全局兜底，toastr 提示一次。
- 防抖持久化：变更先同步落在内存角色卡对象，再经 500ms 防抖调用
  `writeExtensionField` 写回宿主；定时器挂 `globalThis`，热重载不丢。
- 按 avatar 定位写回：防抖期间切换角色不写错卡；角色已删除 / 写入失败 / 宿主不支持时
  数据回退全局设置，避免丢失。

**改动**
- `js/story-data.js`：新增角色卡绑定层（`getStoryCharacter` / `getStoryCardData` /
  `setStoryCardData` / `scheduleStoryCardSave` / `persistStoryCardData` /
  `fallbackStoryDataToSettings`），`getStoryNodes` / `getStoryScripts` / `saveStoryData`
  改为「角色卡优先、全局兜底」。
- `js/views-story.js`：工作台头部新增绑定徽标 `refreshStoryBindingStatus`
  （已绑定 / 待绑定 / 未绑定角色），随 `renderStoryTree` 刷新。
- `js/views-home.js`：首页剧情脉络卡片状态悬停提示绑定信息，空数据时显示「已绑定 · 尚未添加」。
- `js/constants.js`：`STORY_CARD_*` 常量（扩展键 / 版本 / 防抖时长 / 定时器 key）、`STORY_BINDING_ID`。
- `style.css`：绑定徽标三态样式。
- `tests/harness.js`：`makeContext` 支持 characters / characterId / writeExtensionField，
  新增 `makeCharacter`。
- `tests/data.test.js`：新增 8 个角色卡绑定用例（读取优先 / 迁移清空 / 防抖持久化 /
  切换角色不写错卡 / 角色删除回退 / 写入失败回退 / 宿主不支持降级）。
- `tests/ui.test.js`：新增绑定徽标三态用例。
- 版本升至 v0.4.0（manifest + constants）。

**修正（同日）— 剧情脉络只显示当前角色卡绑定的内容**
- 此前有角色但卡上无数据时会回退显示全局设置，导致「切换到没有绑定文件的角色卡
  却看到别的数据」。现改为：有角色时只读角色卡（无数据即空），只有群聊 / 未选
  角色 / 宿主不支持写角色卡时才回退全局设置。
- 各变更函数（create/update/delete 节点与事件、整包导入）开头先
  `ensureStoryCardData`，保证变更落在角色卡数组上；首次变更仍自动把旧版全局数据
  迁入当前角色卡并清空全局。
- 测试：新增「有角色无卡显示为空」「切换到无绑定角色卡显示为空」用例，
  `data.test.js` 33 个用例全绿。

**后续**
- 群聊场景的剧情数据策略（当前回退全局设置）。
- 角色卡数据过大时的体积提示（剧情正文可能撑大卡片）。

### v0.2.0 新增（2026-08-10）— 剧情脉络可视化工作台

**背景**
- 万华镜的定位是「动态叙事引擎」：作者把剧情、世界观、事件组织成多层级结构，运行时由 AI 按对话状态选择模块注入上下文。第一步先把「内容组织」做成可视化工作台，让作者在酒馆里直接维护节点与事件，再谈运行时注入。

**改动**
- 新增 `js/story-data.js`：数据模型（`storyNodes` / `storyScripts`，存 `ctx.extensionSettings.Kaleidoscope`）、树约束（防环、无效父级回落根节点）、事件 ID 自动递增（001 起、重复自动顺延）、删除语义（子节点上移、事件转未分类）、轻量 YAML 子集解析器、整包 / 单事件导入导出与合并（同 id 更新、悬空引用回落）。
- 新增 `js/views-story.js`：全屏遮罩 + 大窗口树状工作台（移动端自动全屏）；节点可层层嵌套（「第一卷」→「第一章」→ …），事件挂在任意节点下、未分类事件单独成组；「＋」菜单统一新建节点 / 事件；右侧浮层编辑器（节点 / 事件双表单，事件正文必填）；单事件 frontmatter 导入后进编辑器确认；保存以当前可见表单为准 + 会话号防抢占。
- 首页新增「剧情脉络」卡片与状态（N 节点 · M 事件），点击打开工作台。
- `js/constants.js` 新增 `STORY_*` 常量与 `DEFAULT_SETTINGS.storyNodes / storyScripts`。

**后续**
- 事件触发条件（trigger）目前只是文本字段，运行时条件求值待叙事引擎阶段实现。

### v0.2.0 新增（2026-08-10）— 系统日志

**背景**
- 需要一套「看不见但查得到」的运行记录：console、页面错误、Promise 拒绝、宿主事件与 fetch 网络请求统一进内存缓冲，面板内可视化排查；参考 SoulLink 的日志系统实现。

**改动**
- 新增 `js/views-log.js`：`initLogCapture`（console 包装 + window error + unhandledrejection）、`initHostEventLogging`（14 个宿主事件）、`initNetworkCapture`（fetch 旁路观察，响应体 clone 读取）。
- 统一入口 `pushLogEntry`：级别 / 来源 / 消息 / 详情、统一脱敏、噪音过滤、连续重复折叠；缓冲上限默认 2000（可调 100–20000），渲染上限 1000。
- 日志视图 UI：级别 chips / 搜索（120ms 防抖）/ 来源 / 条数 / 暂停（快照语义）/ 跟随 / 清空 / 复制 / 导出 JSON / 噪音开关；日志视图加宽（`is-log-mode`）。
- 热重载安全：缓冲、暂停、捕获管道全部挂在 `globalThis.__kaleido_*__`，扩展热重载不丢日志、不重复包装 console / fetch。
- 对外暴露 `globalThis.KaleidoscopeLog`（debug / info / warn / error / log / clear / getEntries / setMaxEntries）；首页「系统日志」卡片显示错误 / 警告数。
- 新增「📦 完整请求体」导出：对话请求的完整请求体 / 响应体（未截断、已脱敏）保留最近 5 次，一键导出 JSON 排查原始内容（对齐 SoulLink 的完整请求体捕获）。

### v0.2.0 新增（2026-08-10）— 自动化测试

- `tests/harness.js`：共享加载器——按构建顺序把 `js/` 源码读进 vm / jsdom 沙箱（`main.js` 含启动副作用，单独排除），简易断言运行器 + 宿主 mock。
- `tests/data.test.js`：数据层——层级创建 / 防环 / 无效父级回落 / 默认名 / 删除上移与转未分类 / ID 顺延 / YAML 往返 / 合并导入（同 id 更新、悬空引用转未分类、导入成环回落）。
- `tests/ui.test.js`：工作台 UI——打开 / 新建节点与事件 / 编辑 / 导入导出 / 删除确认，jsdom 环境（mock `URL.createObjectURL` / `confirm` / `toastr`）。
- 运行方式：`node tests/data.test.js`、`node tests/ui.test.js`（jsdom 装在 `tests/node_modules`）。

### v0.2.0 调整（2026-08-10）— 文档体系与结构速查

- 补齐与 SoulLink 对齐的文档体系：`开发日志.md`（本文件）、`开发日志-归档.md`、`重要开发经验.md`、`重要设计.md`。
- `插件结构.md` 更新为 v0.2.0 结构速查；README 更新目录结构、宿主差异说明与 Roadmap。

### v0.3.0 新增（2026-08-10）— 剧情预筛（Story Gate）

**背景**
- 万华镜的定位是「动态叙事引擎」：作者把剧情组织成节点与事件，运行时由 AI 按对话状态选择事件注入上下文。v0.2.0 完成了内容组织（剧情脉络工作台），本版落地运行时选择与注入的第一环——剧情预筛。

**设计（参考 SoulLink 角色预筛 Gate）**
- 触发时机：用户点击发送（宿主 `messageSent` 事件，监听器返回 Promise 阻塞主模型请求），预筛完成并注入后才恢复发送。
- Gate 输入刻意保持最小：剧情预筛提示词 + 事件目录 + 最近 4 条消息，不携带事件正文、世界书等任何其他上下文。
- 事件目录 `buildStoryEventCatalog`：节点（层级 + 说明）+ 事件（只含名字 / ID / 触发条件 / 描述，不含正文），未分类事件单独成组；JSON 紧凑序列化省 token。
- 请求体四段式：system 提示词 → `<Story_Events>` 目录块 → `<Recent_Messages>` 剧情块 → 输出契约（`{ "events": [] }`）。
- 解析：`parseStoryGateEventIds` 与现有事件求交集，未知 ID 一律丢弃，最多保留 5 个。
- 注入：`<Story_Event>` 块（强制指令 + 事件 id / 名称 / 触发条件 / 说明 + 正文），经 `setExtensionPrompt(IN_CHAT, depth 0, SYSTEM)` 注入到最后一条用户消息正下方；`generationEnded` / `generationStopped` 后清空。
- 稳定性：运行锁 + 签名去重（同一末条只处理一次）、载荷校验（其他插件 emit 的 messageSent 不误触发）、失败即降级放行、`STORY_GATE_TIMEOUT_MS`（45s）硬截止、宿主不支持 `setExtensionPrompt` 时静默跳过。

**改动**
- 新增 `js/story-gate.js`：目录构建 / Gate 消息 / 解析 / 注入文本 / 管线 / messageSent 阻塞监听 / 生成结束清理。
- `js/constants.js`：`STORY_GATE_*` 常量、`DEFAULT_SETTINGS.storyGateEnabled / storyGatePrompt`、`DEFAULT_STORY_GATE_PROMPT`（剧情预筛默认提示词）。
- `js/main.js`：`installHostEventSubscriptions` 挂载 messageSent 阻塞监听与生成结束清理。
- `js/ui-shell.js`：API 连接页新增「剧情预筛」设置区（启用开关 + 提示词编辑 + 恢复默认）；首页「注入实录」卡片启用。
- `js/views-api.js`：`renderGateControl` / `toggleStoryGate` / `resetStoryGatePrompt`。
- `js/views-home.js`：`refreshHomeInjectStatus`（最近一轮结果：已注入 N 事件 / 本轮无事件 / 超时放行 / 未启用）。
- `tests/gate.test.js`：10 个用例（目录 / 消息 / 解析 / 注入 / 管线 / 监听器）。
- 版本升至 v0.3.0（manifest + constants）。

**后续**
- 事件触发条件（trigger）结构化与运行时求值。
- 注入实录详情视图（当前仅首页卡片摘要）。
- 多轮事件去重 / 冷却（同一事件避免连续触发）。

## 下一步计划

- 事件触发条件（trigger）结构化与运行时求值
- 多轮事件去重 / 冷却（同一事件避免连续触发）
- 日志/文档内容变多后，按 SoulLink 惯例滚动归档
