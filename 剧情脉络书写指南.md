# 剧情脉络书写格式

> 万华镜（Kaleidoscope）是一套专为 SillyTavern / TauriTavern 设计的动态剧情管理与世界构建系统。它支持将世界观、地点、事件及规则等内容拆分为多层级的独立模块；在运行过程中，系统通过**调用 API** 实时分析对话进度、角色行为与环境状态，自动判定触发条件并动态将对应模块注入上下文，实现高度智能化与模块化的动态叙事。
> 万华镜（Kaleidoscope）的「剧情脉络」是一份 YAML 文件，用「节点 + 事件」组织角色的剧情目录与事件正文。
> 运行时，**剧情预筛** AI 会在每轮对话前读这份目录、选出本轮该触发的事件，再把事件正文注入上下文。
> 本文是写作指南：语法只有几条，核心是把目录写得让预筛 AI 选得准。

## 一、核心心法：目录是写给预筛 AI 看的

预筛 AI 每次只拿到两份材料：**事件目录**（节点名、事件名、触发条件 `trigger`、描述——不含事件正文）和**最近几条对话消息**，据此判断触发哪些事件。

所以写作时记住四点：

- **`trigger` 是全文件最重要的字段。** 默认格式是「时间 + 地点 + 人物+ 事件」四要素，缺一不可，这是最严谨的写法。例如「夜晚，学生会，玩家整理文件时，辉夜大小姐主动搭话」。四要素齐全，预筛 AI 不看正文、仅凭最近对话就能判断「现在该不该触发」；不要写「合适的时候」这种空话。
- **措辞必须严谨，避免误触发。** 预筛 AI 只凭最近几句对话做判断，条件写得太宽或含糊，日常闲聊就会误触发。比如「下雨」会在角色随口提到天气时触发，「睡觉」会在角色说「昨晚没睡好」时触发。条件要写到「只有真正发生该场景时才满足」的程度——写清主语、时机与前置状态，而不是只写一个孤立的词。
- **触发条件要有区分度。** 每轮最多注入 5 个事件，条件重叠越少，预筛选得越准。
- **命名要直白。** 目录本身读起来应该像一份清晰的剧情说明书。

## 二、语法：只有这些

文件是 YAML，只有两块：`nodes`（目录骨架）与 `scripts`（事件）。按下面的结构写：

```yaml
format: kaleidoscope-story   # 固定标记，导入时靠它识别
version: 1
character: 辉夜大小姐         # 可选：角色名
nodes:                       # 目录骨架
  - id: node-room            # 节点唯一标识，建议可读、唯一
    name: 房间
    parentId: ""             # 父节点 id；省略或留空 = 根节点
    enabled: true           # 可选：false = 停用整棵子树
scripts:                     # 事件
  - id: "001"                # 事件编号，数字样加引号
    nodeId: node-room        # 挂在哪个节点下
    name: 进入房间
    trigger: 周六夜晚，房间，玩家推门进入时    # 触发条件（最重要）：时间+地点+人物+事件
    description: 可选，一句话说明用途
    content: |-              # 事件正文，从下一行缩进两格开始
      你走进房间，辉夜大小姐正坐在窗边看书，闻声抬起头。
```

要点：

- 缩进用空格，列表项 `- ` 与字段对齐，正文比 `content:` 多缩进两格。
- 数字样 id 加引号（`id: "001"`）；其余文本直接写，不用引号。
- `#` 开头是注释，可随手写备注。
- `trigger` 默认按「时间 + 地点 + 人物 + 事件」四要素写，缺一不可；某个要素不适用时写「任意」占位，保持结构完整。前置状态（如「好感度达到 3 后」）并入时间要素。
- `enabled: false` 表示节点**暂时停用**：该节点连同它的子节点、事件都不会进入预筛目录；省略或 `enabled: true` 为启用。工作台里每个节点行都有滑块，点一下关闭、再点一下激活；导出的 YAML 每个节点都会带上 `enabled: true / false`。

语法就这些。剧情内容、事件数量、节点粗细、组织方式，全部自由发挥。

## 三、目录怎么组织

两种典型思路，可以混用：

- **规则模式**：节点 = 场景 / 状态 / 时段（日常、房间、睡眠、战斗……），事件 = 常驻规则，`trigger` 写触发时机：「白天，日常场景，玩家自由活动时，日常问候」「夜晚，房间，玩家准备睡觉时，睡前叮嘱」「雨天，任意地点，玩家外出时，提醒带伞」。
- **剧情模式**：节点 = 卷 / 章 / 幕（第一卷 → 第一章……），事件 = 剧情节点，`trigger` 写推进条件：「第一章开始后，学生会，玩家推门进入时，雨夜初遇」「完成前置事件后，任意地点，玩家独处时，下一幕剧情」。

判断标准只有一条：**按「时间 + 地点 + 人物 + 事件」四要素写全的 trigger，预筛 AI 拿着和最近对话一对照，就能确定这个事件该不该触发。** 写不出这种 trigger 的事件，说明还不到出场的时候。反过来也一样：如果一句日常闲聊就能满足条件，说明条件太宽，必然误触发。拿不准时，宁可把条件写窄、写死，也不要让它模棱两可。

## 四、完整示例

```yaml
format: kaleidoscope-story
version: 1
character: 辉夜大小姐
nodes:
  - id: vol-1
    name: 第一卷
  - id: chap-1
    parentId: vol-1
    name: 第一章
  - id: node-daily
    name: 日常
  - id: node-room
    parentId: node-daily
    name: 房间
scripts:
  - id: "001"
    nodeId: chap-1
    name: 雨夜初遇
    trigger: 第一章开始后，夜晚，学生会，玩家推门进入时，雨夜初遇
    content: |-
      雨夜，学生会的灯还亮着。
      你推开门，辉夜大小姐正坐在灯下批改文件。
  - id: "002"
    nodeId: node-daily
    name: 日常问候
    trigger: 白天，日常场景，玩家自由活动时，日常问候
    content: |-
      现在是日常时段，辉夜大小姐以平时的姿态回应你。
  - id: "003"
    nodeId: node-room
    name: 睡前叮嘱
    trigger: 夜晚，房间，玩家准备睡觉时，睡前叮嘱
    content: |-
      睡觉前，辉夜大小姐提醒你关灯、检查门窗，早点休息。
```

## 五、写完后自查

- 每个事件的 `trigger`：是否按「时间 + 地点 + 人物 + 事件」四要素写全？缺了哪个要素？
- 每个事件的 `trigger`：不看正文，预筛 AI 能判断吗？
- 有没有条件过宽、会被日常闲聊误触发的 `trigger`？条件里的每个词都经得起推敲吗？
- 有没有多个事件的触发条件大面积重叠，让预筛难以取舍？
- 节点结构是否贴合剧情推进顺序，事件是否挂对了节点？

写好后在「剧情脉络 → 导入 剧情脉络」导入；导入后可在可视化工作台里继续微调。

导入整包时会先弹出**导入方式**选择：

- **合并导入**（推荐）：同 id 条目更新，其余追加，不动已有内容；
- **覆盖导入**：清空当前剧情脉络后，用导入文件整体替换（适合整包换血 / 换角色重开）。

两种方式都会保留导入文件里的 `enabled: false` 停用状态；文件里没写 `enabled` 的节点，导入后一律默认启用。