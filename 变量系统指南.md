# 变量系统指南

> 万华镜（Kaleidoscope）v0.8.0 新增：玩家自定义变量系统。
> 变量统一注册并填写变化规则；变量分「默认值（角色卡）」与「游戏值（聊天）」两层；
> 每轮生成结束后由 AI 自动维护游戏值。
> v0.9.0 新增：默认数值层可勾选节点 / 变量，把当前变量状态注入提示词
> （World Info after 之后），让主模型在生成时直接看到变量。
> v0.10.0 新增：剧情触发——按变量当前值是否满足预设条件，确定性触发剧情事件
> （不依赖 AI 判断，与「剧情脉络」互补）。
> v0.12.0 新增：变量分「父变量 / 子变量」——父变量由 AI 按变化规则维护，
> 子变量按同路径父变量值的区间自动派生（如 好感度 40 → 态度「颇具好感」）；
> 变量树 / 变量注册 / 剧情触发的条目可按住行首把手上下拖动排序。
> v1.0.7 新增：**内置默认注册变量**——「友谊 / 友谊等级 / 情欲 / 情欲等级」任何角色卡 / 聊天
> 都默认注册好，新建变量时下拉直接可选，无需手动注册（可编辑覆盖、可恢复默认）。

---

## 一、概念

| 概念 | 说明 |
| --- | --- |
| 已注册变量 | 在「变量注册」页统一注册的变量名，如「好感」「金钱」。注册时填写**变化规则**，规则是 AI 自动维护的唯一依据。 |
| 内置变量 | 默认已注册的变量：「友谊」「情欲」（父变量）与「友谊等级」「情欲等级」（子变量）任何角色卡 / 聊天都可用，新建变量时下拉直接可选；可编辑规则（= 按当前角色卡覆盖），删除卡内覆盖后恢复内置默认。 |
| 父变量 | 默认类型，由 AI 按「变化规则」维护的变量（如「好感度」）。 |
| 子变量 | 注册时指定父变量与「派生区间」；值由同路径下父变量的当前值自动计算，不参与 AI 维护（如「态度」←「好感度」）。 |
| 节点（Node） | 变量树里的容器，如人名「张三」。节点**不对应值**，只用于嵌套：节点下可继续挂节点或变量。 |
| 变量值（Value） | 挂在节点下（或顶层）的条目，如「好感 = 30」「金钱 = 1000」。新建变量时从已注册变量下拉选择。 |
| 默认值 | 与**角色卡**绑定（`data.extensions.kaleidoscope_values`），随角色卡导入 / 导出自动携带；相当于作者预设的初始变量。 |
| 游戏值 | 与**聊天文件**绑定（`chatMetadata.kaleidoscope_values`），随聊天文件保存 / 加载；是每轮生成后由 AI 维护的实际变量。 |

## 二、快速上手

1. 打开万华镜面板 → 首页点「**变量**」卡片（电脑端打开工作台，手机端进入面板视图）。
2. 「**友谊 / 友谊等级 / 情欲 / 情欲等级**」是**内置默认注册变量**，任何角色卡 / 聊天都直接可用：
   切到左侧导航「**变量注册**」可看到带「内置」徽标的它们；想改规则（如换一套
   等级文案）直接点编辑，保存后按当前角色卡覆盖。
3. 切到左侧导航「**变量树**」→ 「＋ 新建」→「**新建变量**」：
   - 从「已注册变量」下拉选择 `友谊`（内置可直接选），变量值填 `30` → 保存；
   - 再「＋ 新建」→「**新建节点**」：名称填 `张三` → 保存；
   - 在「张三」行点「＋」→「新建变量」：下拉选 `友谊`，变量值填 `30` → 保存。
4. 默认值层（角色卡）编辑完成后，切到「**游戏值 · 聊天**」层：新聊天会从默认值
   开始，首次修改或 AI 维护后写入聊天文件。
5. **条目排序**：变量树、变量注册、剧情触发里的条目都可以按住行首的「⋮⋮」
   把手上下拖动调整顺序；顺序随角色卡保存，导出 / 导入变量包时一并携带。
   （内置变量固定在最前，不可拖动、不可删除。）

## 三、两层变量怎么用

- **默认值 · 角色卡**：作者 / 玩家预设的初始状态，**只能手动修改**（工作台
  「默认值 · 手动」层），AI 自动维护不会改动它。绑定角色卡后，导入角色卡时变量
  自动随卡进入插件，导出角色卡时一并导出；群聊 / 未选角色时保存在全局设置。
- **游戏值 · 聊天**：当前这场聊天的实际变量，**由 AI 自动维护**（工作台
  「游戏值 · AI 维护」层）。每轮生成结束后，AI 依据「已注册变量的变化规则 + 当前游戏值 +
  最新两条消息」自动维护；也可以点「✨ 立即维护」手动跑一轮。游戏值只属于当前
  聊天文件，换聊天 / 换角色互不影响。
- **重置游戏值**：游戏值层点「↺ 重置为默认值」，确认后当前聊天的游戏值整体恢复为
  角色卡默认值（写入聊天文件）。AI 维护跑偏、想重新开始时用它最省事。

## 四、AI 自动维护

- 每轮 SillyTavern 输出结束（`generationEnded`，且确实产出了新的 AI 回复）后自动
  调用一次 API，上下文为：**系统提示词 + 已注册变量的变化规则 + 当前游戏值（仅父变量）+
  最新 2 条消息**。
- 维护原则（默认提示词，可在「预设模版 → 变量维护」编辑 / 恢复默认）：
  - 只依据最新消息更新确有变化的变量，拿不准时保持不变；
  - 严格遵循已注册变量的变化规则（增减方式、上下限、触发条件）；
  - 保持结构与命名：节点不删除、不改名，只更新变量值；
  - 未注册的新变量不会被 AI 擅自创建（新结构由玩家在界面创建）；
  - 内置变量（友谊 / 友谊等级 / 情欲 / 情欲等级）在树里还没出现时，其规则不会发给 AI，
    避免 AI 在从未使用过它们的聊天里凭空创建；玩家在树里新建值后才参与维护。
- 开关：预设模版页「**变量自动维护**」；关闭后不再自动调用，手动维护仍可用。

## 五、父变量与子变量

> v0.12.0 新增：注册变量时可选「父变量 / 子变量」两种类型。

- **父变量**：默认类型，由 AI 按「变化规则」维护（如 `好感度`，规则
  `友好互动 +5，冲突 -10，上限 100`）。
- **子变量**：不参与 AI 维护（维护请求发给 AI 的变量表里不包含子变量，AI 返回
  补丁里的子变量也会被丢弃），值由**同路径下的父变量**按「派生区间」自动计算。
  例如 `张三/态度` 的父变量是 `张三/好感度`：

  | 好感度（父） | 态度（子） |
  | --- | --- |
  | 40 | 颇具好感 |
  | 90 | 生死相依 |

### 注册子变量

1. 「变量注册」→「＋ 注册新变量」→ 变量类型选「**子变量（由父变量自动派生）**」；
2. 选择**父变量**（下拉只列已注册的父变量，不能选子变量）；
3. 添加**派生区间**：每行填「下限 ~ 上限 + 子变量文本」，如 `0~30 冷淡`、
   `31~60 颇具好感`、`61~100 生死相依`；下限 / 上限可留空（留空 = 不设边界），
   按顺序匹配，首个满足的区间生效。区间**不能重叠（含边界）**：如 `0~1000`
   之后只能从 `1001` 开始写 `1001~2000`；新添加的行会自动接续上一行的上限 + 1，
   重叠或下限大于上限的行会标红并阻止保存。

### 派生规则

- 子变量值 = 父变量当前值命中的第一个区间文本；父变量缺失 / 非数值 / 无区间命中时
  保持原值。
- 派生在**读取时自动计算**：变量树、注入提示词、剧情触发、YAML 导出看到的都是
  派生后的值；AI 维护或手动修改父变量后，子变量自动跟随重算。
- 树里的子变量行带「派生」徽标，**不可手动编辑**（值由父变量决定）；新建子变量时
  变量值可留空。
- 删除父变量前需先删除或改绑依赖它的子变量（界面会提示）。

## 六、YAML 导入 / 导出

工作台右上角「导入 变量」/「导出 变量」：

```yaml
format: kaleidoscope-values
character: 角色名
keys:
  - name: 好感
    type: parent
    rule: 友好互动 +5，冲突 -10，上限 100
  - name: 金钱
    type: parent
    rule: 按剧情收支变化
  - name: 态度
    type: child
    parent: 好感
    rules:
      - min: 0
        max: 30
        value: 冷淡
      - min: 31
        max: 60
        value: 颇具好感
      - min: 61
        max: 100
        value: 生死相依
defaults:
  张三:
    好感: 30
  金钱: 1000
```

- 导出文件名：绑定角色卡时 `变量: 角色卡名.yaml`，否则 `万华镜-变量-时间戳.yaml`。
- 导入为**合并**语义：同名变量更新规则，其余追加；默认值按路径合并，同名路径覆盖。
- 子变量导出 `type: child` + `parent` + `rules`（区间列表）；旧包没有 `type` 时
  按父变量处理，导入兼容。
- 变量标量保持类型：数字导入后仍是数字；形如数字的字符串（如 `"1000"`）加引号，
  导入后仍是字符串。

## 七、注入提示词（默认数值层勾选）

- **入口**：变量工作台 →「默认数值」层，树行左侧出现滑块（与剧情脉络节点启停同款），
  顶部有「注入提示词」总开关。
- **勾选语义（节点上下级联动）**：
  - 新建变量**默认开启注入**（自动打开其全部上级节点），无需手动勾选；不想注入时手动关闭即可；
  - 打开**变量** = 只注入该变量，且其全部上级节点**自动打开**（下级打开 → 上级必须打开）；
  - 关闭**节点** = 该节点及其全部下级**级联关闭**（上级关闭 → 下级全部关闭）；
  - 打开**节点** = 只允许该子树注入，**不自动打开**下级（上级打开 → 下级选择性打开）。
- **注入内容**：只注入「自身打开」的变量（容器节点本身不注入内容）；节点滑块居中
  （半选）仅出现在导入等数据不一致场景（后代已打开但祖先未打开），点击半选 = 打开节点。
- **注入内容**：发送时把勾选条目的**当前变量状态**（游戏值，未初始化时回退默认值）以 YAML
  形式注入提示词，位置在 **World Info (after) 之后**（SillyTavern 的 IN_PROMPT 锚点）。
- **注入格式**：`<Values>` 块 + 说明 + YAML 树；生成结束后自动清理，下一轮发送前重新注入最新值。
- **配置存储**：勾选与开关随**角色卡**保存（`data.extensions.kaleidoscope_values` 的 `inject`
  字段），导入 / 导出角色卡自动携带；群聊 / 未选角色时回退全局设置。
- **典型用法**：作者在默认值层勾选「张三 → 好感」「金钱」等关键变量，AI 每轮生成时都能
  直接看到当前数值，剧情与变量保持一致；不想暴露的变量（如隐藏线索）不勾选即可。

## 八、剧情触发（变量条件确定性触发）

> 与「剧情脉络」互补：剧情脉络由预筛 AI 依据对话判断触发；剧情触发**不依赖 API**，
> 直接按「某节点下变量的当前值」是否满足预设条件，确定性判定剧情事件是否触发。
> 两者独立运行，最终都作为剧情事件注入后续流程。

### 快速上手

1. 变量工作台 → 左侧导航「**剧情触发**」→「＋ 新建触发」。
2. 填写**事件名称**与**事件内容**（条件满足后注入的剧情正文）。
3. 添加**变量条件**：选择变量路径（如 `张三 / 好感`）、运算符（如 `>=`）、
   比较值（如 `70`）；可添加多条条件。
4. 选择**条件逻辑**：全部满足（且）/ 任一满足（或）。
5. 选择**事件类型**：一次性事件（默认，触发后自动关闭）/ 常驻事件（可重复触发）。
6. 保存后，每次发送前系统按**当前游戏值**（未初始化时回退默认值）判定：
   条件满足 → 事件以 `<Story_Trigger>` 块注入上下文；不满足 → 不触发。

### 条件语法

- **变量路径**：`节点/变量`，如 `张三/好感`、`世界/战争状态`；顶层变量直接写变量名。
- **运算符**：
  - `==` / `!=`：等于 / 不等于（数字与数字字符串可互比，布尔按布尔比较）；
  - `>` / `>=` / `<` / `<=`：大于 / 大于等于 / 小于 / 小于等于（数字优先，字符串按文本）；
  - `contains`：包含（变量值包含指定文本）；
  - `exists` / `not exists`：变量存在 / 不存在（无需填比较值）。
- **比较值**：数字（`70`）、布尔（`true` / `false`）、文本（`友好`）均可。
- **逻辑组合**：`全部满足（且）` = 所有条件都满足才触发；`任一满足（或）` = 满足任意一条即触发。
- **未定义变量**：路径不存在时，除 `exists` / `not exists` 外一律视为不满足。

### 事件类型

- **一次性事件（默认）**：条件满足并成功注入后，该触发**自动关闭**（列表行变灰、
  开关关闭），之后不再参与判定；适合「告白」「真相揭露」等只发生一次的剧情。
- **常驻事件**：触发后保持启用，条件再次满足时可重复触发；适合「好感度达到某值后
  每轮都有反应」这类持续剧情。
- 一次性事件被自动关闭后，可手动重新打开（列表行开关），或编辑时改回常驻。

### 示例

```text
变量：
张三.好感度 = 80
张三.是否已知真相 = true
世界.战争状态 = false

剧情触发条件（全部满足）：
张三.好感度 >= 70
张三.是否已知真相 == true

→ 满足条件，触发对应剧情事件
```

### 运行时机与注入

- 触发时机：用户点击发送（与剧情预筛并发，经跨扩展发送屏障），**同步判定、无 API 调用**。
- 注入位置：`IN_CHAT`（最后一条用户消息正下方，SYSTEM 角色），与剧情预筛的
  `<Story_Event>` 同款强制指令；`generationEnded` / `generationStopped` 后清空，
  下一轮发送前按最新变量值重新判定。
- 总开关：变量工作台「剧情触发」页顶部滑块，或预设模版页「剧情触发」开关；
  单条触发也可在列表行单独启停。
- 最近一轮结果：首页「注入实录」视图下方展示剧情触发段（摘要 + 触发事件 + 注入原文）。

### YAML 导入 / 导出

变量包新增 `triggers` 段，随角色卡 / 全局设置保存，导入 / 导出自动携带：

```yaml
format: kaleidoscope-values
character: 角色名
keys:
  - name: 好感
    rule: 友好互动 +5，冲突 -10，上限 100
defaults:
  张三:
    好感: 30
triggers:
  - id: "001"
    name: 告白事件
    enabled: true
    once: true          # true = 一次性（触发后自动关闭，默认）；false = 常驻
    logic: all          # all = 全部满足（且）；any = 任一满足（或）
    conditions:
      - path: 张三/好感
        op: ">="
        value: 70
      - path: 张三/是否已知真相
        op: "=="
        value: true
    description: 可选，一句话说明用途
    content: |-
      张三向你表白了。
```

- 导入为**合并**语义：同 id 触发更新，其余追加；覆盖导入时整体替换。
- 触发 ID 默认从 `001` 开始自动递增；导入文件里的 id 会原样保留。

## 九、常见问题

- **游戏值显示「尚未初始化」**：当前聊天还没有独立游戏值，显示的是默认值克隆；
  首次修改或 AI 维护后自动写入聊天文件。
- **AI 维护后变量没变**：本轮消息没有触发任何规则变化，属正常行为（宁可漏更不可乱更）。
- **子变量值为什么不能手动改**：子变量是派生变量，值由同路径父变量按区间自动计算；
  想改显示文本请编辑注册时的派生区间，想改值请改父变量。
- **内置变量（友谊 / 友谊等级 / 情欲 / 情欲等级）怎么改 / 删**：它们是内置默认注册，不可直接删除；
  点编辑修改规则会按当前角色卡保存自定义规则（行变为普通卡键行），再点删除即
  恢复内置默认。不随 YAML 导出，但导入含同名键的变量包会按卡保存（遮蔽内置）。
- **删除已注册变量**：只从注册表移除，已存在的变量不会自动删除。
- **变量名不可修改**：变量名是注册身份；需要改名时删除后重新注册。
- **节点不对应值、变量不能挂子条目**：节点只做嵌套（人名 / 分组），变量是唯一对应值的条目；
  新建变量必须从已注册变量下拉选择，未注册的变量名无法直接创建（内置变量除外，
  它们已默认注册）。
