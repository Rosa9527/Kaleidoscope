# 重要开发经验

> 本文件用于记录万华镜（Kaleidoscope）开发过程中积累的重要经验、踩过的坑和解决方案。
> 每一条按日期追加，保持简洁，只记录真正有价值、以后可能再遇到的问题。

---

## 2026-09-20

### 剧情脉络 / 通用拖动引擎

- **顺序不能「既存数组又按时间戳排」**：剧情脉络原来渲染时 `.sort(byStoryCreatedAt)`，
  数组本身也是创建顺序，所以看不出问题；一旦支持拖动排序，重排数组后渲染又按
  `createdAt` 排回去，表现为「拖完一闪就弹回原位」。**教训：可被玩家排序的列表，
  顺序只能有一个权威来源**——数组顺序即顺序，渲染 / 提示词目录 / 导出全都按它走；
  新建与导入追加在末尾，未排序过的数据观感与排序前完全一致（也就无需迁移）。
- **两套形态（对话框 / 面板视图）共用 id 时，初始化必须用「刚创建的容器」**：
  `bindStoryContentEvents()` 里的 `getElementById` 永远命中先创建的那份 DOM，
  resize 后再创建的另一份拿不到事件绑定（表现为「这份界面怎么拖都不动」）。
  拖动注册因此改成 `initStoryTreeDrag(container)`，由创建方传入自己的树容器。
- **手势引擎抽文件而不是复制**：变量系统与剧情脉络的拖动规则完全同构（长按 650ms /
  容差 8px / 按 `row.parentElement` 算落点 / `--depth` 整块移动），差别只有「同级
  怎么算」与「松手怎么写回」。抽成 `js/row-drag.js` 后，两处各只提供
  `getSiblings` / `onReorder`（外加 `rowSelector` / `matches` / `getBlock` 三个选项），
  规则修正一次即两处受益。**教训：第二处需要同一套复杂交互时，先抽引擎再接入，
  别先复制。**
- **分组键要用「有效归属」而不是原始字段**：事件行原样用 `script.nodeId` 分组时，
  挂到已删除节点上的孤儿事件（`nodeId` 指向不存在的节点）与真正的未分类事件
  （`nodeId === ''`）会被算成两组，而它们在界面上渲染在同一个「未分类事件」分组里
  ——长按拖动找不到同级，怎么拖都不动。分组键取「节点存在时的 id，否则空串」，
  与渲染分组保持一致。

## 2026-09-16

### 变量系统

- **DOM 插入的落点必须是「兄弟节点」，不能是「容器」**：剧情触发拖动"只能拖到
  最后一个"的根因——未分类事件被渲染进 `.kaleido-values__trigger-group` 内层容器，
  而拖动代码按外层 `body` 插入。行不在 body 里，`insertBefore` 抛 `NotFoundError`
  （异常被浏览器吞掉，表现为向上拖毫无反应）；`insertBefore(row, null)` 则直接
  append 到 body 末尾，把行甩出分组。**两条规则**：容器一律取 `row.parentElement`；
  「指针落在所有同级之下」的兜底要插到**最后一个同级块之后**而不是 append 到父节点
  末尾（父节点里还排着别的组）。同类 bug 在虚拟列表、分组表格、树形控件里都会复现。
- **拖动落点被静默吞异常 = 最难查的交互 bug**：`insertBefore` 的 `NotFoundError`
  不进 UI、不进日志，玩家只能描述成"拖不动"。写交互代码时对"插入 / 移除父节点"
  这类会抛错的操作，要么先判定落点合法性（`insertBefore.parentElement === parent`），
  要么把异常显式记进日志——"没反应"和"报错"对排查的价值天差地别。
- **长按手势要和滚动共存**：长按进入拖动的三个必备防线——① 按下后移动超过容差
  （8px）就放弃长按，否则正常滚动会被误判为拖动；② 行内按钮 / 输入框上的长按留给
  原生行为；③ 长按生效后吞掉紧随的那次 `click`，不然"按住拖完"会顺带触发行的点击
  行为。触摸端还多两条：把手必须 `touch-action: none`（`touch-action` 在手指按下
  那一刻就定死，事后再设已经晚了），`contextmenu` 要在**按下待定阶段**就拦
  （Android 约 500ms 弹菜单，比 650ms 长按阈值来得早）。**教训：手势的拦截时机永远
  要早于阈值，否则浏览器原生行为先到。**
- **树形结构拖动要整块走**：分类 / 节点行拖动时，渲染在其后的后代行（深度优先扁平
  排列，靠 `--depth` 识别）必须跟着一起移动，否则拖动会把子分类与父分类拆散——
  DOM 上看不出问题，重渲染后位置关系就错乱了。数据层同理：同级重排函数只认本层
  成员 id，跨层 id 混入时忽略而不是硬塞。
- **同一容器注册多个拖动规则要用行筛选区分**：剧情触发页的事件与分类共用一个
  body，各自的分组规则不同（事件按 `categoryId`、分类按 `parentId`）。两个注册
  若都无差别响应 `pointerdown`，就会抢同一个长按计时器（后注册的覆盖前者）。
  加一个 `matches(row)` 声明各自管哪些行，互不干涉。
- **派生 / 索引类数据「只写不删」会被导出原样带出去**：条目拖动排序的 `order`
  顺序表只在玩家拖动时写入、删除条目时不动它。渲染侧有按当前内容的过滤，界面
  看不出任何异常，但导出走的是原始数据——已删除的「八奈见杏菜」就这样被写回
  YAML，导入别的卡时复活。**教训：凡是「渲染时会过滤」的缓存 / 索引结构，导出前
  必须再过滤一次**，不能指望渲染路径的过滤覆盖文件输出；界面上看不见的东西，
  不代表文件里没有。
- **跨层共用的顺序表不能按单棵树裁剪**：`order` 由默认值层与游戏值层共用
  （路径 → 条目名，两层同构），按其中一棵树清理会把另一层仍存在的条目顺序丢掉。
  过滤函数收「多棵树 + 任一存在即保留」，比在各调用点各写一套安全。
- **导出路径只读、写路径才落盘**：清理函数拆成 `filterValuesTreeOrder`（返回新表，
  不改数据）与 `pruneValuesTreeOrder`（就地改 + 返回是否变化）。导出用前者——导出
  是读操作，不该因为玩家点了「导出」就悄悄改角色卡；删除 / 改名 / 保存用后者。
- **注入类功能要防「绑定到单一事件」**：变量注入原先唯一入口是 `messageSent` 的
  发送屏障任务、生成结束再清空，等于注入存活期只有一轮且依赖宿主发事件——swipe /
  retry / 宿主版本差异都会拿到空块。**常驻模型（数据变就刷新、一直挂着）天然覆盖
  那些不发事件的路径**，且与 biotracker 的 mainflow 提示词行为一致。改成常驻时
  两个关键点：① 数据写入口（本例 `saveValuesData` / `saveValuesChatState`）是
  统一挂钩点，比在每个变更函数里散点调用可靠；② 需要「运行态是否写过」标记才能
  既做到「空内容时清空旧块」又做到「从未写入的聊天零调用宿主 API」；③ 切聊天
  **不能**重置该标记——新聊天没有可注入内容时，「曾经写过」正是清掉旧聊天残留的
  依据（这一点一开始写反了，靠补测试才发现）。
- **有副作用的功能不能只做「数据变了就重算」**：常驻注入模型（数据一变就重算刷新）
  只适用于**纯幂等**的重算（如变量注入：读数据 → 拼文本）。剧情触发的判定本身也是
  纯读，但判定后的动作有副作用——事件效果改值、一次性事件自动关闭——而副作用又会
  反向触发数据变更刷新，于是「数据一变就重判」会**自我破坏**：效果把值改到条件之外 /
  一次性事件被关闭，重判就判定不满足，刚触发的事件从块里消失；副作用还会被重复执行
  （重复计入效果、重复关闭）。**三条规则**：① 判定与副作用必须分离，副作用只在
  「用户点击发送」这个一次性事件里各执行一次，刷新路径只读数据、只写提示词；
  ② 判定完成后**锁定本轮**，此后的刷新按锁定的记录原样重写、不再重判（swipe /
  重新生成据此拿到与本轮一致的结果），新一轮发送 / 切聊天 / 启动才解锁；
  ③ 锁定重写只按「条目是否还存在」过滤，**不要**再检查它当时的启用状态 / 分类激活
  ——本轮刚被自动关闭的一次性事件仍属于本轮，按启用状态过滤会把它自己踢出去。
- **判定结果必须先于副作用发布**：本轮记录（`LAST_ROUND_KEY`）要在应用事件效果 /
  自动关闭**之前**写入 globalThis——副作用会同步经数据写入口触发刷新回调，回调在
  锁定分支读该记录；发布迟一步（例如放在 `finally`）读到的是上一轮，会把刚写的块
  冲掉。**凡「A 的副作用会同步触发 B、B 要读 A 的结果」的链路，结果发布顺序是正确性
  问题，不是风格问题。**
- **「常驻」一词要留给定一个含义**：注入方式叫「常驻注入」后，事件类型再叫「常驻
  事件」（指可重复触发）就会在 UI / 文档里互相指涉、玩家分不清。同一次改造里把事件
  类型改名为「**可重复事件**」，一词一义；改名要连带常量标签、行内徽标、编辑器选项、
  测试断言与全部文档一次改完，否则会出现半新半旧的混称。

## 2026-09-01

### 剧情预筛

- **retry（重新生成）全程不发 messageSent**：宿主 `Generate('regenerate')` 只在开头发
  `GENERATION_STARTED`（第一参数为 `'regenerate'` 字符串，正常发送为 `'normal'`），
  随后删掉末条 AI 楼层再生成——预筛挂在 messageSent 上天然覆盖不了 retry。修复走
  `generationStarted`（type='regenerate'）**同步重放上一轮入选事件**：不重调 Gate
  （同触发点判定结论不变，也避免等待网络）、不重复应用事件效果（加减值会重复计入）。
  两个关键约束：① `onHostEvent` 包装器**不 await 处理器的 promise**（与 messageSent
  专用订阅不同），所以重放必须是纯同步函数、在 `GENERATION_STARTED` emit 内完成
  `setExtensionPrompt`，才能赶在宿主组装主请求 prompt 之前生效；② retry 时宿主先
  删末条 AI 楼层再生成，注入在删除之后、prompt 组装之前完成，不会被
  `messageDeleted` 清掉。群聊 retry 是 `type='normal'` 且事件触发时用户消息尚未
  提交，无法与群聊正常发送区分，不覆盖。
- **测试模拟宿主时序要用 pop 不是 length 截断**：模拟 retry「删掉末条 AI 楼层」时
  写了 `chat.length = 1`——但数组截断是从尾部移除，`[AI, 用户, AI回复]` 截到 1
  只剩 `[AI]`，把用户消息也删了，重放自然找不到触发消息（排查半天以为是实现 bug）。
  模拟「删末条」用 `pop()`，或先想清楚数组截断的语义。

## 2026-08-11

### 剧情预筛

- **发送屏障按「ID 判重」会误伤删楼后重发**：宿主（TauriTavern）删除消息后复用被删
  消息的 ID（楼层序号式），删两层楼后重发的新消息拿到与旧轮相同的 ID，屏障
  `waitAll` 会把它误判为「同一发送已处理」直接返回旧轮结果，剧情预筛整轮被跳过
  （删楼后首条消息不预筛、第二条才恢复）。修复双保险：轮次复用前再比对末条消息
  内容（同 ID 不同内容必是新发送）；`messageDeleted` 时调 `clearSendBarrierRound()`
  清掉旧轮（同 ID 同内容重发也兜得住）。SoulLink 需同步该语义。

## 2026-08-10

### 工程与构建

- **TauriTavern 的 manifest 只接受单个 JS 文件**：`js` 字段必须是字符串或单元素数组，不支持多文件数组——所以源码拆在 `js/` 下、用 `build.js` 按依赖顺序拼成 `index.js`。新增源码文件必须加进 `build.js` 的 `FILES` 数组，且放在依赖它的文件之前。
- **`index.js` 是构建产物，手改会被覆盖**：所有改动进 `js/`，改完跑 `node build.js && node --check index.js`。
- **所有文件共享全局作用域**（非 ES module）：顶层 `const` / `function` 必须全局唯一（`KALEIDO` / `kaleido` 前缀），否则两个文件里的同名顶层声明会互相覆盖。`js/utils.js` 的 `logApp` 引用 `js/views-log.js` 的 `pushLogEntry`，靠构建顺序「运行时已就绪」即可——因为所有调用都发生在 bootstrap 之后，拼接顺序其实很宽松。
- **热重载会重置顶层状态**：扩展脚本重新执行时顶层 `let` 全部重来，所以日志缓冲、暂停状态、序列号、事件订阅、ESC 监听、菜单观察者、看门狗、console / fetch 包装都要挂 `globalThis[key]`；console / fetch 包装**只装一次**（`state.handlers.length === 0` / `!state.original` 判断），重载只替换捕获目标，否则日志会层层嵌套、越重载越卡。

### 宿主适配

- **宿主 context 可能是冻结对象**：直接给 `ctx.extensionSettings` 赋值会抛 TypeError（TauriTavern 踩过），要 try-catch 后回退模块级 `WeakMap` 存储。
- **TauriTavern 的 eventSource 只认 `ctx.event_types` 的值**：裸字符串事件名（如 `'GENERATION_ENDED'`）不触发；标准 SillyTavern 的 `event_types` 值则是 snake_case。统一经 `resolveHostEventType` 从 `ctx.event_types` 解析，取不到再回退原字符串。
- **`appReady` 可能早于扩展脚本执行完**：要加定时兜底重试 bootstrap（60 次 × 500ms），bootstrap 幂等（全局标记）防止重复执行。
- **TauriTavern 主生成后可能重建 `ctx.eventSource`**：bootstrap 时绑到旧事件源的订阅会成为孤儿。加看门狗每 4s 对比「绑定事件源」与「宿主现时事件源」的身份，变了就重挂订阅。
- **菜单双通道要防双条**：宿主 API 注册成功后必须移除手动注入项（按 id 精确 remove）；`hasMenuEntry()` 按 id 与文本双重判断防重复。宿主重建 `#extensionsMenu` 后手动项会丢，用 MutationObserver 自愈重插，并顺手把 `#extensionsMenuButton` 显示出来。

### API 连接

- **宿主代理必须带会话请求头 + CSRF Token**：`POST /api/backends/chat-completions/status` 不带 `ctx.getRequestHeaders()` 里的 session 头（含 X-CSRF-Token）会撞 CSRF 防护返回 403，且会被误判成上游 API 故障。回退直连的白名单要含 401/403/404/405 与「cannot post / not found / no route / ENOENT」文本。
- **TauriTavern 的对话代理接口是 `/api/backends/chat-completions/generate`，不是 `/status`**：`/status` 只返回模型列表，把对话请求发过去会拿到 `{data:[...]}` 模型列表、被误判成「AI 未返回文本内容」。请求体格式也要同步：`custom_include_headers` 的值需带引号（`Authorization: "Bearer xxx"`），参数参考宿主自带 st-chatu8 扩展。代理返回非 JSON、无内容 JSON（choices 为空且 content/reasoning_content 皆空）或 401/403/404/405 时回退直连。
- **messageSent 阻塞监听要防其他插件误触发**：宿主以消息 ID（数字）为 messageSent 载荷（TauriTavern），字符串载荷校验形同虚设——要按载荷类型分别校验：字符串对末条消息文本、数字对末条消息 ID、其他类型一律跳过。再加主生成配对：TauriTavern 的 messageSent 在 generationStarted 之后触发，确认过该顺序的宿主上「无生成在途的 messageSent」判定为插件误触发直接跳过；标准 SillyTavern 顺序相反（messageSent 先于 generationStarted），保持放行不误伤。
- **/models 响应形态差异极大**：兼容 `data.data`（标准 OpenAI）、`data.models`、纯数组，以及包在 `response` 字段里的嵌套响应；模型项可能是字符串或 `{ id }` 对象。模型列表只是探测请求，超时 20s 足够。
- **部分渠道不支持 /models**：下拉拉不到就手动填——UI 永远保留「下拉 + 手动输入」双通道，手动填的模型名与下拉选择互相同步。
- **思考型模型偶发把答案写进 `reasoning_content`、`content` 留空**：解析时要兜底取 `reasoning_content` / `reasoning`，否则会误报「AI 未返回文本内容」；但**兜底必须排除 `finish_reason=length`**——推理模型的 `max_tokens` 同时包含思维链与最终答案，预算被思考吃光时返回「200 + content 空 + 截断的 reasoning」，这半截思考不是答案，取用它只会把「输出被截断」伪装成下游的解析错误（剧情预筛报「无法解析为 JSON：Unexpected token '我'」正是这么来的）。截断一律当「没拿到内容」抛可重试错误，并带出 `finish_reason=length` 与「调大 max_tokens / 改用非推理模型」提示。给推理模型留预算也要按「思维链 + 答案」一起估：Gate 这类只产出 ID 名单的调用写死 1024 每轮必炸，4096 在 28 个事件的目录上仍不够（实测），最终定 12000——预算要按「思考可能有多长」估，不能按「答案有多短」估。
- **失败轮要留下「AI 到底返回了什么」**：预筛这类「解析 AI 输出」的环节，原文快照必须在**解析前**写入，失败原因也要随轮次记录（不能只进 toast——转瞬即逝，面板上只剩「未注入」，玩家反馈的截图里看不到任何可排查信息）。
- **TauriTavern 后端请求失败时返回「错误信封」而非非 2xx**：宿主代理在后端网络请求失败时，会把错误文本包装成 chat.completion 形状的响应（content 以 `[API 错误]` / `[后端错误]` 开头，或直接是 `Network request failed. (...)`），而不是返回非 2xx 或 `{error}` 信封。这类内容不是模型回复，必须在解析后识别并抛「上游 API 返回错误」，否则下游（剧情预筛等）会把错误文本当 AI 内容解析，报出误导性的 JSON 解析错误。
- **Base URL 归一化**：去尾部斜杠，并剥掉用户误填的 `/chat/completions` 或 `/models` 后缀，允许直接粘贴完整接口地址。
- **并发限制要占住整个任务**：`acquireApiConcurrencySlot` 占一个名额再发送，超出排队；整个任务（含自动重试）占同一个名额，任务结束或取消立即释放；排队中的请求取消要立即出队，否则取消后请求还滞留在队列里。

### 日志系统

- **包装 console 前先把原始方法 `bind(console)` 存下来**：直接取 `console.log` 再调用可能 Illegal invocation；自己内部记日志一律走原始方法，避免无限递归（`CONSOLE_ORIGINALS`）。
- **fetch 旁路观察**：包装器 `await state.original(...args)` 后原样返回，行为零改动；响应体用 `response.clone()` 异步读，**绝不读原始 response**（body 只能消费一次）；克隆分支读到上限就 `reader.cancel()`，SSE 长流不会无限缓冲。
- **请求条目要在 await 原始 fetch 之前派发**，否则请求日志和响应日志的时间差会误导排查（漏派 request 事件是真实踩过的坑）。
- **`fetch(new Request(...))` 的 body 是流**：原 Request 的 body 会被 fetch 消费，直接读会抛「body already used」——必须 `input.clone()` 再读。
- **脱敏必须在 pushLogEntry 统一做**：宿主自己的 console debug 会 dump 宏变量全表（`[Prompt Template] all variables: {...}`），里面躺着 `ZapiKey`、`apiKey` 明文——console 捕获等于把密钥带进日志。统一在入口对 message/detail 做正则脱敏（`sk-...`、JSON 字段 `apiKey|ZapiKey|password|proxy_password|authorization|token`、`Bearer xxx`、`tauri-invoke-key`），请求头与请求/响应体同样处理。
- **噪音过滤要看来源 + 级别**：真实环境里 `[WI]` / `[Prompt Template]` 常以 **info** 级别输出，只过滤 debug 会漏网，所以 console 噪音前缀在 debug + info 都过滤；warn/error 和 kaleido / network 来源永不误伤。宿主扩展更新检查的 error（如 `Authenticated Git remote URLs are not supported`——TauriTavern 读扩展目录 git remote 时发现 URL 内嵌认证令牌会拒绝做版本对比）按内容模式单独匹配，不违背「warn/error 不误伤」。
- **暂停要记录快照边界**：暂停时记 `pausedAtId`，暂停期间新日志只入内存（+N）；暂停中重渲染 / 过滤 / 搜索也只显示暂停前快照，恢复后一次性显示——否则「暂停」形同虚设。
- **对话接口的完整响应体要从同一个 clone 读完**：先 clone 读完整、再截断出展示文本；不要在 fetchText 已消费原始响应体后再二次 clone（会抛错，把已读到的内容一起丢掉，有内容的响应被记成「(空)」误导排查）。完整请求体同理：`Request` 的 body 是流，要 `input.clone()` 再读。
- **Headers 遍历用 `new Headers(x).forEach((value, key) => ...)`**：key 会被规范化为小写，断言 / 输出别写死原始大小写；脱敏判断按小写 key 匹配。

### UI 交互

- **面板拖拽会吃掉标题栏按钮的点击**：`setPointerCapture` 会把后续 click 重定向到标题栏，返回 / 关闭按钮失效——pointerdown 落在按钮上（`target.closest('button')`）时不要启动拖拽、不捕获指针。
- **下拉框只认显式预设值**：`select.value = 预设` 前先确认预设 id 存在，否则会沿用上一次会话的旧值导致「新建子节点挂到旧父级」「新事件误归属旧节点」。
- **渲染用户内容必须 escapeHtml**：节点 / 事件名称、说明会进 innerHTML（行内 + title），不转义会被 HTML 注入破坏布局。
- **悬浮球位移阈值 8px**：超过才算拖拽，否则算点击——避免点击误触发拖拽；长按 650ms 隐藏；位置恢复和 resize 都要 clamp 在视口内。

### 剧情脉络（数据层与工作台）

- **事件 ID 的前导零是字符串**：`001` 是用户可读 id，YAML 解析时 `/^0\d+$/` 必须走字符串分支（不转数字），否则导出再导入 `001 → 1` 会悄悄改变身份。事件 ID 自动递增要**排除自身**（编辑时不算自己），重复时按序列顺延到空位。
- **删除节点不删事件**：子节点上移到父级、事件转「未分类」——数据不误删，UI 明确提示「N 个子节点将上移，M 个事件将转为未分类」。
- **导入合并防环要双保险**：导入集内的环用 `leadsBackTo`（沿 parentId 链）检测；与现有树的关系用 `isStoryNodeAncestor` 检测；悬空引用（parentId / nodeId 指向不存在的节点）一律回落为根 / 未分类。
- **异步导入会抢占编辑器**：`readTextFile` 期间用户可能已切换表单——用会话号 `storyEditorSession` 守卫，导入完成后会话变了就放弃接管；保存以「当前可见的表单」为准，而不是内部模式标记。
- **YAML 子集解析要报可读错误**：解析器只支持本项目子集（映射 / 列表 / 标量 / 块文本 / 注释 / `---`），缩进异常、意外列表项等给出**带行号**的报错，否则用户导入失败完全摸不着头脑。
- **文件名清洗**：导出文件名里的 `\ / : * ? " < > |` 与空白要替换掉并截断，否则 Windows 下下载失败。
- **jsdom 测试要 mock 浏览器能力**：`URL.createObjectURL` / `URL.revokeObjectURL` 必须 mock（否则导出测试崩）；`confirm` / `toastr` 注入沙箱收集调用；`main.js` 含启动副作用（定时器、菜单注入）不能加载进测试沙箱，harness 单独排除它。

### 剧情脉络 · 角色卡绑定

- **「和角色卡绑定」= 数据存进角色卡 extensions**：参考 SillyTavern 局部正则脚本——数据放
  `character.data.extensions.<key>`，角色卡序列化（导入 / 导出 / 复制 / 替换）天然携带，
  **不需要挂任何导入导出事件**。宿主提供 `ctx.writeExtensionField(characterId, key, value)`
  （TauriTavern 与标准 SillyTavern 的 getContext 都有），它会同步更新内存角色对象并
  POST `/api/characters/merge-attributes` 持久化。
- **写角色卡要防抖 + 按 avatar 定位**：`writeExtensionField` 是异步 fetch，连续保存要防抖；
  防抖期间用户可能切换角色（`ctx.characterId` 已变），持久化时按 avatar 重新定位索引写回
  原角色，否则会把 A 的剧情写进 B 的卡。角色已删除 / 写入失败 / 宿主不支持时回退全局设置，
  数据不丢。
- **首次保存即迁移**：旧数据在全局设置里，角色卡首次绑定（卡上无 `kaleidoscope_story`）时
  把当前数据整体迁入角色卡并清空全局兜底，toastr 提示一次——避免「每个角色都看到同一份
  旧数据」的困惑。
- **读路径「有角色只读角色卡，无角色才回退全局」**：`getStoryNodes` / `getStoryScripts`
  有角色但卡上无数据时返回空数组——否则「切换到没有绑定文件的角色卡」会串出别的角色 /
  旧全局数据。只有群聊（`characterId` 未定义）/ 未选角色 / 宿主不支持写角色卡时才回退
  全局设置。变更函数（create/update/delete/导入）开头必须先 `ensureStoryCardData`，
  否则变更会落在 `getStoryNodes` 返回的临时空数组上、被迁移逻辑丢掉。
- **测试要可控定时器**：角色卡持久化是防抖的，测试沙箱里 `setTimeout` / `clearTimeout`
  要 mock 成可手动触发的队列（`flushTimers`），否则用例要么等 500ms 要么永远不触发。
*** End Patch