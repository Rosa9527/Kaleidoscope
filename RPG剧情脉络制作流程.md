# RPG剧情脉络制作流程

> 本文是与《RPG剧情脉络制作指南》配套的制作流程文档。
> 指南讲「语法与结构规范」，本文讲「制作顺序与思路」：先主线骨架，再填血肉，最后分叉。
> 语法只是地基，不该困住思路——所有步骤最终按指南的 YAML 格式落地，但制作时先想剧情，再想语法。

## 总览：五步流程

```
第一步  明确时间线      →  只有时间节点 + 当天主线目标（日程表骨架）
第二步  搭好主线        →  时间 → 地点 →（人物）→ 事件 全部挂好
第三步  够得着检查      →  主线每个事件都能触发？
第四步  丰富内容        →  地点池 → 支线剧情 → 再查够得着
第五步  主线 if 线      →  关键选择点的另一种可能
```

## 第一步：明确时间线

- 列出剧本全部时间节点（某一天 / 某时间段），**不写事件**，只搭日程骨架。
- 按时间先后排列：`2026年8月11日`、`2026年8月12日~2026年8月14日`，读起来就是一本日程表。
- 每个时间节点的 `description` 写当天（或该时段）的主线目标，如 `9月7日：与A在悠远咖啡重逢`。节点说明会进入预筛目录，帮预筛 AI 把握当日主线。
- 检查标准：通读整条时间线，主线起承转合是否完整——起因、发展、高潮、收尾都有落点。

```yaml
nodes:
  - id: t-0907
    parentId: plot-events
    name: 2026年9月7日
    description: 与A在悠远咖啡重逢，得知书店的怪事
  - id: t-0914
    parentId: plot-events
    name: 2026年9月14日
    description: 调查废弃仓库，第一次直面「隙间」
```

## 第二步：搭好主线

- 把主线剧情全部挂进骨架，形成 时间 → 地点 →（人物）→ 事件 的完整目录。
- 每个事件 = `trigger`（四要素：时间 + 地点 + 人物 + 触发动作）+ `content`（正文，开头交代日期）。
- 主线事件之间用前置状态衔接（如「完成前置事件后」并入时间要素）。
- 检查标准：主线完整走通——从第一个事件到最后一个事件，玩家顺着走不会断、不会漏。

## 第三步：够得着检查（主线）

判断标准只有一条：**玩家顺着剧情走，这个动作一定会发生吗？**

逐事件核对：

- 四要素写全，且能从最近对话核实？
- 触发动作是玩家自然会做的动作（而不是玩家没理由去做的事）？
- 游戏内「今天」在对话里可核实（正文开头带日期，如 `2026年8月11日，上午。`）？
- 时间段事件的时间要素写「某日至某日期间」，而不是「旅途中」「放假时」这种无法核实的词？

不满足时两种解法：

- **放宽触发条件**：改成玩家每天都会发生的动作（如「玩家进入自己的卧室时」）；
- **加提示事件**：先写一个容易触发的事件给玩家做动作的理由，目标事件再跟着触发。

## 第四步：丰富内容

主线已经完整，这一步开始填血肉。玩家按时间线游玩、在各地点行走触发事件，所以从地点入手。

### 4.1 地点池：让地点成为全局存在

- 汇总剧本里全部可用地点，形成地点清单。
- 让每个地点成为**所有相关时间节点下都可能存在**的地点，而不是某个时间节点的专属——「悠远咖啡」出现在 9月7日，就让它也出现在 9月14日、9月21日。
- 关键规则：节点树是严格的树，跨天不能共用节点。同一地点出现在多天时，**每天各建一个同名节点**，靠时间父节点区分日期；同名不影响预筛。
- 可选增强：加一条「地点总览」规则事件（如玩家询问或进入新地点时注入地点图景），帮预筛 AI 把握地点池。

```yaml
# 同一地点出现在多天 = 多个同名节点，各自挂在不同时间下
  - id: t-0907
    parentId: plot-events
    name: 2026年9月7日
  - id: loc-coffee-0907
    parentId: t-0907
    name: 悠远咖啡
  - id: t-0914
    parentId: plot-events
    name: 2026年9月14日
  - id: loc-coffee-0914
    parentId: t-0914
    name: 悠远咖啡
```

### 4.2 根据地点编支线

- 完成地点扩充后，按地点编纂支线剧情。
- 支线两种定位：
  - **主线侧面展示**：同一地点的另一个视角，补全世界观与人设，回应主线留下的疑问；
  - **纯趣味事件**：让世界更生动，与主线无关也可以。
- 支线的 `trigger` 同样按四要素写；避免与主线事件大面积重叠（条件重叠会让预筛难以取舍）。
- 支线之间也注意不要互相干扰。

### 4.3 再查够得着

- 支线会不会被主线对话误触发？主线会不会被支线误触发？
- 支线之间条件是否重叠？
- 每条支线都满足：玩家顺着剧情走，这个动作一定会发生。

## 第五步：主线 if 线

主线已完整、内容已丰富，最后补分叉——**关键选择点的另一种可能**。

例子：原主线是「玩家拒绝了A的表白」，这一步要问：如果玩家接受了A的表白，会发生什么？

做法：

- if 线事件挂在对应的时间/地点/人物节点下（如原主线事件的同节点，或新加 `书房（表白后）` 这类阶段节点）。
- 触发条件写成**选择后的可核实状态**：`2026年9月14日，书房，A，玩家接受A的表白后`——「接受」是对话里能核实的事实，可以写进 `trigger`。
- 相反，「玩家拒绝A的表白时」这类**结果**不能写进 `trigger`：拒绝是原选择场景发生后的剧情，预筛 AI 无法核实，应放回原事件的 `content`。
- 分工明确：原事件负责「选择场景」本身，if 事件负责「选择后的发展」。两者用前置状态区分，检查分支之间不重叠。
- 每个关键选择点都问一遍：不接受这个走向，玩家还可能选什么？选了会怎样？

```yaml
scripts:
  - id: "301"
    nodeId: char-a
    name: A的表白
    trigger: 2026年9月14日，书房，A，A向玩家表白时
    content: |-
      2026年9月14日，傍晚。
      书房里只有你们两个人。A深吸一口气，说出了那句话。
  - id: "302"
    nodeId: char-a
    name: if-接受表白之后
    trigger: 2026年9月14日，书房，A，玩家接受A的表白后
    content: |-
      2026年9月14日，夜晚。
      你答应了A。A愣了一瞬，然后笑起来，握紧了你的手——但这之后，你们之间的关系、后续剧情的走向都变了。
  - id: "303"
    nodeId: char-a
    name: if-拒绝表白之后
    trigger: 2026年9月14日，书房，A，玩家拒绝A的表白后
    content: |-
      2026年9月14日，夜晚。
      你婉拒了A。A勉强扯出一个笑容，说「我早该猜到」——气氛安静下来，但至少，你们还坐在一起。
```

检查标准：每条 if 线都能独立走通、后续事件能接得上，且不与主线其他事件冲突。

## 贯穿原则

- **语法是地基，不是目的**：先想剧情，再按指南落地 YAML；语法问题（缩进、id 引号）留在最后统一自查。
- **trigger 是唯一触发依据**：任何事件（主线、支线、if 线、规则事件）都按四要素写，判断标准都是「预筛 AI 拿 trigger 和最近对话一对照，就能确定该不该触发」。
- **规则事件随手补**：地点总览、状态变化、分支进度等，可以在第四、五步进行时顺手补充，不必最后再回头整理。
- **够得着是终检**：第三、四、五步各做完一遍内容，都回到同一条标准——玩家顺着剧情走，这个动作一定会发生吗？
