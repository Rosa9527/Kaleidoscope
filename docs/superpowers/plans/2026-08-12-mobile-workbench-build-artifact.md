# 移动端剧情脉络工作台构建产物修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 SillyTavern 手机端加载包含 v0.7.5 移动端适配的 `index.js`，并用测试防止构建产物版本落后于源码。

**Architecture:** `js/` 与 `manifest.json` 继续作为版本来源；`index.js` 由 `build.js` 生成。新增独立的 Node 一致性测试，只读取三个版本来源并在构建产物落后时失败，不改变工作台业务逻辑或样式。

**Tech Stack:** Node.js 内置 `fs` / `path`、仓库现有 `build.js`、Node 语法检查、现有 jsdom 自动化测试、浏览器手机视口预览。

---

### Task 1: 添加构建产物版本一致性回归测试

**Files:**
- Create: `tests/build.test.js`

- [ ] **Step 1: Write the failing test**

创建一个不依赖第三方库的 Node 测试，读取清单、源码常量和构建产物，并在构建产物版本落后时抛出明确错误：

```js
'use strict';

const fs = require('fs');
const path = require('path');

const root = path.resolve(__dirname, '..');
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8');
const fail = (message) => { throw new Error(message); };

const manifest = JSON.parse(read('manifest.json'));
const source = read('js/constants.js');
const bundle = read('index.js');
const sourceVersion = source.match(/const MODULE_VERSION = ['"]([^'"]+)['"]/);
const bundleVersion = bundle.match(/const MODULE_VERSION = ['"]([^'"]+)['"]/);

if (!sourceVersion) fail('js/constants.js 缺少 MODULE_VERSION');
if (!bundleVersion) fail('index.js 缺少 MODULE_VERSION');
if (manifest.version !== sourceVersion[1]) {
  fail(`manifest.json 与 js/constants.js 版本不一致：${manifest.version} !== ${sourceVersion[1]}`);
}
if (bundleVersion[1] !== sourceVersion[1]) {
  fail(`index.js 与 js/constants.js 版本不一致：${bundleVersion[1]} !== ${sourceVersion[1]}`);
}

console.log(`构建产物一致性通过：${sourceVersion[1]}`);
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```powershell
node tests/build.test.js
```

Expected before rebuilding:

```text
Error: index.js 与 js/constants.js 版本不一致：0.7.4 !== 0.7.5
```

这个失败必须来自旧 `index.js` 的版本差异，而不是路径或语法错误。

### Task 2: 重新生成宿主实际加载的构建产物

**Files:**
- Modify: `index.js` (generated by `build.js`)

- [ ] **Step 1: Generate the bundle from source files**

Run:

```powershell
node build.js
```

Expected output includes:

```text
[build] 已生成 index.js
```

不要直接编辑 `index.js`；其内容必须来自现有构建脚本。

- [ ] **Step 2: Check generated JavaScript syntax**

Run:

```powershell
node --check index.js
```

Expected: exit code 0 and no syntax error output.

### Task 3: Run the regression test green

**Files:**
- Test: `tests/build.test.js`

- [ ] **Step 1: Run the consistency test**

Run:

```powershell
node tests/build.test.js
```

Expected:

```text
构建产物一致性通过：0.7.5
```

### Task 4: Run existing automated coverage

**Files:**
- Test: `tests/data.test.js`
- Test: `tests/ui.test.js`
- Test: `tests/proxy.test.js`
- Test: `tests/gate.test.js`
- Test: `tests/barrier.test.js`

- [ ] **Step 1: Run all existing test files**

Run:

```powershell
node tests/data.test.js
node tests/ui.test.js
node tests/proxy.test.js
node tests/gate.test.js
node tests/barrier.test.js
```

Expected: every test file exits with code 0 and reports all cases passed, with no new errors or warnings.

- [ ] **Step 2: Recheck version and working-tree scope**

Run:

```powershell
rg -n "MODULE_VERSION|0\.7\.[45]" js/constants.js index.js manifest.json
git diff --check
git status --short
```

Expected: all three version sources show `0.7.5`; only the new test and generated `index.js` are task changes, while the pre-existing untracked RPG guide remains untouched.

### Task 5: Verify the mobile presentation

**Files:**
- Verify: `tests/_mobile_preview_story.html`
- Verify: `style.css`

- [ ] **Step 1: Open the existing story preview at 346×778**

Load `tests/_mobile_preview_story.html` in the local browser preview with a 346×778 viewport and inspect the rendered workbench.

Expected: the workbench fills the mobile viewport; deep node names remain visible; row action buttons stay inside the viewport; no horizontal scrollbar appears; the visible version is v0.7.5 in the actual extension bundle.

- [ ] **Step 2: Commit the implementation**

Run:

```powershell
git add -- tests/build.test.js index.js
git commit -m "fix: rebuild mobile workbench bundle"
```

Do not stage or commit the unrelated untracked RPG guide.
