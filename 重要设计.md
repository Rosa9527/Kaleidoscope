# 重要设计

> 记录万华镜（Kaleidoscope）的架构与关键设计决策，方便后续迭代时保持一致。
> 与「开发日志.md」（进度）和「重要开发经验.md」（踩坑）配合阅读。

---

## 1. 总体结构

当前为「多文件源码 + 单文件构建产物」结构（`js/` → `index.js`），职责划分：

- `js/constants.js`：所有常量（DOM ID / 默认设置 / 超时 / 尺寸 / 噪音名单）。
- `js/utils.js`：设置读写、API 请求、chatCompletion、并发限制、JSON 解析。
- `js/host.js`：宿主适配（`getContextSafe` / 事件订阅）。
- `js/ui-shell.js`：悬浮球、面板、视图切换、ESC 处理。
- `js/views-api.js`：API 连接视图 UI 与交互。
- `js/views-log.js`：系统日志（捕获 / 存储 / 视图 UI / 对外 API）。
- `js/views-home.js`：首页状态刷新。
- `js/story-data.js`：剧情脉络数据模型 / YAML 导入导出 / 合并。
- `js/story-gate.js`：剧情预筛（Gate 预筛 + 事件注入，messageSent 阻塞监听）。
- `js/views-story.js`：剧情脉络可视化工作台 UI。
- `js/main.js`：入口（bootstrap / 菜单注册 / 事件看门狗）。
- `build.js` / `build.bat`：拼接构建。
- `tests/`：开发测试（data / ui，jsdom，不参与构建）。

**为什么多文件 + 构建**：TauriTavern 的 manifest 只接受单个 JS 文件（`js` 必须是字符串或单元素数组），不支持多文件数组。源码拆在 `js/` 下便于维护，`node build.js` 按依赖顺序拼回一个 `index.js` 供宿主加载。

## 2. 命名约定

| 类别 | 规则 | 示例 |
| --- | --- | --- |
| DOM id | `kaleido-` 前缀 | `kaleido-panel`、`kaleido-floating-sphere`、`kaleido-story-dialog` |
| CSS class | `kaleido-` 前缀，BEM 风格 | `kaleido-panel__dialog`、`kaleido-sphere.is-dragging`、`kaleido-log__row--error` |
| localStorage key | `Kaleidoscope_` 前缀 | `Kaleidoscope_floating_sphere_position` |
| 设置命名空间 | `ctx.extensionSettings.Kaleidoscope` | `{ apiUrl, apiKey, model, modelOptions, ... }` |
| globalThis 运行时 key | `__kaleido_` 前缀 | `__kaleido_bootstrapped__`、`__kaleido_log_state__` |
| 菜单项 id | `kaleido-menu-` 前缀 | `kaleido-menu-item`（手动）、`kaleido-menu-api`（host） |
| 模块级全局常量 | `KALEIDO` / `kaleido` 前缀 | 全局唯一，禁止与宿主或其他扩展冲突 |

## 3. 构建与热重载

- `build.js` 的 `FILES` 数组决定拼接顺序：常量 → 工具 → 宿主 → UI 外壳 → 各视图 → 入口。所有文件都是普通脚本（非 ES module），顶层 `const` / `function` 进入全局作用域，命名必须全局唯一。
- 新增源码文件必须加进 `FILES`，且放在依赖它的文件之前。
- `index.js` 是构建产物，**手改会被覆盖**；改完跑 `node build.js && node --check index.js`。
- **热重载安全**：脚本重新执行时顶层 `let` 会重置，所有需要跨重载保持的全局副作用（事件订阅、ESC 监听、菜单观察者、看门狗、日志缓冲与捕获管道）一律挂 `globalThis[key]` 去重；console / fetch 包装只装一次，重载只替换捕获目标。

## 4. 启动流程（bootstrap）

1. 订阅 `appReady` 事件；同时启动定时兜底重试（最多 60 次 × 500ms），应对 TauriTavern 的 `appReady` 早于扩展脚本注册的情况。
2. `bootstrap` 幂等：`__kaleido_bootstrapped__` 全局标记防止重复执行；失败时复位标记允许重试。
3. 顺序：初始化日志捕获 → 装事件订阅 → 看门狗 → 面板 → 悬浮球 → 菜单注册。
4. 宿主上下文统一走 `Luker.getContext()` / `SillyTavern.getContext()`，不用全局 `getContext()`。

## 5. 菜单入口：双通道 + 自愈

- **主通道**：`ST_API.ui.registerExtensionsMenuItem`（TauriTavern 支持），注册前先 await `__TAURITAVERN__.ready` / `__TAURITAVERN_MAIN_READY__`。
- **兜底通道**：手动往 `#extensionsMenu` 注入 `list-group-item` 结构（与宿主原生菜单项一致），支持点击与键盘（Enter / 空格）。
- **去重**：`hasMenuEntry()` 按 id（手动 / API）与文本双重判断；host 注册成功后移除手动项，避免菜单出现两条。
- **自愈**：MutationObserver 监视 `#extensionsMenu` 被重建后重新注入，并确保 `#extensionsMenuButton` 可见。

## 6. 悬浮球交互模型

- 六棱万花筒视觉：clip-path 六边形 + 旋转 conic-gradient 镜眼 + 轴心（`fa-fan` 扇面图标呼应意象）。
- 固定定位（`z-index: 10000`），44px；Pointer Events 拖拽，**位移阈值 8px**：超过才算拖拽，否则视为点击。
- 长按 650ms 隐藏悬浮球；点击 = 隐藏悬浮球 + 打开面板。
- 位置持久化：localStorage 保存，恢复与 `resize` 时都 clamp 在视口内（`EDGE_GAP=24`）。
- **入口闭环**：悬浮球隐藏后仍可通过菜单打开面板；关闭面板时悬浮球以 grow 动画重新出现——永远有入口找回面板。

## 7. 面板与视图

- 固定定位卡片（`z-index: 9999`），默认右上角，标题栏拖拽（位置仅会话内记忆，dataset）；关闭方式：关闭按钮、ESC。
- 视图切换 `showPanelView(viewId)`：`.kaleido-view` 激活态 + 标题 + 返回键；打开面板默认回首页。
- 宽视图机制 `PANEL_WIDE_MODES`：日志视图需要更宽的窗口，进入视图时加 `is-log-mode` 类（后续加宽视图只需在表里登记）。
- 剧情脉络工作台是**独立的全屏遮罩 + 大窗口对话框**（不占面板空间），移动端（max-width 640px）自动全屏；ESC 优先级：工作台打开时让位给工作台，否则先返回首页再关闭面板。
- 样式全部以 `--k-*` CSS 变量组织（宣纸 / 墨色 / 朱砂 / 鎏金古风主题），后续加主题只需变量覆盖（如 `[data-theme='...']`）。

## 8. 设置持久化

- 设置存宿主 `ctx.extensionSettings.Kaleidoscope`，随宿主存档 / 设置一起保存。
- `getSettings` 负责默认值合并（`DEFAULT_SETTINGS`）：缺字段自动补齐并触发保存；`modelOptions` 额外校验数组类型。
- **降级兜底**：宿主 context 冻结 / 不可扩展时自动回退模块级 `WeakMap`（`FALLBACK_SETTINGS_STORE`），保证读写不中断。
- 保存分两种：输入即时保存用 `saveSettings`（debounced），关键操作（如拉取模型后）用 `saveSettingsImmediate`。

## 9. API 连接设计

- Base URL 归一化 `getApiBase`：去尾部斜杠、剥离 `/chat/completions` 与 `/models` 后缀，允许用户粘贴完整接口地址。
- 跨域判定 `isCrossOriginUrl` 对比 `location.origin`；跨域才走宿主代理（`POST /api/backends/chat-completions/status`），代理失败按白名单回退直连。
- 代理请求必须带会话请求头 + CSRF Token（`getHostProxyHeaders`），否则 403 会被误判成上游故障。
- `/models` 响应形态兼容：`data.data` / `data.models` / 纯数组 / 嵌套 `response` 字段；模型项可能是字符串或 `{ id }` 对象。
- 模型列表只是探测请求，超时 20s；UI 保留「下拉 + 手动输入」双通道——部分渠道不支持 /models，拉不到也能手动填了直接用。
- 思考强度：`reasoning_effort`（Ollama 与 OpenAI 兼容端点都认），选项为默认（不发送）/ none / low / medium / high / max。

## 10. chatCompletion 稳定性设计

- 超时（AbortController，120s）、自动重试（仅 429 / 5xx 等可重试错误，最多 5 次、退避 1.2s × 次数）、可取消（AbortSignal 贯穿到网络层）。
- **并发限制**：`acquireApiConcurrencySlot` 占住一个名额再发送，超出排队；整个任务（含自动重试）占用同一个名额，任务结束 / 取消立即释放；取消的排队请求立即出队，不留死队列。
- 响应解析兜底：思考型模型偶发把答案写进 `reasoning_content`、`content` 留空 → 兜底取用；`finish_reason=length` 提示「调大 max_tokens 或改用非推理模型」；嵌套 `response` 字段自动解包。
- `parseAgentJson`：兼容 ```json 代码块，自动截取首尾花括号。

## 11. 宿主适配层

- `getContextSafe()`：`Luker.getContext` → `SillyTavern.getContext` 二选一。
- 事件名差异：TauriTavern 的 eventSource 需要 `ctx.event_types` 的值（如 `ctx.event_types.GENERATION_ENDED`），裸字符串名不触发；`resolveHostEventType` 统一解析，取不到回退原字符串。
- `onHostEvent` 以全局 key 去重，重挂时先 `removeListener`，避免重复触发。
- **事件源自愈看门狗**：TauriTavern 主生成后可能重建 `ctx.eventSource`，bootstrap 时绑定到旧事件源的订阅会成为孤儿；每 4s 对比绑定事件源与宿主现时事件源的身份，被换就重挂订阅。

## 12. 剧情脉络：数据模型

- 存储：`settings.storyNodes`（`{ id, parentId, name, description, createdAt, updatedAt }`）+ `settings.storyScripts`（`{ id, nodeId, name, trigger, description, content, createdAt, updatedAt }`），随设置持久化。
- 节点 id：`genStoryId('n')`（时间戳 + 随机段）生成稳定身份；事件 id：用户可读的 001 序列，默认自动递增、重复时自动顺延（`resolveStoryScriptId` 排除自身后找空位）。
- 树约束：`parentId` 空 = 根；不能挂到自己或自己的后代下（`isStoryNodeAncestor` 沿 parentId 链向上查，带 1000 步守卫）；无效父级自动回落为根。
- 删除语义：删节点时**子节点上移到其父级**（顶层则成根）、**事件转「未分类」**（不删数据），返回 `{ movedChildren, detachedScripts }` 供 UI 明确提示。

## 13. 剧情脉络：YAML 格式与导入合并

- 整包格式：`format: kaleidoscope-story`（自描述标记，导入识别依据）+ `version / character`（绑定角色卡时写入角色名）+ `nodes / scripts`（nodes 含 `id / parentId / name / description`；scripts 内含 `id / nodeId / name / trigger / description / content`）；解析时凭 format 标记或 nodes / scripts 段识别，兼容旧版导出。
- 单事件格式：frontmatter（`--- name / id / Trigger / description ---`）+ 正文，兼容 SillyTavern 事件 / 技能式文件；导入后进编辑器确认（默认未分类）。
- `parseYamlSubset` 是**本项目所需子集**的轻量解析器：映射 / 列表 / 标量（含引号）/ 块文本（`|`、`|-`）/ 注释 / `---` 标记；高级特性（锚点、多文档、流式内嵌）不支持，导入时给出带行号的可读报错。
- 导入合并 `mergeStoryBundleInto`：**同 id 更新、其余追加**；引用不存在节点的脚本自动转「未分类」；导入集内成环检测（`leadsBackTo`）+ 现有树防环双保险。
- 导出：整包绑定角色卡时 `剧情脉络: 角色卡名.yaml`（群聊 / 未选角色回退 `万华镜-剧情脉络-时间戳.yaml`）；单事件 `万华镜-事件-名称.yaml`（文件名经 `sanitizeStoryFilename` 清洗）。

## 14. 剧情脉络：工作台 UI

- 树状渲染 `renderStoryTree`：节点行可展开 / 收起，事件挂在节点下，未分类事件单独成组显示。
- 「＋」菜单：顶部工具栏与节点行加号统一弹二选一菜单（新建节点 / 新建事件），点击菜单外部自动收起。
- 编辑器右侧浮层（移动端全幅覆盖）：节点（名称 / 上级下拉排除自身与后代 / 说明）、事件（ID / 名称 / 触发条件 / 说明 / 所属节点 / 正文必填）。
- **保存以「当前可见的表单」为准**（而非内部模式标记），避免异步导入等场景「界面是节点表单、保存却按事件校验」的错位。
- **会话号防抢占**：异步导入完成后若编辑器会话已变（`storyEditorSession`）则放弃接管，不顶掉正在编辑的表单。
- 上级 / 所属节点下拉只认显式预设值，避免沿用上一次会话的旧值导致误挂父级 / 误归属。
- 渲染用户内容一律 `escapeHtml`（名称、说明进 title / 行内）。

## 15. 系统日志设计

- 捕获来源：console（log / info / warn / error / debug）、window error、unhandledrejection、宿主事件（14 个）、fetch 网络请求。
- **热重载共享状态**：缓冲 / 暂停 / 序列号挂在 `globalThis.__kaleido_log_state__`；console 包装与 fetch 包装只装一次（`__kaleido_log_capture__` / `__kaleido_network_capture__`），热重载只替换捕获目标——日志不中断、不重复包装。
- 统一入口 `pushLogEntry(level, source, args, detail)`：脱敏 → 噪音过滤 → 连续重复折叠（同级别 / 来源 / 内容只更新计数与时间）→ 入缓冲 → 追加渲染。
- 暂停语义：记录暂停时刻边界 `pausedAtId`，暂停期间新日志只入内存（+N），恢复后一次性显示；暂停中重渲染也只显示暂停前快照。
- 渲染上限 `LOG_RENDER_CAP=1000`（缓冲上限默认 2000，可调 100–20000）；搜索 120ms 防抖；跟随 = 钉在底部。
- 噪音治理：console 噪音按前缀（debug + info），network 噪音按 URL 模式（debug），宿主扩展更新检查的已知 error 按内容模式单独匹配——warn/error 永不误伤；噪音过滤可开关（设置 + UI 按钮）。
- 脱敏在 `pushLogEntry` 统一做：console 宏变量 dump 会带出 `ZapiKey` / `apiKey` 明文密钥，请求头 / 请求体 / 响应体同样处理。
- 对外 API `globalThis.KaleidoscopeLog`：debug / info / warn / error / log / clear / getEntries / setMaxEntries，热重载后整体重建、指向当前实例缓冲。
- **完整请求体/响应体捕获**：对话接口（chat-completions）的请求体与响应体额外完整保留最近 `LOG_FULL_BODY_MAX=5` 次（未截断、已脱敏），日志视图「📦 完整请求体」按钮一键导出 JSON（`kaleidoscope-fullbody-时间戳.json`），供排查截断展示看不到的原始内容。

## 16. 首页状态

- 四张卡片：API 连接（模型 + 地址）、剧情脉络（N 节点 · M 事件）、系统日志（错误 / 警告 / 记录数）、注入实录（剧情预筛最近一轮结果）；统一 `refreshHomeStatuses()` 入口，新增卡片状态在此挂接。
- 「注入实录」卡片展示最近一轮剧情预筛结果（已注入 N 事件 / 本轮无事件 / 超时放行 / 未启用 / 尚未运行），点击卡片进入注入实录详情视图。

## 17. 测试策略

- `tests/harness.js`：共享加载器——按构建顺序把 `js/` 源码读进 vm / jsdom 沙箱（`main.js` 含启动副作用单独排除），简易测试运行器 + 宿主 mock（可扩展 extensionSettings）。
- `tests/data.test.js`：数据层——层级创建 / 防环 / 无效父级回落 / 删除上移与转未分类 / ID 顺延 / YAML 往返 / 合并导入。
- `tests/ui.test.js`：工作台 UI——打开 / 建节点 / 建事件 / 导入导出 / 删除确认，jsdom 环境（mock URL.createObjectURL / confirm / toastr）。
- 测试直接读 `js/` 源码（不走构建产物）；新增源码文件记得同步 `harness.js` 的 `SOURCE_FILES`。
- `tests/gate.test.js`：剧情预筛——目录构建（不含正文）/ Gate 消息四段式 / ID 解析交集 / 注入文本 / 管线成功与降级 / messageSent 监听器（mock `chatCompletion` 与 `setExtensionPrompt`）。
- `tests/barrier.test.js`：发送屏障——幂等 / 签名计算 / 并发启动 / 同签名去重 / 级联去重 / 失败隔离 / 载荷透传 / 与剧情预筛打通 / 屏障缺失回退 / 硬截止。

## 18. 剧情预筛（Story Gate）

- **触发时机**：用户点击发送（宿主 `messageSent` 事件；宿主的 emit 会 await 监听器），监听器返回 Promise，从而在「预筛完成并注入」之前阻塞主模型请求——与 SoulLink 角色预筛 Gate 同构。
- **并发协调（v0.7.0 发送屏障）**：预筛管线注册进跨扩展发送屏障 `js/send-barrier.js`
  （与 SoulLink 共用协议，两边实现逐字节一致），messageSent 监听器只做「取屏障 → `waitAll`」，
  屏障不可用时回退为直接阻塞。宿主串行 await 监听器，后一个扩展的监听器必然晚于本轮
  完成才被调用——屏障按「末条消息 ID（缺失回退文本）」签名复用本轮结果，绝不重跑；
  两个扩展的 Gate 并发执行，发送前耗时从串行之和降为 `max(两个 Gate)`，
  任何一方失败 / 超时都不阻塞发送。
- **Gate 输入刻意保持最小**：剧情预筛提示词 + 事件目录 + 最近 4 条消息。不携带事件正文、世界书、完整聊天记录等任何其他上下文；Gate 只负责缩小候选集（产出事件 ID），不产出内容。
- **事件目录**：节点（层级 + 说明）+ 事件（只含名字 / ID / 触发条件 / 描述，不含正文），未分类事件单独成组；JSON 紧凑序列化（省缩进 token），减少输入体积、加快 Gate 返回。
- **请求体四段式**：system 提示词 → `<Story_Events>` 目录块（XML 包裹）→ `<Recent_Messages>` 剧情块（XML 包裹）→ 输出契约（`{ "events": [] }`）。
- **解析**：`parseStoryGateEventIds` 与现有事件求交集（精确匹配、去重、未知 ID 丢弃），最多保留 `STORY_GATE_MAX_SELECTED`（5）个。
- **注入**：`<Story_Event>` 块（强制指令 + 事件 id / 名称 / 触发条件 / 说明 + 正文），经 `setExtensionPrompt(IN_CHAT, depth 0, SYSTEM)` 注入到最后一条用户消息正下方；`generationEnded` / `generationStopped` 后清空，保证 swipes / 重生成 / 后续轮次不复用。
- **稳定性设计**（针对「发送前阻塞」加固）：
  - 运行锁 + 签名去重：上一轮预筛还在飞时新发送直接放行（本轮内容已覆盖）；同一末条被宿主反复触发只处理一次。
  - 载荷校验：其他插件自行 emit `messageSent`（如 QuickReply、自动回复脚本）时载荷与末条消息不一致，判定为误触发直接放行。
  - 失败即降级：Gate 失败 / 0 入选 / 宿主不支持注入 → 不注入直接放行，绝不让发送流程卡死。
  - 总超时：`STORY_GATE_TIMEOUT_MS`（45s）硬截止，中止在途请求并放行发送。
  - 能力检查：宿主不提供 `setExtensionPrompt` / `extension_prompt_types` 时静默跳过（枚举值按已知数值常量兜底：IN_CHAT=1、SYSTEM=0）。
- **设置与 UI**：`storyGateEnabled`（默认开）+ `storyGatePrompt`（留空用 `DEFAULT_STORY_GATE_PROMPT`），API 连接页可开关 / 编辑 / 恢复默认；最近一轮快照存 `globalThis.__kaleido_story_gate_last_round__`。
- **注入实录视图**（参考 SoulLink「角色扮演」视图）：最近一轮快照除入选 ID 外，还记录入选事件详情（`selectedEvents`：id / name / trigger / description / content）与注入提示词原文（`injectionText`）；面板内「注入实录」视图按 摘要 → 预筛原文（Gate 返回）→ 本轮触发的事件 → 注入提示词原文（可复制）四段展示，首页卡片点击进入。

## 19. 剧情脉络 · 角色卡绑定

- **为什么绑定角色卡**：剧情脉络是「角色专属」内容（作者为某个角色设计的剧情树），
  与角色卡同生共死。参考 SillyTavern 局部正则脚本的做法：数据直接存
  `character.data.extensions.kaleidoscope_story`（`{ version, nodes, scripts }`），
  角色卡序列化（导入 / 导出 / 复制 / 替换）天然携带该字段，**无需挂任何导入导出事件**。
- **读写路径**：`getStoryNodes` / `getStoryScripts` 只读当前角色卡绑定的内容——
  有角色但卡上无数据时显示为空，**不回退全局设置**（避免把别的角色 / 旧数据串到
  当前角色卡上）；只有群聊 / 未选角色 / 宿主不支持写角色卡时才用全局设置
  `storyNodes` / `storyScripts` 兜底（兼容旧数据）。`saveStoryData` 有角色且宿主
  提供 `ctx.writeExtensionField` 时写角色卡，否则写全局设置。
- **首次变更即迁移**：角色卡尚无剧情数据时，各变更函数先 `ensureStoryCardData`
  把当前数据（可能来自旧版全局设置）整体迁入角色卡并清空全局兜底，toastr 提示
  一次；之后该角色的剧情数据只存在于角色卡。
- **防抖持久化**：变更先同步落在内存角色卡对象（读路径一致），再经
  `STORY_CARD_SAVE_DEBOUNCE_MS`（500ms）防抖调用 `writeExtensionField` 写回宿主。
  定时器挂 `globalThis[STORY_CARD_SAVE_TIMER_KEY]`，热重载不丢。
- **按 avatar 定位防写错卡**：防抖期间用户可能切换角色，持久化时按 avatar
  重新定位角色索引，写回原角色；角色已删除 / 写入失败 / 宿主不支持时
  数据回退全局设置，避免丢失。
- **UI 反馈**：工作台头部徽标三态——已绑定（ok）/ 待绑定（warn，卡上暂无数据）/
  未绑定角色（idle，群聊或未选角色）；首页剧情脉络卡片状态悬停提示绑定信息。
