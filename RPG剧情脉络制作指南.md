# RPG剧情脉络制作指南

> 本文是万华镜「剧情脉络」的 RPG 制作指南，内容自包含：YAML 怎么写、`trigger` 怎么定、目录怎么组织，均在本篇内说明，可独立使用。
> 适用剧本：带明确日程的 RPG（具体某一天 / 某段时间），例如侦探、冒险，以及日常与主线混合的剧本。

## 一、总览：两个根节点，本质都是事件容器

整个剧情脉络有且只有两个根节点：**剧情类事件**与**规则类事件**。仅看名字，容易以为一个是「剧情」、一个是「规则」；实际上两者本质相同——**都是挂载事件的目录容器**。节点本身不承载任何内容，内容全部写在事件里（`trigger` + `content`），两类节点的区别只在于组织方式与 `trigger` 的写法：

- **剧情类事件**：按「时间 → 地点 →（人物）」组织，事件是剧情正文，`trigger` 写日期、地点、人物与动作；
- **规则类事件**：按「分类」组织，事件是常驻规则，`trigger` 写「什么场景下生效」，`content` 写那一刻实际发生的事。

因此两类都叫「事件」，而不叫「剧情」「规则」，用意正是提醒：**规则不是另一种写法，它也是事件**。在预筛 AI 眼中，不存在「剧情文件」与「规则文件」之分——它拿到的只有一份事件清单，两类事件混在一起，统一按 `trigger` 与最近对话的匹配情况挑选，满足条件的就注入。

```
剧情类事件
├── 2026年8月11日（某一天）
│   ├── 书房（地点）
│   │   ├── 发现旧笔记        （地点下直接挂事件）
│   │   └── 林晓（人物）
│   │       └── 林晓的求助    （人物下挂事件）
│   └── 商场（地点）
└── 2026年7月10日~2026年7月17日（时间段）
    └── 列车（地点）

规则类事件
├── 世界观
│   └── 隙间的显现             （规则事件：trigger + content）
├── 战斗
│   └── 战斗判定              （规则事件）
└── 对话
    └── NPC 信息边界           （规则事件）
```

## 二、文件结构：语法基础

文件是 YAML，只有两块：`nodes`（目录骨架）与 `scripts`（事件）。按下面的结构写：

```yaml
format: kaleidoscope-story   # 固定标记，导入时靠它识别
version: 1
character: 暑假侦探           # 可选：角色卡名或主角名
nodes:                       # 目录骨架
  - id: plot-events          # 节点唯一标识，建议可读、唯一
    name: 剧情类事件
    parentId: ""             # 父节点 id；省略或留空 = 根节点
    enabled: true            # 可选：false = 停用整棵子树
scripts:                     # 事件
  - id: "001"                # 事件编号，数字样加引号
    nodeId: plot-events      # 挂在哪个节点下
    name: 事件名
    trigger: 触发条件（全文件最重要的字段，写法见「四」）
    description: 可选，一句话说明用途
    content: |-              # 事件正文，从下一行缩进两格开始
      正文……
```

格式要点：

- 缩进用空格，列表项 `- ` 与字段对齐，正文比 `content:` 多缩进两格。
- 形如数字的 id 要加引号（`id: "001"`），否则会被 YAML 解析成数字；其余文本直接写，不用引号。
- `#` 开头是注释，可随手写备注。
- `enabled: false` 表示节点**暂时停用**：该节点连同它的子节点、事件都不会进入预筛目录；省略或 `enabled: true` 为启用。工作台里每个节点行都有滑块，点一下关闭、再点一下激活；导出的 YAML 每个节点都会带上 `enabled: true / false`。

语法仅此而已。剧情内容、事件数量、节点粒度与组织方式，都可以自由决定。

## 三、剧情类事件：时间 → 地点 →（人物）→ 事件

层级最多四层，每一层只允许挂指定类型：

| 层级 | 节点类型 | 命名示例 | 下面只能挂 |
| --- | --- | --- | --- |
| 第 1 层 | 根节点 | 剧情类事件 | 时间节点 |
| 第 2 层 | 时间 | `2026年8月11日` / `2026年7月10日~2026年7月17日` | 地点节点 |
| 第 3 层 | 地点 | 书房 / 商场 / 列车 | 事件 或 人物节点 |
| 第 4 层 | 人物 | 林晓 / 神秘商人 | 事件 |

> 不要在时间节点下直接挂事件——时间节点只是日程骨架，事件必须落在某个地点下。

### 3.1 时间节点（第 2 层）

- 两种写法：**某一天** `2026年8月11日`；**时间段** `2026年7月10日~2026年7月17日`（用 `~` 连接两端；全文件日期格式保持一致）。
- 文件里按时间先后排列节点，导入后目录顺序即文件顺序，读起来就是一本日程表。
- 在时间节点的 `description` 里写一句当天（或该时段）的剧情目标，如 `抵达老家的第一天：发现旧笔记，林晓上门求助`。节点说明会进入预筛目录，帮预筛 AI 把握当日主线。
- 某一天（或某个时间段）整体不参与预筛时，把该时间节点的滑块关掉（`enabled: false`）即可，不用删除；想恢复再点开。

### 3.2 地点节点（第 3 层）

- 挂在时间节点下，名字直接用地点名：书房、商场、废弃仓库。
- 同一地点出现在多天时，**每天各建一个节点**：8 月 11 日的「书房」与 8 月 12 日的「书房」是两个节点。节点树是严格的树，不支持跨天共用节点；同名不影响预筛，日期由各自的时间父节点区分。
- 同一天、同一地点要区分不同阶段时，可在名字里注明：`书房（夜晚）`、`书房（搜查后）`。
- 在 `description` 里写一句场景氛围，或「玩家在这里能做什么」，同样会进入预筛目录。
- 地点下可以直接挂事件，也可以挂人物节点（见 3.3）。

### 3.3 人物节点（第 4 层）

- 挂在地点下，把「围绕某个角色的剧情」单独收拢。当同一地点有多个角色、且每个角色的事件较多时，人物层能让目录一目了然。
- 如果只有一两个事件、且事件与地点强绑定，直接挂在地点下即可，不必为了整齐强行加人物层。
- 人物节点下只挂事件；人物是最后一层，不要再往下挂。

## 四、trigger 怎么写（全文件最重要的字段）

`trigger` 是预筛 AI 判断「现在该不该触发」的唯一依据，标准格式是四要素，缺一不可：

**时间 + 地点 + 人物 + 触发动作**

例如「2026年8月11日，书房，林晓，林晓上门找你时」——四要素齐全，预筛 AI 不看正文、仅凭最近对话就能判断「现在该不该触发」。

在本结构下，四要素与两类事件的对应关系如下：

| 要素 | 剧情类事件 | 规则类事件 |
| --- | --- | --- |
| 时间 | 所在时间节点的日期，写全：`2026年8月11日`；时间段写 `2026年7月10日至7月17日期间` | 任意时间 |
| 地点 | 所在地点节点名；确有多地点通用写 `任意地点` | 任意地点 |
| 人物 | 所在人物节点名（如 `林晓`）；直接挂在地点下时写涉及的人物，没有特定人物写 `任意人物` | 任意人物（或限定角色） |
| 触发动作 | 玩家或角色做了什么：`玩家翻找书桌抽屉时` | 场景动作：`进入战斗或进行战斗判定时` |

细则：

- 某个要素不适用时写「任意」占位，保持结构完整。
- 前置状态（如「好感度达到 3 后」「完成前置事件后」）并入时间要素。
- 第四要素只写对话里能核实到的动作或状态（如「玩家入睡后」）；事件触发后才会发生的剧情（如「梦见荒原与月下人群」）一律放 `content`。

常见错误：

- **把剧情内容写进 `trigger`。** 「2026年8月11日，书房，任意人物，玩家翻找书桌抽屉时，发现爷爷的旧笔记」是错的：发现什么要等事件注入后才会发生，预筛 AI 永远无法核实；正确写法是「2026年8月11日，书房，任意人物，玩家翻找书桌抽屉时」，把「发现旧笔记」写进 `content`。同理，「林晓主动搭话」「雨夜初遇」这类结果，都该从 `trigger` 里删掉。
- **条件过宽。** 例如「下雨」会在角色随口提到天气时触发，「睡觉」会在角色说「昨晚没睡好」时触发。条件要窄到「只有该场景真正发生时才会满足」——写清主语、时机与前置状态，而不是只写一个孤立的词。
- **写空话。** 不要写「合适的时候」这类无法核实的条件。

判断标准只有一条：**把四要素写全之后，预筛 AI 拿 `trigger` 和最近对话一对照，就能确定这个事件该不该触发。** 写不出这种 `trigger` 的事件，说明它还不到出场的时候；反过来，如果一句日常闲聊就能满足条件，则说明条件过宽，必然误触发。拿不准时，宁可把条件写窄、写死，也不要让它模棱两可——但「窄」要窄在精确，不能窄到够不着（见 4.1）。

### 4.1 触发动作要「够得着」

四要素写全只是第一步：**触发动作必须是玩家在剧情里自然会做的事**。玩家没有理由做的动作不会发生，事件也就永远触发不了。

例如「2010年7月17日，玩家住处，任意人物，玩家查看邮箱时」——玩家为什么要查看邮箱？没有铺垫的话，这个动作不会发生，生日邮件事件就永远出不来。

两种正确解法：

- **放宽触发条件。** 把「玩家查看邮箱时」改成玩家每天都会做的动作，如「2010年7月17日，晚上，玩家卧室，任意人物，玩家进入自己的卧室时」——晚上回卧室是每天都会发生的事，事件自然触发。
- **加一个提示事件。** 先写一个容易触发的事件，给玩家一个做动作的理由：进入卧室时触发「电脑屏幕上似乎跳出了一个新窗口」，玩家看到后自然会去查看邮箱，邮件事件再跟着触发。

判断标准：**玩家顺着剧情走，这个动作一定会发生吗？** 如果不会，就按上面两种办法处理。

RPG 结构下还有两个容易踩的坑：

- **游戏内「今天」必须能在最近对话里被核实。** 预筛 AI 靠最近几条消息判断日期，所以剧情正文开头要带日期交代（如 `2026年8月11日，上午。`），AI 回复时也会延续这种写法；否则预筛无法确认今天是不是 8 月 11 日，带日期的事件会漏触发。
- **时间段内的事件，时间要素要写「某日至某日期间」。** 不要只写「旅途中」「放假时」这类无法核实的词。

## 五、规则类事件：分类 → 规则事件

规则类事件树下挂的**同样是事件**，不是规则文档。常驻规则不能直接挂进节点当设定文本，要写成事件：`trigger` 写「什么场景下生效」，`content` 写「那一刻实际发生什么」，预筛 AI 会像挑选剧情事件一样把它筛选出来注入。

举个例子。假设世界观设定是「女生道歉时要露出内裤」——不要把它写成一条设定文档挂进「世界观」分类，而要写成事件：

| 字段 | 写法 |
| --- | --- |
| `name` | 道歉的规矩 |
| `trigger` | `任意时间，任意地点，女生，女生要向玩家道歉时` |
| `content` | 女生把裙子拎起来露出内裤，向玩家道歉。 |

剧情里女生要向玩家道歉时，预筛 AI 就会把这个事件注入，这条规矩就以剧情的形式出现在对话里——**规则事件就是这样一种「在正确时刻发生的事」，而不是一份整体灌输的设定文档。**

分类只负责把规则事件按场景归组，方便浏览与维护，分类本身不承载任何设定文本。常见分法如下（每个分类写的都是「触发场景 → 对应事件」）：

```
规则类事件
├── 世界观       例：女生要向玩家道歉时 → 女生拎起裙子露出内裤道歉
├── 战斗         例：进入战斗、需要判定结果时 → 战斗判定事件
├── 对话         例：玩家向 NPC 打听关键情报时 → NPC 信息边界事件
├── 行动         例：玩家尝试有风险的动作时 → 行动成败判定事件
└── 状态与进度    例：好感度达到阈值、任务进度推进时 → 状态变化事件
```

- 分类名要互斥、一看就懂，避免「杂项」「其他」这类模糊分类；规则系统复杂时，分类下可以再加一层二级分类（如 `战斗 → 伤害判定`），但最多三层，再深就难读了。
- 每条规则事件写两件事：`trigger`（生效时机）+ `content`（那一刻发生什么、该怎么表现；可用 `【世界观】` 等标记开头）。
- 规则事件的 `trigger` 同样按四要素写，时间、地点通常为「任意」，人物视规则范围而定：
  - `任意时间，任意地点，任意人物，进入战斗或进行战斗判定时` → 战斗判定事件；
  - `任意时间，任意地点，任意人物，玩家尝试购买或讨价还价时` → 交易规则事件；
  - `任意时间，任意地点，任意人物，玩家向 NPC 打听关键情报时` → NPC 信息边界事件。
- 剧情事件与规则事件各写各的：剧情正文里不要重复贴规则，规则事件里也不要写剧情，避免同一信息被两个事件重复注入、互相冲突。

写不出「什么场景下生效」的规则，就不该挂在这里（判断标准同「四」）。

## 六、完整示例

```yaml
format: kaleidoscope-story
version: 1
character: 暑假侦探          # 可选：角色卡名或主角名
nodes:
  # ===== 剧情类事件：时间 → 地点 →（人物）→ 事件 =====
  - id: plot-events
    name: 剧情类事件
    description: 剧情事件按时间、地点、人物组织
  - id: t-2026-0710-0717
    parentId: plot-events
    name: 2026年7月10日~2026年7月17日
    description: 返乡列车旅途，第一次目击「隙间」
  - id: loc-train
    parentId: t-2026-0710-0717
    name: 列车
    description: 深夜车厢，乘客稀少
  - id: t-2026-08-11
    parentId: plot-events
    name: 2026年8月11日
    description: 抵达老家的第一天：发现旧笔记，林晓上门求助
  - id: loc-study
    parentId: t-2026-08-11
    name: 书房
    description: 爷爷的书房，堆满旧书与笔记，抽屉里藏着秘密
  - id: char-lin
    parentId: loc-study
    name: 林晓
    description: 邻居家的女孩，知道爷爷的往事
  - id: loc-mall
    parentId: t-2026-08-11
    name: 商场
    description: 镇上唯一的商场，传闻晚上有异常动静
  # ===== 规则类事件：分类 → 规则事件 =====
  - id: rule-events
    name: 规则类事件
    description: 常驻规则事件，按分类组织
  - id: cat-world
    parentId: rule-events
    name: 世界观
  - id: cat-combat
    parentId: rule-events
    name: 战斗
  - id: cat-talk
    parentId: rule-events
    name: 对话

scripts:
  # ----- 剧情事件（挂在 地点 / 人物 下） -----
  - id: "001"
    nodeId: loc-train
    name: 深夜异象
    trigger: 2026年7月10日至7月17日期间，列车，任意人物，玩家在深夜醒来时
    content: |-
      2026年7月14日，凌晨。
      你从浅眠中醒来，车窗外的景色像被撕开一道黑色的口子——你第一次见到了「隙间」。
  - id: "002"
    nodeId: loc-study
    name: 发现旧笔记
    trigger: 2026年8月11日，书房，任意人物，玩家翻找书桌抽屉时
    content: |-
      2026年8月11日，上午。
      你拉开书桌最下面的抽屉，一本封皮发黄的笔记压在旧报纸底下，扉页写着爷爷的名字。
  - id: "003"
    nodeId: char-lin
    name: 林晓的求助
    trigger: 2026年8月11日，书房，林晓，林晓上门找你时
    content: |-
      2026年8月11日，下午。
      林晓推门进来，脸色发白：「爷爷的书店……昨天晚上出了怪事。」
  - id: "004"
    nodeId: loc-mall
    name: 商场的异常
    trigger: 2026年8月11日，商场，任意人物，玩家在商场调查异常传闻时
    content: |-
      2026年8月11日，傍晚。
      商场里没什么人。三楼尽头那家关门的店铺，门缝里透出不属于灯光的冷白色微光。
  # ----- 规则事件（挂在 规则类事件 的分类下） -----
  - id: "101"
    nodeId: cat-world
    name: 隙间的显现
    trigger: 任意时间，任意地点，任意人物，玩家目睹隙间或提及隙间时
    content: |-
      【世界观】隙间是现实世界的裂缝，普通人看不见，只有少数人能察觉。它出现时，空气像被撕开一道黑色的口子；描写时保持神秘感，不一次说透。
  - id: "102"
    nodeId: cat-combat
    name: 战斗判定
    trigger: 任意时间，任意地点，任意人物，进入战斗或进行战斗判定时
    content: |-
      【战斗】战斗由骰子判定：成功率低于 50% 视为失败，失败会带来代价而非停滞；战斗描写注重动作与后果，一轮一结算。
  - id: "103"
    nodeId: cat-talk
    name: NPC 信息边界
    trigger: 任意时间，任意地点，任意人物，玩家向 NPC 打听关键情报时
    content: |-
      【对话】NPC 只透露自己立场下会说的话：林晓知道传闻但不知道细节，爷爷的旧友知道往事但要求条件。不通过闲聊解锁关键情报。
```

## 七、写完后自查

语法：

- 文件头是否带 `format: kaleidoscope-story`？数字样 id 是否加了引号？缩进是否一致？（见「二」）
- 每个事件的 `trigger` 是否按「时间 + 地点 + 人物 + 触发动作」四要素写全？第四要素是不是误写成了剧情内容（如「梦见……」「主动搭话」）？（见「四」）
- 有没有条件过宽、会被日常闲聊误触发的 `trigger`？条件里的每个词都经得起推敲吗？（见「四」）
- 每个事件的触发动作，是玩家顺着剧情自然会做的吗？如果不会，是否放宽了条件，或加了提示事件引导？（见 4.1）
- 有没有多个事件的触发条件大面积重叠，让预筛难以取舍？（见「四」）

结构：

- 是否恰好两个根节点：`剧情类事件` 和 `规则类事件`？
- 剧情类事件：时间节点下是否只挂了地点？地点下是否只挂了事件或人物？人物下是否只挂了事件？有没有把事件直接挂在时间下？（见「三」）
- 同一地点跨多天时，是否每天各建了一个地点节点，而不是把两个时间节点挂到同一个地点下？（见 3.2）
- 每个剧情事件的 `trigger` 是否与所在节点链一致（日期、地点、人物都能对上）？（见「四」）
- 日期是否在事件正文里交代清楚，预筛 AI 从最近对话里能核实游戏内「今天」吗？（见「四」）
- 规则类事件：每条规则是否都写成了事件（`trigger` + `content`），而不是把规则文档直接挂在节点下？分类是否互斥、有序、一看就懂？规则事件会不会在剧情场景里误触发，或者与剧情事件大面积重叠？（见「五」）
- 有没有忘记恢复的 `enabled: false` 节点？（见「二」）

## 八、导入与后续调整

写好后在「剧情脉络 → 导入 剧情脉络」导入；导入后可在可视化工作台里继续微调。

导入整包时会先弹出**导入方式**选择：

- **合并导入**（推荐）：同 id 条目更新，其余追加，不动已有内容；
- **覆盖导入**：清空当前剧情脉络后，用导入文件整体替换（适合整包换血 / 换角色重开）。

两种方式都会保留导入文件里的 `enabled: false` 停用状态；文件里没写 `enabled` 的节点，导入后一律默认启用。

## 九、从模板起步

新剧本可以从下面的骨架起步：先建两个根节点，再逐日填充事件。

```yaml
format: kaleidoscope-story
version: 1
nodes:
  - id: plot-events
    name: 剧情类事件
  - id: rule-events
    name: 规则类事件
scripts: []
```