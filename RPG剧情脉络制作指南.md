# RPG剧情脉络制作指南

> 本文是万华镜「剧情脉络」的 RPG 制作指南，内容自包含：YAML 怎么写、`trigger` 怎么定、目录怎么组织，均在本篇内说明，可独立使用。
> 适用剧本：带明确日程的 RPG（具体某一天 / 某段时间），例如侦探、冒险，以及日常与主线混合的剧本。

## 一、总览：两个根节点，本质都是事件容器

整个剧情脉络有且只有两个根节点：**剧情类事件**与**规则类事件**。两者本质相同，都是挂载事件的目录容器：节点本身不承载内容，内容全部写在事件（`trigger` + `content`）里。

两类事件的区别只在触发限制：

- **剧情类事件**：触发受时间、地点或人物的限制，`trigger` 写日期、地点、人物与动作；
- **规则类事件**：触发不受限制或限制很少，`trigger` 只写触发场景（如「玩家抬头看向太阳时」），全局、永久生效，极少使用。

因此两类都叫「事件」而不叫「剧情」「规则」：规则不是另一种写法，它也是事件。预筛 AI 拿到的只有一份事件清单，两类事件混在一起，统一按 `trigger` 与最近对话的匹配情况挑选，满足条件的就注入。

```
剧情类事件
├── 2026年8月11日（某一天）
│   ├── 书房（地点）
│   │   ├── 发现旧笔记        （地点下直接挂事件）
│   │   └── 林晓（人物）
│   │       └── 林晓的求助    （人物下挂事件）
│   └── 商场（地点）
└── 2026年7月10日~2026年7月17日（时间段）
    └── 列车（地点）

规则类事件
└── 道歉的规矩                 （规则事件：全局永久，trigger + content）
```

## 二、文件结构：语法基础

文件是 YAML，只有两块：`nodes`（目录骨架）与 `scripts`（事件）。按下面的结构写：

```yaml
format: kaleidoscope-story   # 固定标记，导入时靠它识别
version: 1
character: 暑假侦探           # 可选：角色卡名或主角名
nodes:                       # 目录骨架
  - id: plot-events          # 节点唯一标识，建议可读、唯一
    name: 剧情类事件
    parentId: ""             # 父节点 id；省略或留空 = 根节点
    enabled: true            # 可选：false = 停用整棵子树
scripts:                     # 事件
  - id: "001"                # 事件编号，数字样加引号
    nodeId: plot-events      # 挂在哪个节点下
    name: 事件名
    trigger: 触发条件（全文件最重要的字段，写法见「四」）
    description: 可选，一句话说明用途
    content: |-              # 事件正文，从下一行缩进两格开始
      正文……
```

格式要点：

- 缩进用空格，列表项 `- ` 与字段对齐，正文比 `content:` 多缩进两格。
- 形如数字的 id 要加引号（`id: "001"`），否则会被 YAML 解析成数字；其余文本直接写，不用引号。
- `#` 开头是注释，可随手写备注。
- `enabled: false` 表示节点**暂时停用**：该节点连同它的子节点、事件都不会进入预筛目录；省略或 `enabled: true` 为启用。工作台里每个节点行都有滑块，点一下关闭、再点一下激活；导出的 YAML 每个节点都会带上 `enabled: true / false`。

语法仅此而已。剧情内容、事件数量、节点粒度与组织方式，都可以自由决定。

## 三、剧情类事件：时间 → 地点 →（人物）→ 事件

层级最多四层，每一层只允许挂指定类型：

| 层级 | 节点类型 | 命名示例 | 下面只能挂 |
| --- | --- | --- | --- |
| 第 1 层 | 根节点 | 剧情类事件 | 时间节点 |
| 第 2 层 | 时间 | `2026年8月11日` / `2026年7月10日~2026年7月17日` | 地点节点 |
| 第 3 层 | 地点 | 书房 / 商场 / 列车 | 事件 或 人物节点 |
| 第 4 层 | 人物 | 林晓 / 神秘商人 | 事件 |

> 不要在时间节点下直接挂事件——时间节点只是日程骨架，事件必须落在某个地点下。

### 3.1 时间节点（第 2 层）

- 两种写法：**某一天** `2026年8月11日`；**时间段** `2026年7月10日~2026年7月17日`（用 `~` 连接两端；全文件日期格式保持一致）。
- 文件里按时间先后排列节点，导入后目录顺序即文件顺序，读起来就是一本日程表。
- 在时间节点的 `description` 里写一句当天（或该时段）的剧情目标，如 `抵达老家的第一天：发现旧笔记，林晓上门求助`。节点说明会进入预筛目录，帮预筛 AI 把握当日主线。
- 某一天（或某个时间段）整体不参与预筛时，把该时间节点的滑块关掉（`enabled: false`）即可，不用删除；想恢复再点开。

### 3.2 地点节点（第 3 层）

- 挂在时间节点下，名字直接用地点名：书房、商场、废弃仓库。
- 同一地点出现在多天时，**每天各建一个节点**：8 月 11 日的「书房」与 8 月 12 日的「书房」是两个节点。节点树是严格的树，不支持跨天共用节点；同名不影响预筛，日期由各自的时间父节点区分。
- 同一天、同一地点要区分不同阶段时，可在名字里注明：`书房（夜晚）`、`书房（搜查后）`。
- 在 `description` 里写一句场景氛围，或「玩家在这里能做什么」，同样会进入预筛目录。
- 地点下可以直接挂事件，也可以挂人物节点（见 3.3）。

### 3.3 人物节点（第 4 层）

- 挂在地点下，把「围绕某个角色的剧情」单独收拢。当同一地点有多个角色、且每个角色的事件较多时，人物层能让目录一目了然。
- 如果只有一两个事件、且事件与地点强绑定，直接挂在地点下即可，不必为了整齐强行加人物层。
- 人物节点下只挂事件；人物是最后一层，不要再往下挂。

## 四、trigger 怎么写（全文件最重要的字段）

`trigger` 是预筛 AI 判断「现在该不该触发」的唯一依据，标准写法是让 trigger 体现四要素：

**时间 + 地点 + 人物 + 触发动作**

例如「2010年7月17日，上午，建材城，玩家陪路明非购买马桶座圈时」——时间、地点、人物、动作都在里面，预筛 AI 不看正文、仅凭最近对话就能判断「现在该不该触发」。

四要素体现在 trigger 里即可：人物已经出现在动作中时，动作本身就同时承担人物与动作两个要素——「玩家陪路明非购买马桶座圈时」这一句，人物（路明非）与动作（购买马桶座圈）都写清了。

在本结构下，四要素与两类事件的对应关系如下：

| 要素 | 剧情类事件 | 规则类事件 |
| --- | --- | --- |
| 时间 | 所在时间节点的日期，写全：`2026年8月11日`；时间段写 `2026年7月10日至7月17日期间` | 省略；确有限制时写 |
| 地点 | 所在地点节点名；确有多地点通用时省略 | 省略；确有限制时写 |
| 人物 | 事件涉及的人物，体现在动作里即可（如「玩家陪路明非购买马桶座圈时」）；没有特定人物就省略 | 涉及的角色体现在场景里（如 `女生`）；没有就省略 |
| 触发动作 | 玩家或角色做了什么：`玩家翻找书桌抽屉时` | 特殊场景：`玩家抬头看向太阳时` |

细则：

- 要素不适用时直接省略：没有特定人物就不写人物；规则事件不受时间、地点约束时，也不写时间、地点。
- 前置状态（如「好感度达到 3 后」「完成前置事件后」）并入时间要素。
- 第四要素只写对话里能核实到的动作或状态（如「玩家入睡后」）；事件触发后才会发生的剧情（如「梦见荒原与月下人群」）一律放 `content`。

常见错误：

- **把剧情内容写进 `trigger`。** 「2026年8月11日，书房，玩家翻找书桌抽屉时，发现爷爷的旧笔记」是错的：发现什么要等事件注入后才会发生，预筛 AI 永远无法核实；正确写法是「2026年8月11日，书房，玩家翻找书桌抽屉时」，把「发现旧笔记」写进 `content`。同理，「林晓主动搭话」「雨夜初遇」这类结果，都该从 `trigger` 里删掉。
- **条件过宽。** 例如「下雨」会在角色随口提到天气时触发，「睡觉」会在角色说「昨晚没睡好」时触发。条件要窄到「只有该场景真正发生时才会满足」——写清主语、时机与前置状态，而不是只写一个孤立的词。
- **写空话。** 不要写「合适的时候」这类无法核实的条件。

判断标准只有一条：**把四要素体现出来之后，预筛 AI 拿 `trigger` 和最近对话一对照，就能确定这个事件该不该触发。** 写不出这种 `trigger` 的事件，说明它还不到出场的时候；反过来，如果一句日常闲聊就能满足条件，则说明条件过宽，必然误触发。拿不准时，宁可把条件写窄、写死，也不要让它模棱两可——但「窄」要窄在精确，不能窄到够不着（见 4.1）。

### 4.1 触发动作要「够得着」

把四要素体现出来只是第一步：**触发动作必须是玩家在剧情里自然会做的事**。玩家没有理由做的动作不会发生，事件也就永远触发不了。

例如「2010年7月17日，玩家住处，玩家查看邮箱时」——玩家为什么要查看邮箱？没有铺垫的话，这个动作不会发生，生日邮件事件就永远出不来。

两种正确解法：

- **放宽触发条件。** 把「玩家查看邮箱时」改成玩家每天都会做的动作，如「2010年7月17日，晚上，玩家卧室，玩家进入自己的卧室时」——晚上回卧室是每天都会发生的事，事件自然触发。
- **加一个提示事件。** 先写一个容易触发的事件，给玩家一个做动作的理由：进入卧室时触发「电脑屏幕上似乎跳出了一个新窗口」，玩家看到后自然会去查看邮箱，邮件事件再跟着触发。

判断标准：**玩家顺着剧情走，这个动作一定会发生吗？** 如果不会，就按上面两种办法处理。

RPG 结构下还有两个容易踩的坑：

- **游戏内「今天」必须能在最近对话里被核实。** 预筛 AI 靠最近几条消息判断日期，所以剧情正文开头要带日期交代（如 `2026年8月11日，上午。`），AI 回复时也会延续这种写法；否则预筛无法确认今天是不是 8 月 11 日，带日期的事件会漏触发。
- **时间段内的事件，时间要素要写「某日至某日期间」。** 不要只写「旅途中」「放假时」这类无法核实的词。

## 五、规则类事件：极少使用，全局永久生效

规则类事件是剧情脉络里最容易用错的部分：**它罕见，大多数剧本根本不需要它**。先记住它的三个特征：

- **它是事件，不是介绍。** 人物介绍、世界观设定、力量体系这类内容不该写进剧情脉络——它们属于角色卡或预设。规则类事件必须能写成「场景出现 → 发生某事」。
- **它全局、永久生效。** 与剧情类事件不同，它不受时间、地点、人物的约束：触发场景一出现就注入，任何时候、任何地方都成立。
- **它很少。** 通常只有一两条，大部分剧本一条都没有；`规则类事件` 根节点下面没有事件也没关系。

只有那种「确实很特殊、又必须在任何时候都成立」的设定，才值得写成规则事件。例如「女生道歉时要露出内裤」——这不是介绍，而是一个事件：

| 字段 | 写法 |
| --- | --- |
| `name` | 道歉的规矩 |
| `trigger` | `女生要向玩家道歉时` |
| `content` | 女生把裙子拎起来露出内裤，向玩家道歉。 |

再比如「只要玩家抬头看向太阳，就会有一道光线射下来杀死玩家」：

| 字段 | 写法 |
| --- | --- |
| `name` | 直视太阳的代价 |
| `trigger` | `玩家抬头看向太阳时` |
| `content` | 一道光线从太阳中射出，正中你的眼睛——你死了。 |

两个例子的共同点：场景一出现，事件立即注入，规则以剧情的形式出现在对话里；其余时间它不出现。

挂载方式：规则事件直接挂在 `规则类事件` 根节点下；规则很多（超过三五个）时才考虑加一层分类，分类只负责归组，不承载任何设定文本。

规则事件的 `trigger` 写法相同，只是时间、地点通常省略，人物需要限定时体现在场景里：

- `女生要向玩家道歉时` → 道歉的规矩；
- `玩家抬头看向太阳时` → 直视太阳的代价。

剧情事件与规则事件各写各的：剧情正文里不要重复贴规则，规则事件里也不要写剧情，避免同一信息被两个事件重复注入、互相冲突。

判断标准：**这条规则需要在任何时候、任何地方、对任何人都生效吗？** 如果不是，它就不是规则事件——要么是剧情事件，要么根本不该写进剧情脉络。

## 六、完整示例

```yaml
format: kaleidoscope-story
version: 1
character: 暑假侦探          # 可选：角色卡名或主角名
nodes:
  # ===== 剧情类事件：时间 → 地点 →（人物）→ 事件 =====
  - id: plot-events
    name: 剧情类事件
    description: 剧情事件按时间、地点、人物组织
  - id: t-2026-0710-0717
    parentId: plot-events
    name: 2026年7月10日~2026年7月17日
    description: 返乡列车旅途，第一次目击「隙间」
  - id: loc-train
    parentId: t-2026-0710-0717
    name: 列车
    description: 深夜车厢，乘客稀少
  - id: t-2026-08-11
    parentId: plot-events
    name: 2026年8月11日
    description: 抵达老家的第一天：发现旧笔记，林晓上门求助
  - id: loc-study
    parentId: t-2026-08-11
    name: 书房
    description: 爷爷的书房，堆满旧书与笔记，抽屉里藏着秘密
  - id: char-lin
    parentId: loc-study
    name: 林晓
    description: 邻居家的女孩，知道爷爷的往事
  - id: loc-mall
    parentId: t-2026-08-11
    name: 商场
    description: 镇上唯一的商场，传闻晚上有异常动静
  # ===== 规则类事件：极少使用，全局永久生效 =====
  - id: rule-events
    name: 规则类事件
    description: 特殊全局设定，全局永久生效

scripts:
  # ----- 剧情事件（挂在 地点 / 人物 下） -----
  - id: "001"
    nodeId: loc-train
    name: 深夜异象
    trigger: 2026年7月10日至7月17日期间，列车，玩家在深夜醒来时
    content: |-
      2026年7月14日，凌晨。
      你从浅眠中醒来，车窗外的景色像被撕开一道黑色的口子——你第一次见到了「隙间」。
  - id: "002"
    nodeId: loc-study
    name: 发现旧笔记
    trigger: 2026年8月11日，书房，玩家翻找书桌抽屉时
    content: |-
      2026年8月11日，上午。
      你拉开书桌最下面的抽屉，一本封皮发黄的笔记压在旧报纸底下，扉页写着爷爷的名字。
  - id: "003"
    nodeId: char-lin
    name: 林晓的求助
    trigger: 2026年8月11日，书房，林晓上门找你时
    content: |-
      2026年8月11日，下午。
      林晓推门进来，脸色发白：「爷爷的书店……昨天晚上出了怪事。」
  - id: "004"
    nodeId: loc-mall
    name: 商场的异常
    trigger: 2026年8月11日，商场，玩家在商场调查异常传闻时
    content: |-
      2026年8月11日，傍晚。
      商场里没什么人。三楼尽头那家关门的店铺，门缝里透出不属于灯光的冷白色微光。
  # ----- 规则事件（只有真正特殊的全局设定才写，本例只有一条） -----
  - id: "101"
    nodeId: rule-events
    name: 只有你能看见隙间
    trigger: 隙间出现或旁人提及隙间时
    content: |-
      隙间是现实世界的裂缝，只有你能看见，其他人毫无察觉；旁人提起时只当作错觉或传说，你也不会主动解释。描写保持神秘感，不一次说透。
```

## 七、写完后自查

语法：

- 文件头是否带 `format: kaleidoscope-story`？数字样 id 是否加了引号？缩进是否一致？（见「二」）
- 每个事件的 `trigger` 是否包含「时间 + 地点 + 人物 + 触发动作」四要素（体现在 trigger 里即可，不适用时省略）？第四要素是不是误写成了剧情内容（如「梦见……」「主动搭话」）？（见「四」）
- 有没有条件过宽、会被日常闲聊误触发的 `trigger`？条件里的每个词都经得起推敲吗？（见「四」）
- 每个事件的触发动作，是玩家顺着剧情自然会做的吗？如果不会，是否放宽了条件，或加了提示事件引导？（见 4.1）
- 有没有多个事件的触发条件大面积重叠，让预筛难以取舍？（见「四」）

结构：

- 是否恰好两个根节点：`剧情类事件` 和 `规则类事件`？
- 剧情类事件：时间节点下是否只挂了地点？地点下是否只挂了事件或人物？人物下是否只挂了事件？有没有把事件直接挂在时间下？（见「三」）
- 同一地点跨多天时，是否每天各建了一个地点节点，而不是把两个时间节点挂到同一个地点下？（见 3.2）
- 每个剧情事件的 `trigger` 是否与所在节点链一致（日期、地点、人物都能对上）？（见「四」）
- 日期是否在事件正文里交代清楚，预筛 AI 从最近对话里能核实游戏内「今天」吗？（见「四」）
- 规则类事件：是不是真的需要它（大部分剧本可以一条都不写）？每条规则是否写成了「场景出现 → 发生某事」的事件，而不是介绍或文档？它是否全局、永久生效？（见「五」）
- 有没有忘记恢复的 `enabled: false` 节点？（见「二」）

## 八、导入与后续调整

写好后在「剧情脉络 → 导入 剧情脉络」导入；导入后可在可视化工作台里继续微调。

导入整包时会先弹出**导入方式**选择：

- **合并导入**（推荐）：同 id 条目更新，其余追加，不动已有内容；
- **覆盖导入**：清空当前剧情脉络后，用导入文件整体替换（适合整包换血 / 换角色重开）。

两种方式都会保留导入文件里的 `enabled: false` 停用状态；文件里没写 `enabled` 的节点，导入后一律默认启用。

## 九、从模板起步

新剧本可以从下面的骨架起步：先建两个根节点，再逐日填充剧情事件；规则类事件通常留空。

```yaml
format: kaleidoscope-story
version: 1
nodes:
  - id: plot-events
    name: 剧情类事件
  - id: rule-events
    name: 规则类事件    # 通常留空，只在有特殊全局设定时才加事件
scripts: []
```