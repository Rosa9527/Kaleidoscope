# 万华镜 · 插件结构速查（v0.3.0）

## 一、总体结构

```
Kaleidoscope/
├── manifest.json      # 插件清单：display_name / js / css / loading_order
├── index.js           # 构建产物（node build.js 从 js/ 拼接生成，勿手改）
├── style.css          # 全部样式：古风典雅 + 万花筒意象
├── build.js           # 构建脚本（FILES 数组决定拼接顺序）
├── build.bat          # Windows 双击构建
├── js/                # 源码（普通脚本，非 ES module，全局作用域共享）
│   ├── constants.js   # 所有常量：DOM ID / 默认设置 / 超时 / 尺寸
│   ├── utils.js       # 设置读写、API 请求、chatCompletion、并发限制
│   ├── host.js        # 宿主适配：getContextSafe / 事件订阅
│   ├── send-barrier.js  # 跨扩展发送屏障：并发协调多个扩展的发送前任务（与 SoulLink 共用协议）
│   ├── ui-shell.js    # 悬浮球、面板、视图切换、ESC 处理
│   ├── views-api.js   # API 连接视图 UI 与交互
│   ├── views-log.js   # 系统日志：捕获 / 存储 / 视图 UI
│   ├── views-home.js  # 首页状态刷新
│   ├── story-data.js  # 剧情脉络：数据模型 / YAML 导入导出 / 合并
│   ├── story-gate.js  # 剧情预筛：Gate 预筛 + 事件注入（messageSent 阻塞，经发送屏障并发）
│   ├── views-story.js # 剧情脉络：可视化树状工作台 UI
│   └── main.js        # 入口：bootstrap / 菜单注册 / 事件看门狗
├── tests/             # 开发测试（data / ui / proxy / gate / barrier，不参与构建）
├── 开发日志.md        # 开发进度（按日期，v0.2.0 起）
├── 开发日志-归档.md   # 历史开发日志归档（v0.1.x）
├── 重要开发经验.md    # 踩坑与解决方案
├── 重要设计.md        # 架构与设计决策
└── README.md
```

## 二、各文件职责

### ① `js/constants.js` — 常量中心
- 模块名 `MODULE_NAME`（设置存储键）、显示名 `MODULE_DISPLAY_NAME`、版本。
- 所有 DOM ID（`kaleido-*` 前缀）、图标 class（`MENU_ICON_CLASS` = `fa-solid fa-fan`）。
- `DEFAULT_SETTINGS`：apiUrl / apiKey / model / modelOptions / 并发 / 思考强度 /
  storyNodes / storyScripts（剧情脉络数据）/ storyGateEnabled / storyGatePrompt（剧情预筛）。
- `DEFAULT_STORY_GATE_PROMPT`：剧情预筛默认提示词（事件目录 + 最近 4 条消息 → 事件 ID）。
- `PANEL_VIEW_TITLES`：视图 ID → 标题。
- 注意：`js/` 所有文件共享全局作用域，常量命名要全局唯一（`KALEIDO` / `kaleido` 前缀）。

### ② `js/utils.js` — 工具与 API
- 设置读写：`getSettings`（自动补齐默认值）/ `saveSettings` / `saveSettingsImmediate`，
  宿主 context 不可扩展时走 `FALLBACK_SETTINGS_STORE`（WeakMap）兜底。
- API：`getApiBase`（自动剥掉 `/chat/completions`、`/models` 后缀）、`getAuthHeaders`、
  `fetchModelList`（拉取模型列表）、`chatCompletion`（OpenAI 兼容对话）。
- 跨域处理：`isCrossOriginUrl` → 走宿主代理 `requestHostProxy*`（对话 `/api/backends/chat-completions/generate`、模型列表 `/status`），失败回退直连。
- 稳定性：超时（AbortController）、自动重试（仅 429/5xx 等可重试错误）、
  并发限制（`acquireApiConcurrencySlot`，排队 + 取消即出队）。
- `parseAgentJson`：解析 AI 返回的 JSON（兼容 ```json 代码块）。

### ③ `js/host.js` — 宿主适配层
- `getContextSafe()`：`Luker.getContext` / `SillyTavern.getContext` 二选一。
- `onHostEvent(ctx, eventName, handler, key)`：统一挂事件订阅；
  经 `ctx.event_types` 解析事件名（TauriTavern 与标准 SillyTavern 的差异），
  以全局 key 去重，重挂时先 removeListener。

### ④ `js/send-barrier.js` — 跨扩展发送屏障
- 与 SoulLink 共用的发送屏障协议 v1（两边实现逐字节一致），挂 `globalThis.__preSendInjectionBarrier__`。
- 背景：宿主逐个 await messageSent 监听器，多个扩展各跑各的时发送前耗时 = 各 Gate 之和；
  屏障把已注册任务并发执行，耗时 = max(各任务)，同时保持「所有注入都在主请求发出前完成」。
- `register(name, task)`：注册发送前任务（task: `(ctx, payload) => Promise<void>`）；
  `waitAll(ctx, payload, timeoutMs)`：并发执行本轮所有任务，同签名共享同一 Promise。
- 轮次签名取末条消息 ID（缺失回退文本）：后到的监听器复用本轮结果，绝不重跑；
  新签名开新轮替换旧轮；`Promise.allSettled` 失败隔离 + 整轮硬截止兜底。
- 调用方必须把全部守卫（开关 / 载荷校验 / 超时 / 降级）放进任务，并处理屏障不可用时的回退。
### ⑤ `js/ui-shell.js` — UI 外壳
- 悬浮球：六棱万花筒（clip-path 六边形 + 旋转 conic-gradient 镜眼 + 轴心），
  拖拽 / 点击开面板 / 长按隐藏，位置存 localStorage。
- 面板：宣纸卷轴卡，头部墨色横批 + 鎏金底边 + 「镜」朱砂印章，回纹饰带，
  可拖拽、ESC 关闭 / 返回首页。
- 视图切换：`showPanelView(viewId)` 切换 `.kaleido-view` 激活态 + 标题 + 返回键。
- `createPanel()`：一次生成首页 + API 两个视图的 HTML，
  装配拖拽 / 视图 / API 交互；首页「剧情脉络」卡片点击后打开独立工作台对话框。
- 剧情脉络工作台不占面板空间：由 `views-story.js` 的 `initStorySection()`
  创建全屏遮罩 + 大窗口对话框（移动端全屏），面板 ESC 处理器会先让位给工作台。

### ⑥ `js/views-api.js` — API 连接视图
- 连接并拉取模型（`connectAndLoadModels`），失败提示 toastr + 状态徽标。
- 表单持久化：输入即存（`bindPersist`），模型列表 / 手动输入双向同步。
- 并发限制开关 + 上限输入（1–10）；思考强度下拉。
- 剧情预筛设置区：启用开关 + 预筛提示词编辑（留空用默认，可一键恢复默认）。

### ⑦ `js/views-log.js` — 系统日志
- 参考 SoulLink 的日志系统：捕获 console、页面错误、Promise 拒绝、宿主事件与 fetch 网络请求。
- `pushLogEntry`：统一入口（级别 / 来源 / 消息 / 详情），噪音过滤 + 连续重复折叠。
- `initLogView(panel)`：日志视图 UI（级别 chips / 搜索 / 来源 / 条数 / 暂停 / 跟随 / 清空 / 复制 / 导出 / 完整请求体导出 / 噪音开关）。
- 对外暴露 `globalThis.KaleidoscopeLog` API；热重载共用 `LOG_STATE_KEY` 缓冲不丢状态。

### ⑧ `js/views-home.js` — 首页状态
- `refreshHomeApiStatus`：按「已选模型 + 已填地址 → 有地址无模型 → 未连接」三态显示。
- `refreshHomeStoryStatus`：剧情脉络卡片显示「X 节点 · Y 事件」。
- `refreshHomeStatuses()`：统一刷新入口，后续新增卡片状态在此挂接。

### ⑨ `js/views-inject.js` — 注入实录视图
- 参考 SoulLink「角色扮演」视图：展示最近一轮剧情预筛的完整结果。
- `renderInjectView()`：按 摘要（结果 + 耗时 / 候选 / 入选 + 触发名单）→
  预筛原文（Gate 返回）→ 本轮触发的事件（名称 / ID / 触发条件 / 说明 / 正文）→
  注入提示词原文 四段渲染；无记录时显示空态。
- `copyInjectInjectionText()`：一键复制注入提示词原文（Clipboard API，回退 execCommand）。

### ⑩ `js/story-data.js` — 剧情脉络数据与 YAML
- 数据模型：`storyNodes` / `storyScripts`（id 为 `kaleido-n-*` / `kaleido-s-*`）。
- **角色卡绑定**（参考 SillyTavern 局部正则脚本）：剧情数据存
  `character.data.extensions.kaleidoscope_story`（`{ version, nodes, scripts }`），
  随角色卡导入/导出自动携带；`getStoryCharacter` / `getStoryCardData` 读取当前角色，
  `saveStoryData` 经 `ctx.writeExtensionField` 防抖写回角色卡（按 avatar 定位，防抖期间
  切换角色不写错卡）；**剧情脉络只显示当前角色卡绑定的内容**——有角色但卡上无数据时
  显示为空，只有群聊 / 未选角色 / 宿主不支持时才回退全局设置
  `storyNodes` / `storyScripts`；各变更函数先 `ensureStoryCardData`，首次变更自动把
  旧版全局数据迁入当前角色卡并清空全局。
- 节点层级：节点带 `parentId`，支持任意层嵌套（第一卷 → 第一章 → …）；查询助手
  `getStoryRootNodes` / `getStoryNodeChildren` / `getStoryNodeById` / `isStoryNodeAncestor`。
- 防环：`create/updateStoryNode` 校验父级——无效父级、挂到自己或后代一律回落为根；
  导入合并同样对悬空父级 / 自引用 / 成环做回落处理，保证导入后仍是合法树。
- 增删改查：`create/update/deleteStoryNode`、`create/update/deleteStoryScript`；
  删除节点时子节点上移到其父级（顶层则成根）、其下事件转为「未分类」（不删事件），
  返回 `{ movedChildren, detachedScripts }` 供 UI 提示。
- 轻量 YAML 解析器 `parseYamlSubset`：仅支持本项目子集（映射 / 列表 / 标量 / 块文本
  `|`、`|-` / 注释 / `---` 标记），导入失败会给出可读报错。
- 整包格式：`version / nodes / scripts`（nodes 含 `id / parentId / name / description / enabled`；
  scripts 内含 `id / nodeId / name / trigger / description / content`）；
  单事件格式：`--- name / id / Trigger / description ---` + 正文。
- 导入合并 `mergeStoryBundleInto`：同 id 更新、其余追加；引用不存在的节点自动转「未分类」。

### ⑨ `js/views-story.js` — 剧情脉络可视化工作台
- 全屏遮罩 + 居中大窗口（`kaleido-story-dialog`），移动端（`max-width: 640px`）
  自动转为全屏，操作按钮常显、隐藏辅助信息，适配小屏。
- 树状渲染：`renderStoryTree` / `renderStoryNodeRows`；节点行可展开 / 收起、
  通过「＋」菜单新建子节点或事件、导入事件、编辑、删除；未分类事件单独成组显示。
- 「＋」新建菜单：顶部工具栏与节点行的加号统一弹出二选一菜单（新建节点 / 新建事件），
  根级新建事件默认未分类；点击菜单外部自动收起。
- 编辑器（右侧浮层，移动端全幅覆盖）：
  - 节点：名称 / 上级节点下拉（排除自身与后代，防环）/ 说明；
  - 事件：ID（默认 001 起自动递增，重复时自动顺延）/ 名称 / 触发条件 / 说明 / 所属节点 / 正文（必填），保存按钮区分「保存（导入）」。
- 保存以「当前可见的表单」为准（而非内部模式标记），异步导入使用会话号防抢占：
  即使导入的事件文件尚未读完，也不会把正在编辑的节点表单顶掉。
- 导入：整包 YAML 或单事件 frontmatter 文件（单事件导入后进入编辑器确认，默认未分类）。
- 导出：整包 YAML（绑定角色卡 `剧情脉络: 角色卡名.yaml`，群聊 / 未选角色回退
  `万华镜-剧情脉络-时间戳.yaml`，含 `format: kaleidoscope-story` 标记与角色卡名）
  或单事件（`万华镜-事件-名称.yaml`）。
- 打开 / 关闭：`openStoryWorkbench` / `closeStoryWorkbench`；ESC 与点击遮罩关闭。
- 头部「角色卡绑定」徽标 `refreshStoryBindingStatus`：已绑定（ok）/ 待绑定（warn）/
  未绑定角色（idle），随 `renderStoryTree` 刷新。

### ⑨½ `js/story-gate.js` — 剧情预筛（Story Gate）
- 触发时机：用户点击发送（宿主 `messageSent` 事件，监听器返回 Promise 阻塞主模型请求）。
- 流程：Gate（剧情预筛提示词 + 事件目录 + 最近 4 条消息）→ 解析事件 ID →
  拼接 `<Story_Event>` 块 → `setExtensionPrompt(IN_CHAT, depth 0, SYSTEM)` 注入
  到最后一条用户消息正下方 → 恢复发送；`generationEnded` / `generationStopped` 后清空注入。
- 事件目录 `buildStoryEventCatalog`：节点（层级 + 说明）+ 事件（只含名字 / ID /
  触发条件 / 描述，不含正文），未分类事件单独成组。
- 稳定性：运行锁 + 签名去重、名单交集（未知 ID 丢弃）、失败即降级放行、
  总超时 `STORY_GATE_TIMEOUT_MS` 硬截止、注入清理、宿主能力检查。
- 最近一轮快照存 `globalThis.__kaleido_story_gate_last_round__`：除入选 ID 外还记录
  入选事件详情（`selectedEvents`）与注入提示词原文（`injectionText`），
  供「注入实录」视图展示；首页「注入实录」卡片点击进入详情视图。

### ⑩ `js/main.js` — 入口
- `bootstrap()`：等 `appReady` → 装事件订阅 → 看门狗 → 面板 → 悬浮球 → 菜单。
- 菜单：宿主 API（`ST_API.ui.registerExtensionsMenuItem`）+ 手动注入
  （`#extensionsMenu`）+ 菜单自愈（MutationObserver 监视菜单重建）。
- `startHostEventWatchdog`：宿主重建 eventSource 时重挂订阅。
- 末尾：`onHostEvent(..., 'appReady', bootstrap, ...)` + `scheduleBootstrapFallback()`。

## 三、改哪里（速查）

| 想做什么 | 改哪里 |
|---------|--------|
| 加 DOM ID / 调超时 / 改默认设置 | `js/constants.js` |
| 改 API 请求 / 设置读写 / 并发 | `js/utils.js` |
| 挂宿主事件（如 generationEnded） | `js/main.js` 的 `installHostEventSubscriptions` |
| 改悬浮球 / 面板 / 加视图入口 | `js/ui-shell.js` |
| 改 API 连接面板 | `js/views-api.js` |
| 改系统日志（捕获 / 视图 / API） | `js/views-log.js` + `js/ui-shell.js` 的 `createPanel` |
| 改首页状态卡片 | `js/views-home.js` + `js/ui-shell.js` 的 `createPanel` |
| 改剧情脉络（数据 / YAML / 导入导出 / 角色卡绑定） | `js/story-data.js` |
| 改剧情预筛（Gate / 注入 / 清理） | `js/story-gate.js` + `js/main.js` 的 `installHostEventSubscriptions` |
| 改剧情脉络界面 | `js/views-story.js`（工作台 UI + 编辑器）+ `js/ui-shell.js`（首页卡片入口） |
| 改样式 / 主题 | `style.css`（`--k-*` 变量） |
| 新增一个源码文件 | 建文件 + 加进 `build.js` 的 `FILES`（放在依赖它的文件之前） |

## 四、给 AI 维护者的提醒

1. **改 `js/`，别改 `index.js`**：`index.js` 是 `node build.js` 的产物，手改会被覆盖。
2. **新增文件必须加进 `build.js` 的 `FILES` 数组**，且放在依赖它的文件之前。
3. **所有文件共享全局作用域**（非 ES module），顶层 `const`/`function` 全局唯一。
4. **热重载安全**：事件订阅、看门狗、ESC 监听都用了 `globalThis[KEY]` 去重；
   新增「全局副作用」时沿用这个模式。
5. **宿主差异**：SillyTavern 与 TauriTavern 的 API 有差异（事件名、getContext、
   setExtensionPrompt 等），统一经 `host.js` / `utils.js` 适配。
6. **改完跑一遍**：`node build.js && node --check index.js`，确认语法无误；
   数据与 UI 行为可跑 `node tests/data.test.js` / `node tests/ui.test.js`（jsdom 已装在 `tests/node_modules`）。
7. **主题机制**：样式全部以 `--k-*` CSS 变量组织，后续加主题只需在 `style.css`
   追加变量覆盖（如 `[data-theme='...']`）。
8. **文档同步**：重要进展记入 `开发日志.md`，踩坑记入 `重要开发经验.md`，架构决策记入 `重要设计.md`。
