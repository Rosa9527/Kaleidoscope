# 剧情脉络文件格式与编写方法

> 万华镜（Kaleidoscope）「剧情脉络」用「节点 + 事件」一套结构通吃两种典型应用场景：
> 一是**规则 / 常驻触发**——节点组织场景 / 状态 / 阶段（日常、房间、睡眠、战斗……），
> 事件是常驻触发规则，`trigger` 写触发时机（如「日常时」「进入房间」「睡觉前」），
> 适合角色养成、生活模拟、状态机式的规则系统；
> 二是**剧情叙事**——节点组织卷 / 章 / 幕（第一卷 → 第一章……），事件是剧情事件，
> `trigger` 写推进条件，适合线性 / 非线性剧情与任务线。
> 运行时都由剧情预筛在每轮对话前判断哪些事件命中，命中才把正文注入上下文。
>
> 本文介绍两种可交换文件：**整包文件**（一份完整的剧情脉络，节点 + 事件）与
> **单事件文件**（一个事件，frontmatter 风格），以及它们的格式、编写方法与导入行为。

## 1. 文件从哪来

- 剧情脉络工作台「导出 剧情脉络」：导出当前角色卡绑定的**整包**剧情脉络。
  - 绑定角色卡：`剧情脉络: 角色卡名.yaml`（如 `剧情脉络: 辉夜大小姐.yaml`）；
  - 群聊 / 未选角色：回退 `万华镜-剧情脉络-时间戳.yaml`。
- 事件编辑器「导出事件」：导出**单事件**文件（`万华镜-事件-名称.yaml`）。
- 两种文件都可以手写，然后通过工作台「导入 剧情脉络」导入（支持 `.yaml` / `.yml`）。

## 2. 两种典型应用场景

- **规则 / 常驻触发模式**：节点 = 场景 / 状态 / 阶段（日常、房间、睡眠、战斗、外出……），
  事件 = 一条常驻规则，`trigger` 写「什么时候触发」（日常时 / 进入房间 / 睡觉前 / 下雨时）。
  适合角色养成、生活模拟、状态机：规则常驻生效，命中就注入对应表现。
- **剧情叙事模式**：节点 = 卷 / 章 / 幕（第一卷 → 第一章 → …），事件 = 剧情事件，
  `trigger` 写「推进到哪一步触发」（第一章开始 / 完成前置事件 / 玩家到达某地）。
  适合线性 / 非线性剧情、任务线：按对话进展逐步推进。
- **两种模式共用同一套文件格式与导入逻辑，也可以混用**（剧情节点下挂常驻规则，
  规则节点下挂剧情事件）；运行时都由剧情预筛判断命中。

## 3. 整包文件格式

整包文件是导入 / 导出的主路径，一份文件包含全部节点（nodes）与事件（scripts）。
下面是完整示例：

```yaml
# 万华镜（Kaleidoscope）剧情脉络导出
# 在「剧情脉络 → 导入 剧情脉络」中可重新导入；同 id 条目会被更新，其余追加。
format: kaleidoscope-story
version: 1
character: 辉夜大小姐
nodes:
  - id: node-life
    parentId: ""
    name: 生活
    description: 日常生活的场景与时段
  - id: node-daily
    parentId: node-life
    name: 日常
    description: 没有特殊场景的日常时段
  - id: node-room
    parentId: node-life
    name: 房间
    description: 进入房间后的场景
  - id: node-sleep
    parentId: node-life
    name: 睡眠
    description: 睡觉前后的时段
scripts:
  - id: "001"
    nodeId: node-daily
    name: 日常问候
    trigger: 日常时
    description: 常驻触发：日常时段命中
    content: |-
      现在是日常时段。辉夜大小姐以平时的姿态回应玩家。
  - id: "002"
    nodeId: node-room
    name: 进入房间
    trigger: 进入房间
    description: 常驻触发：玩家进入房间时命中
    content: |-
      玩家走进房间。辉夜大小姐正坐在窗边看书，闻声抬起头。
  - id: "003"
    nodeId: node-sleep
    name: 睡前叮嘱
    trigger: 睡觉前
    description: 常驻触发：每晚睡觉前命中
    content: |-
      睡觉前，辉夜大小姐提醒玩家关灯、检查门窗，早点休息。
```

### 3.1 顶层字段

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `format` | 推荐 | 自描述标记，固定为 `kaleidoscope-story`；导入时凭它或 `nodes` / `scripts` 段识别文件 |
| `version` | 推荐 | 格式版本，当前为 `1`；导入不校验，建议保留 |
| `character` | 否 | 来源角色卡名；导出时自动写入，导入时仅用于确认框提示，不会强制改名或校验 |
| `nodes` | 否 | 节点列表，可为空（写 `nodes: []`） |
| `scripts` | 否 | 事件列表，可为空（写 `scripts: []`） |

### 3.2 节点（nodes）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 建议 | 节点唯一标识，推荐可读且唯一（如 `node-life`、`node-room`，或 `kaleido-n-xxx`）；缺省会临时生成，但该条目的父子关联无法解析（会变成根节点），**手写时请显式给出** |
| `parentId` | 否 | 父节点 id；空（`""`）或省略 = 根节点；引用不存在的父级会自动回落为根 |
| `name` | 否 | 节点名（空则显示为「未命名节点」） |
| `description` | 否 | 节点说明，可省略 |

节点可层层嵌套（如「生活」→「日常」「房间」「睡眠」），由 `parentId` 串成树；
嵌套越深，规则划分越细（大场景 → 子场景 → 具体状态）。

### 3.3 事件（scripts）

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 否 | 事件 id；工作台内是 `001` 起的可读序列，建议保留；数字样 id 建议加引号（见 3.4） |
| `nodeId` | 否 | 所属节点 id；空（`""`）或省略 = 未分类；引用不存在的节点会自动转未分类 |
| `name` | 建议 | 事件名 |
| `trigger` | 否 | 触发时机 / 条件——规则模式写时机短句（如「日常时」「进入房间」「睡觉前」），剧情模式写推进条件（如「第一章开始」「完成前置事件」）；运行时由剧情预筛在每轮对话前判断是否命中，命中才注入正文 |
| `description` | 否 | 事件说明 |
| `content` | 建议 | 事件正文，多行内容用块文本 `|-`（见 3.4） |

### 3.4 YAML 写法要点（本插件支持的子集）

- **缩进**：列表项 `- ` 与条目字段保持同一缩进层级；同一列表内所有条目同级字段对齐。
  整包文件里 `- id:` 缩进 2 格，字段缩进 4 格；正文行必须比 `content:` 行缩进更深。
- **字符串**：普通文本（中文、英文）可以不加引号；以下情况建议加双引号：
  数字样（`001`、`2026`）、布尔样（`true` / `false`）、含 `: ` 或 `#`、首尾带空白。
- **块文本**：`content: |` 保留末尾换行，`content: |-` 去掉末尾换行；两者都保留正文内的
  空行。事件正文通常用 `|-`。
- **注释**：`#` 开头的行是注释（行内 ` # 说明` 也可以）。
- **空值**：写 `""` 或直接省略字段。
- **不支持**：锚点（`&a` / `*a`）、多文档、流式内嵌（`[a, b]` / `{a: b}`）；
  空集合 `[]` / `{}` 例外，可作空列表 / 空对象。

### 3.5 如何组织节点与事件（两种模式）

- **规则模式**：节点按「场景 / 状态 / 阶段」组织，一个节点代表一类常驻生效的环境；
  事件挂到对应节点下，`trigger` 写时机化短句（「日常时」「进入房间」「睡觉前」
  「下雨时」「好感度达到 3」），`content` 写命中后的角色表现 / 环境描述 / 系统指令。
- **剧情模式**：节点按「卷 / 章 / 幕」组织（第一卷 → 第一章 → …），`trigger` 写剧情
  推进条件（「第一章开始」「完成前置事件」「玩家到达某地」），`content` 写该阶段
  要注入的剧情正文。
- **`description`** 说明事件用途，方便日后维护。
- **同一节点可挂多个事件**：触发时机互斥或叠加都交给剧情预筛判断，命中的事件
  会以 `<Story_Event>` 块一起注入；每轮最多注入 5 个。

## 4. 导入行为

- **识别路由**：文件去掉开头空白后若以 `---` 开头 → 按**单事件**文件处理；
  否则按**整包**文件解析。整包文件请用 `#` 注释开头，不要用 `---` 开头。
- **整包识别**：有 `format: kaleidoscope-story` 标记，或含 `nodes` / `scripts` 段
  即识别为整包（兼容旧版导出文件）；两者都没有时报错。
- **合并而非覆盖**：同 id 条目更新（节点名 / 说明，事件名 / 触发 / 说明 / 正文 / 归属），
  其余条目追加；导入不会清空现有数据。
- **悬空引用自动修正**：`parentId` 指向不存在的节点 → 该节点回落为根；
  `nodeId` 指向不存在的节点 → 事件转未分类；导入集内成环自动断开。
- **确认提示**：导入前确认框显示文件里的 `character`（来源角色卡），如
  「将导入 2 个节点、1 个事件（来自「辉夜大小姐」）」。

## 5. 单事件文件格式

单事件文件是 frontmatter 风格（与 SillyTavern 事件 / 技能式文件兼容），适合单独分享一个事件：

```yaml
---
name: 进入房间
id: 001
Trigger: 进入房间
description: 常驻触发：玩家进入房间时
---
玩家走进房间。辉夜大小姐正坐在窗边看书，闻声抬起头。
```

- 文件必须以 `---` 开头、以 `---` 结尾，正文在结尾分隔线之后。
- 字段：`name`（必填，缺失报错）、`id`（可选，缺省时导入后自动分配、重复自动顺延）、
  `Trigger`（触发条件；大小写兼容，写 `trigger` 也能识别）、`description`（可选）。
- 导入后打开事件编辑器待确认，默认「未分类」；可在确认时修改归属节点再保存。

## 6. 手写示例：从零开始

**最小可用文件**（一个场景节点 + 一条常驻触发规则）：

```yaml
format: kaleidoscope-story
version: 1
nodes:
  - id: node-room
    name: 房间
scripts:
  - id: "001"
    nodeId: node-room
    name: 进入房间
    trigger: 进入房间
    content: |-
      玩家走进房间。辉夜大小姐正坐在窗边看书，闻声抬起头。
```

**加场景分层与更多常驻规则**：

```yaml
format: kaleidoscope-story
version: 1
character: 辉夜大小姐
nodes:
  - id: node-daily
    name: 日常
  - id: node-room
    parentId: node-daily
    name: 房间
scripts:
  - id: "001"
    nodeId: node-daily
    name: 日常问候
    trigger: 日常时
    content: |-
      现在是日常时段。辉夜大小姐以平时的姿态回应玩家。
  - id: "002"
    nodeId: node-room
    name: 睡前叮嘱
    trigger: 睡觉前
    content: |-
      睡觉前，辉夜大小姐提醒玩家关灯、检查门窗，早点休息。
```

**剧情叙事模式示例**（卷 / 章组织，按剧情进展触发）：

```yaml
format: kaleidoscope-story
version: 1
character: 辉夜大小姐
nodes:
  - id: vol-1
    name: 第一卷
  - id: chap-1
    parentId: vol-1
    name: 第一章
scripts:
  - id: "001"
    nodeId: chap-1
    name: 雨夜初遇
    trigger: 第一章开始且下雨
    content: |-
      雨夜，学生会的灯还亮着。
      你推开门，辉夜大小姐正坐在灯下批改文件。
```

编写顺序建议：先规划结构（场景 / 状态树，或卷 / 章 / 幕）与触发时机 → 写 `nodes` →
写 `scripts` 并按 `nodeId` 归属、在 `trigger` 写清触发时机 →
用工作台「导入 剧情脉络」验证。导入后可继续在可视化工作台里调整。

## 7. 常见问题

- **「YAML 缩进异常（第 N 行）」**：列表内字段缩进没对齐，检查空格数。
- **整包文件被当成单事件**：文件以 `---` 开头了；整包文件开头写 `#` 注释或直接写 `format:`。
- **数字样 id 的归一**：`id: 001` 会保留前导零；`id: 100` 会按数字处理再转回 `100`。
  想绝对保险，就给数字样 id 加引号：`id: "001"`。
- **多余字段**：解析器忽略未知字段，可以放心写备注，或直接用 `#` 注释。
- **想清空重建**：导入是合并语义，不会清空现有数据；需要先手动删除旧节点 / 事件。
