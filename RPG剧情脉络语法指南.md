# RPG剧情脉络语法指南

> 本文是《RPG剧情脉络制作指南》的精炼版，只介绍语法与概念：YAML 怎么写、节点怎么组织、`trigger` 怎么写。完整方法与详解见制作指南。
> 适用剧本：带明确日程的 RPG（具体某一天 / 某段时间），如侦探、冒险、日常与主线混合的剧本。

## 一、概念

- **剧情脉络**是一份 YAML 文件，由 `nodes`（目录骨架）与 `scripts`（事件）两块组成。
- **节点**是挂载事件的容器，本身不承载内容；内容全部写在事件里（`trigger` + `content`）。
- **根节点有且只有两个**，本质相同，区别只在触发限制：

| 根节点 | 用途 | trigger 写法 | 使用频率 |
| --- | --- | --- | --- |
| 剧情类事件 | 按时间、地点、人物组织剧情 | 日期、地点、人物与动作 | 主体 |
| 规则类事件 | 全局、永久生效的设定 | 只写触发场景（如「玩家抬头看向太阳时」） | 极少，多数剧本一条不写 |

- **预筛**：预筛 AI 每轮对话前读取事件目录（节点名、事件名、`trigger`、`description`，不含正文）与最近对话，按匹配情况挑选事件并注入正文。两类事件混在同一份清单里统一挑选。

## 二、文件结构

```yaml
format: kaleidoscope-story   # 固定标记，导入时靠它识别
version: 1
character: 暑假侦探           # 可选：角色卡名或主角名
nodes:                       # 目录骨架
  - id: plot-events          # 节点唯一标识，建议可读、唯一
    name: 剧情类事件
    parentId: ""             # 父节点 id；省略或留空 = 根节点
    enabled: true            # 可选：false = 停用整棵子树
scripts:                     # 事件
  - id: "001"                # 事件编号，数字样加引号
    nodeId: plot-events      # 挂在哪个节点下
    name: 事件名
    trigger: 触发条件        # 写法见「四」
    description: 可选，一句话说明用途
    content: |-              # 事件正文，从下一行缩进两格开始
      正文……
```

格式要点：

- 缩进用空格；列表项 `- ` 与字段对齐；正文比 `content:` 多缩进两格。
- 形如数字的 id 加引号（`id: "001"`），否则会被 YAML 解析成数字；其余文本直接写。
- `#` 开头是注释。
- `enabled: false` 停用该节点及整棵子树（不进预筛目录）；省略或 `enabled: true` 为启用。工作台节点行有滑块可切换。

## 三、层级结构

剧情类事件最多四层，每层只允许挂指定类型：

| 层级 | 节点类型 | 命名示例 | 下面只能挂 |
| --- | --- | --- | --- |
| 第 1 层 | 根节点 | 剧情类事件 | 时间节点 |
| 第 2 层 | 时间 | `2026年8月11日` / `2026年7月10日~2026年7月17日` | 地点节点 |
| 第 3 层 | 地点 | 书房 / 商场 / 列车 | 事件或人物节点 |
| 第 4 层 | 人物 | 林晓 / 神秘商人 | 事件 |

规则：

- 时间节点两种写法：某一天 `2026年8月11日`；时间段 `2026年7月10日~2026年7月17日`（用 `~` 连接，全文件日期格式一致）。按时间先后排列节点，导入后目录顺序即文件顺序。
- 事件必须挂在地点或人物下，不挂在时间下。
- 同一地点跨多天时每天各建一个节点（节点树是严格树，不支持跨天共用）；同一天同一地点分阶段可在名字注明：`书房（夜晚）`。
- 人物节点只挂事件，是最后一层；事件少且与地点强绑定时，可直接挂地点下，省略人物层。
- 节点 `description` 会进入预筛目录：时间节点写当日（该时段）剧情目标，地点节点写场景氛围或可做的事。

## 四、trigger

`trigger` 是预筛 AI 判断「现在该不该触发」的唯一依据，标准写法是体现四要素：

**时间 + 地点 + 人物 + 触发动作**

示例：「2026年8月11日，书房，玩家翻找书桌抽屉时」。人物要素体现在动作里即可（「玩家陪路明非购买马桶座圈时」同时写清人物与动作）。

四要素在两类事件中的写法：

| 要素 | 剧情类事件 | 规则类事件 |
| --- | --- | --- |
| 时间 | 所在节点的日期写全：`2026年8月11日`；时间段写 `2026年7月10日至7月17日期间` | 省略；确有限制时写 |
| 地点 | 所在地点节点名；确有多地点通用时省略 | 省略；确有限制时写 |
| 人物 | 体现在动作里；没有特定人物就省略 | 涉及的角色体现在场景里（如「女生」）；没有就省略 |
| 触发动作 | 玩家或角色做了什么：`玩家翻找书桌抽屉时` | 特殊场景：`玩家抬头看向太阳时` |

规则：

- 要素不适用即省略。
- 前置状态并入时间要素（如「好感度达到 3 后」「完成前置事件后」）。
- 第四要素只写对话中可核实的动作或状态（「玩家入睡后」）；事件触发后才会发生的剧情（如「梦见……」）一律写进 `content`。
- 触发动作必须是玩家顺着剧情自然会做的动作；否则放宽条件（改成每天必做的动作），或加一个提示事件引导。
- 条件要窄到只有该场景真正发生时才满足，写清主语、时机与前置状态；不写「合适的时候」这类无法核实的空话。
- 事件正文开头交代日期（如 `2026年8月11日，上午。`），保证预筛 AI 能从最近对话核实游戏内「今天」。

## 五、规则类事件

- 是事件，不是介绍：必须能写成「场景出现 → 发生某事」。人物介绍、世界观设定、力量体系属于角色卡或预设，不写进剧情脉络。
- 全局、永久生效：不受时间、地点、人物约束，触发场景一出现就注入。
- 极少：通常只有一两条，多数剧本一条不写；根节点下没有事件也没关系。
- 挂载：直接挂 `规则类事件` 根节点下；超过三五个时才加一层分类，分类只归组、不承载文本。
- 剧情事件与规则事件各写各的：正文里不互相重复，避免同一信息被重复注入、互相冲突。

## 六、完整示例

```yaml
format: kaleidoscope-story
version: 1
character: 暑假侦探          # 可选：角色卡名或主角名
nodes:
  # ===== 剧情类事件：时间 → 地点 →（人物）→ 事件 =====
  - id: plot-events
    name: 剧情类事件
  - id: t-2026-0710-0717
    parentId: plot-events
    name: 2026年7月10日~2026年7月17日
    description: 返乡列车旅途，第一次目击「隙间」
  - id: loc-train
    parentId: t-2026-0710-0717
    name: 列车
  - id: t-2026-08-11
    parentId: plot-events
    name: 2026年8月11日
    description: 抵达老家的第一天：发现旧笔记，林晓上门求助
  - id: loc-study
    parentId: t-2026-08-11
    name: 书房
  - id: char-lin
    parentId: loc-study
    name: 林晓
  # ===== 规则类事件：极少使用，全局永久生效 =====
  - id: rule-events
    name: 规则类事件

scripts:
  # ----- 剧情事件（挂在 地点 / 人物 下） -----
  - id: "001"
    nodeId: loc-train
    name: 深夜异象
    trigger: 2026年7月10日至7月17日期间，列车，玩家在深夜醒来时
    content: |-
      2026年7月14日，凌晨。
      你从浅眠中醒来，车窗外的景色像被撕开一道黑色的口子——你第一次见到了「隙间」。
  - id: "002"
    nodeId: loc-study
    name: 发现旧笔记
    trigger: 2026年8月11日，书房，玩家翻找书桌抽屉时
    content: |-
      2026年8月11日，上午。
      你拉开书桌最下面的抽屉，一本封皮发黄的笔记压在旧报纸底下，扉页写着爷爷的名字。
  - id: "003"
    nodeId: char-lin
    name: 林晓的求助
    trigger: 2026年8月11日，书房，林晓上门找你时
    content: |-
      2026年8月11日，下午。
      林晓推门进来，脸色发白：「爷爷的书店……昨天晚上出了怪事。」
  # ----- 规则事件 -----
  - id: "101"
    nodeId: rule-events
    name: 只有你能看见隙间
    trigger: 隙间出现或旁人提及隙间时
    content: |-
      隙间是现实世界的裂缝，只有你能看见，其他人毫无察觉；旁人提起时只当作错觉或传说，你也不会主动解释。描写保持神秘感，不一次说透。
```

## 七、自查

语法与结构：

- 文件头带 `format: kaleidoscope-story`；数字样 id 加引号；缩进一致。
- 恰好两个根节点：`剧情类事件`、`规则类事件`。
- 时间下只挂地点；地点下只挂事件或人物；人物下只挂事件。
- 同一地点跨多天时每天各建一个节点。
- 每个事件的 trigger 与所在节点链一致（日期、地点、人物对得上）。

trigger：

- 体现「时间 + 地点 + 人物 + 触发动作」，要素不适用才省略。
- 第四要素是触发动作，不是剧情内容；剧情内容在 content 里。
- 条件不会被日常闲聊误触发；触发动作玩家顺着剧情一定会做。
- 正文开头交代了日期；时间段事件写「某日至某日期间」。

规则事件：

- 是否「任何时候、任何地方、对任何人都生效」？不是就不写。
- 写成「场景出现 → 发生某事」，不是介绍或文档。

## 八、导入

- 「剧情脉络 → 导入 剧情脉络」；可选**合并导入**（同 id 更新，其余追加）或**覆盖导入**（清空后整体替换）。
- 导入保留 `enabled: false` 停用状态；未写 `enabled` 的节点默认启用。
